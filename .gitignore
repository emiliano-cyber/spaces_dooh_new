# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# `.env.production` a secas NO estaba, y en el droplet existe: apps/web/.env.production
# con los secretos de produccion, sin ignorar. Un `git add .` lo habria commiteado.
# Se comprobo que nunca llego a la historia (2026-08-06), asi que no hay nada que
# rotar por esto — pero faltaba la red.
.env.production
# Los respaldos que deja el despliegue al editarlo: en el droplet hay tres
# (.bak.1784062399, .bak.1784755285, .bak.1784838580). Llevan los mismos
# secretos que el original, asi que ignorar solo el original no bastaba.
.env*.bak.*
.env*.bak

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist
*.tsbuildinfo

# Local backup directory created by handoff
backup/


# Debug
dev.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem
.tmp.driveupload/

# Postgres local (datos persistentes del docker-compose)
db/spaces_pgdata/

# Respaldos locales de BD (no se versionan)
migration-backup/

# Python (SDK de integración DOOHmain)
__pycache__/
*.pyc
.venv/
venv/

# Material de referencia de identidad visual (handoff de marca; no se versiona)
Visual identity project Space OS-handoff*.zip
Visual identity project Space OS-handoff*/
visual-identity-project-space-os/
