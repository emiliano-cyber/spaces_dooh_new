===============================================================================
 LO QUE FALTA POR SUBIR — 2026-09-23
 SPACE OS · quedan 21 dias para el OOH SUMMIT (14 de octubre)
===============================================================================

 SUSTITUYE a TODO-MANUAL-20260922.txt. Lo que aquel decia y ya hiciste no se
 repite aqui. Si un bloque te sale "ya estaba", avisa: esta hoja se quedo vieja.

 Medido hoy sobre el arbol, no recordado.


###############################################################################
 LEE ESTO ANTES QUE NADA — EL ORDEN NO ES DECORATIVO
###############################################################################

 EL HALLAZGO DE HOY:

   El arreglo `d9f1aee` (marcar lo instalado cuando la tabla nace en esa
   corrida) NO ESTA EN NINGUNA MAQUINA.

   Por que: `marcar_instalado` vive en `update.sh`, que esta en el ANFITRION.
   Que el arreglo viaje dentro de la imagen v0.6.1 NO LO INSTALA. El PADRE
   hizo `git pull` hasta 80d0bd6 (v0.6.0) el 22/09, y d9f1aee es POSTERIOR.

   Medido:
     git log --oneline 80d0bd6..main -- infra/scripts/update.sh
       c56c89f  (solo prosa, sin logica)
       d9f1aee  <- EL ARREGLO

 QUE PASA SI NO LO HACES EN ORDEN:

   g500 es la que va a ESTRENAR la tabla `actualizaciones_instancia`. Con el
   update.sh de ayer cae en el defecto exacto que ya se midio en DEMO:
   `version_instalada` y `digest_instalado` quedan en NULL, la pantalla
   anuncia una version nueva teniendo esa misma corriendo, y si la apruebas
   la aprobacion NO SE CONSUME NUNCA.

   Resultado: g500 DEJA DE RECIBIR VERSIONES y no da ningun error.
   Es la unica instancia con datos de cliente real.

   REGLA: el update.sh ARREGLADO entra en g500 ANTES de que g500 tome la
          version que crea la tabla. O sea: bloque C antes del bloque D.


###############################################################################
 BLOQUE A · TU MAQUINA (git)  —  3 comandos
###############################################################################

 A1 · EMPUJA `main`. Va 1 commit por delante del remoto.

   cd C:/Users/Server/spaces_doohmain_nueva
   git push emiliano main

   Que es ese commit: c56c89f, SOLO PROSA. Cambia un comentario de update.sh
   y una nota de boveda. Cero lineas de logica.

   CONSECUENCIA UTIL: como d9f1aee YA esta en el remoto, el `git pull` del
   bloque B trae el arreglo AUNQUE no empujes esto. Empujarlo es orden, no
   requisito. No te bloquees aqui.

   Si te lo rechaza por proteccion de rama: la quitas en GitHub, repites, y la
   vuelves a poner. Decidiste push directo, no PR.


 A2 · LA RAMA DEL RESPALDO DIARIO. NUNCA SE HA EMPUJADO Y NO ESTA FUSIONADA.

   Son 10 commits, con su arnes en verde (18 escenarios, 67 comprobaciones,
   0 fallos) y sin una linea de update.sh en el diff.

   Lo que construye: `infra/scripts/respaldo-diario.sh`, para que una
   instancia respalde TODOS LOS DIAS y no solo cuando se despliega una version
   -que es la regla real de hoy, y por eso el ultimo respaldo de g500 en el
   bucket es del 17/09-.

   cd C:/Users/Server/spaces_doohmain_nueva
   git checkout main
   git merge feat/respaldo-diario
   cd apps/web
   npm run typecheck
   npm test
   cd ../..
   git push emiliano main

   NO lo fusiones sin correr esas dos. Si alguna sale roja, PARA y avisa.


 A3 · LA RAMA DE TICKETS NO LA TOQUES TODAVIA.

   `feat/tickets-de-soporte` esta a medio construir en esta sesion (ADR 0038).
   No la fusiones ni la empujes hasta que te diga que paso su revision.


###############################################################################
 BLOQUE B · EL PADRE  —  consola web del droplet <IP-del-PADRE>
###############################################################################

 B1 · TRAE EL ARREGLO:

   cd /var/www/Spaces
   git pull

 B2 · COMPRUEBA QUE LLEGO — esta es la que separa hacerlo de creer que lo
      hiciste:

   git log --oneline -3 -- infra/scripts/update.sh

   TIENE QUE APARECER `d9f1aee` (o el merge 11b4225) en la lista.
   Si no aparece, el pull no trajo el arreglo: PARA y avisa.


###############################################################################
 BLOQUE C · REINSTALAR update.sh EN EL PADRE (que es donde vive DEMO)
###############################################################################

 El update.sh de /opt/space-os/ es el que usan el PADRE y DEMO. Hoy es el de
 ayer, SIN el arreglo.

 C1 · Guarda el que hay:

   cp -a /opt/space-os/update.sh /root/update.sh.antes-d9f1aee

 C2 · Copia el arreglado:

   install -o root -g root -m 750 /var/www/Spaces/infra/scripts/update.sh /opt/space-os/update.sh

 C3 · COMPRUEBA QUE ES EL ARREGLADO, no solo que es "nuevo":

   grep -c 'sin-tabla' /opt/space-os/update.sh

   Debe dar un numero MAYOR QUE 0. Ese texto lo introduce d9f1aee.
   Si da 0, copiaste el viejo: vuelve a C2.

 C4 · Que no rompiste DEMO (ojo al SPACE_OS_CONF, DEMO no usa el de omision):

   SPACE_OS_CONF=/etc/space-os/demo-instancia.env /opt/space-os/update.sh --dry-run

   Debe terminar en:
     "--dry-run terminado. Ni base, ni contenedor, ni respaldo: nada cambio."

   Si no, vuelve atras y NO SIGAS:
     cp -a /root/update.sh.antes-d9f1aee /opt/space-os/update.sh


###############################################################################
 BLOQUE D · g500  —  LA UNICA CON DATOS REALES. NUNCA SE HA HECHO.
###############################################################################

 SOLO DESPUES de que C3 haya dado un numero mayor que 0.

 Estado medido de g500 (22/09):
   update.sh    el del 9 de septiembre, 117 820 bytes. El nuevo NO esta.
   canal        estable        version   v0.5.1
   base         spaces         montaje   ESTANDAR (sin SPACE_OS_CONF)
   cron         una sola linea, 17 4 * * *
   acceso       ssh -i ~/.ssh/spaces_deploy   (el PADRE NO tiene llave a g500)

 D1 · LLEVARLE EL ARCHIVO. g500 no tiene el repositorio.

   DESDE TU MAQUINA (es el camino que consta que funciona):

   scp -i C:/Users/Server/.ssh/spaces_deploy C:/Users/Server/spaces_doohmain_nueva/infra/scripts/update.sh root@<IP-de-g500>:/opt/space-os/update.sh

   OJO: esto copia EL DE TU MAQUINA. Asegurate de estar en `main` con A1 hecho:
     git branch --show-current     # tiene que decir main

 D2 · EN LA CONSOLA DE g500, y en este orden:

   cp -a /opt/space-os/update.sh /root/update.sh.antes-adr0037
   chown root:root /opt/space-os/update.sh
   chmod 750 /opt/space-os/update.sh

   grep -c 'sin-tabla' /opt/space-os/update.sh      # > 0 o PARAS
   /opt/space-os/update.sh --help | grep -c comprobar   # > 0 o PARAS

   /opt/space-os/update.sh --dry-run                # "nada cambio"
   /opt/space-os/update.sh --comprobar              # la primera corrida real

   ESPERADO EN --comprobar, Y ES CORRECTO:
     "actualizaciones_instancia no existe todavia en esta instancia"
   Porque g500 corre v0.5.1 y la migracion viaja dentro de la imagen.

 D3 · EL CRON DE COMPROBACION. Montaje estandar, SIN SPACE_OS_CONF:

   printf '%s\n' '*/15 * * * * root /opt/space-os/update.sh --comprobar >> /var/log/space-os/cron.log 2>&1 || [ $? -eq 75 ]' >> /etc/cron.d/space-os-update

   grep -cE '^\*/15 |^17 4 ' /etc/cron.d/space-os-update    # debe dar 2


###############################################################################
 BLOQUE E · PROMOVER A `estable`  —  lo que hace que g500 reciba todo esto
###############################################################################

 SOLO DESPUES del bloque D COMPLETO. Si promueves antes, g500 toma la version
 con el update.sh viejo y entra en el defecto del recuadro de arriba.

 E1 · En GitHub -> Actions -> "Promover a estable" -> Run workflow.
      Version: v0.6.1

      Escribe la version SIN espacios delante ni punto detras. Los guards la
      imprimen entre comillas justo para que veas si se colo alguno: los tres
      primeros intentos del 02/09 murieron por eso.

 E2 · La siguiente corrida de g500 (17 4 * * *, o lanzala a mano) tomara
      v0.6.1, aplicara la migracion y CREARA la tabla.

      A partir de ahi g500 esta en modo `aprobacion` y NO se actualiza sola.
      Es lo decidido: el cliente real usa la capacidad de decidir.

 E3 · COMPRUEBA que el marcado quedo bien -esto es lo que el arreglo d9f1aee
      viene a garantizar-:

   sudo -u postgres psql -d spaces -c "select version_instalada, digest_instalado is not null as tiene_digest, version_disponible from actualizaciones_instancia;"

      version_instalada TIENE QUE DECIR v0.6.1 y tiene_digest TIENE QUE SER t.
      Si salen NULL, el update.sh que corrio era el viejo: avisa.


###############################################################################
 BLOQUE F · EL RESPALDO DIARIO  —  depende de A2
###############################################################################

 Hoy g500 solo respalda fuera de su droplet CUANDO SE DESPLIEGA UNA VERSION.
 Lo que hay en el bucket es del 17/09 y lo subiste tu a mano.

 La tarjeta con los comandos es `12-instalar-respaldo-diario.txt`, que ya esta
 en Descargas. NO la corras hasta hacer A2: el guion no existe en el PADRE
 mientras esa rama no este fusionada y traida con `git pull`.


###############################################################################
 BLOQUE G · LO QUE NO ES CONSOLA  —  panel de DigitalOcean
###############################################################################

 G1 · DAR A LA LLAVE DE SPACES ACCESO AL BUCKET `space-os-logs`.

   Medido: la misma llave entra en `space-os-respaldos` y da 403 en
   `space-os-logs`. En DigitalOcean el permiso es POR BUCKET.

   Mientras siga asi, el log de cada corrida se queda en el droplet y para
   saber si algo fallo hay que entrar al servidor -que es justo lo que este
   modelo viene a evitar-. Tambien afectara al respaldo diario del bloque F.

   No es codigo y no hay comando: es el panel.


###############################################################################
 RESUMEN EN UNA LINEA POR BLOQUE
###############################################################################

   A · empuja main (1 commit) y decide si fusionas el respaldo diario
   B · git pull en el PADRE  -> tiene que aparecer d9f1aee
   C · reinstala update.sh en el PADRE -> `grep -c sin-tabla` > 0
   D · g500: copiale el update.sh arreglado y ponle el cron  [NUNCA HECHO]
   E · promueve v0.6.1 a estable  -> SOLO despues de D
   F · respaldo diario: tarjeta 12, despues de A2
   G · panel de DO: acceso al bucket de logs

 LO QUE NO ESTA EN ESTA HOJA PORQUE NO DEPENDE DE TI:
   la rama feat/tickets-de-soporte, que se esta construyendo ahora.
