============================================================
 OBSOLETA -- NO LA CORRAS. Usa en su lugar:
   docs/evidencias/padre-desplegar-27-agosto.txt
============================================================
 Esta tarjeta describia solo F5.1 y F5.2. Desde entonces
 entraron los arreglos de la auditoria y DATA-02, y dos de
 sus afirmaciones dejaron de ser ciertas:
   - decia que NO hacia falta `npm install`. Ahora SI.
   - decia que no habia migracion. Ahora hay una.
 Correrla tal cual dejaria el despliegue a medias.
============================================================

============================================================
 DESPLEGAR F5.1 + F5.2 EN EL PADRE
 Todo en [PADRE] 137.184.107.53.
 Version 2 -- la version 1 tenia tres errores, corregidos abajo.
============================================================

 >>>  PEGA UNA LINEA CADA VEZ.                          <<<

============================================================
 LO QUE SE DESPLIEGA DE VERDAD -- SON DOS TAREAS, NO UNA
============================================================
 La version 1 de esta tarjeta decia "desplegar F5.2". Era
 incompleto: el `git pull` trae TAMBIEN F5.1, y F5.1 toca el alta
 de organizaciones EN PRODUCCION.

 F5.1 -- `crearOrgConDueno` pasa a ser UNA TRANSACCION.
   Antes eran dos escrituras sueltas: si fallaba la del usuario,
   la organizacion sobrevivia sin nadie dentro y ocupaba su slug
   para siempre. Ahora nacen juntas o no nace ninguna.
   Toca: `db.ts`, `cuentas-controller.ts`, `tenant.ts`,
   `usuarios-repo.ts`. Es el camino que usas tu para dar de alta
   organizaciones desde el panel.

 F5.2 -- `POST /api/bootstrap`, el alta de la primera
   organizacion de una instancia nueva. Llega APAGADA (ver abajo).
   Toca: `app/api/bootstrap/route.ts`, `middleware.ts`.

 Los dos son ROJO. Verificado en local: typecheck limpio, 858
 unitarias en 79 archivos, e2e 22 archivos / 227 + 1 omitida.
 `aislamiento.e2e.test.ts`, `servidor-e2e.ts` y `db/schema.sql`
 sin tocarse.

============================================================
 LO QUE **NO** LLEVA
============================================================
 - NO hay migracion. Siguen siendo 73 archivos y 72 aplicadas.
 - NO hay `npm install`: ni `package.json` ni el lock cambiaron.

 Los dos medidos contra el repo, no supuestos.

============================================================
 POR QUE LA RUTA NUEVA LLEGA APAGADA
============================================================
 `/api/bootstrap` exige TRES cosas a la vez:
   1. `BOOTSTRAP_TOKEN` en el entorno  -> el PADRE no lo tiene
   2. que el token coincida
   3. que la tabla `tenants` este VACIA -> `spaces_prod` tiene
      `rgb` y `spaces_demo` tiene `demo`

 Fallan la 1 y la 3. **No hay que configurar nada.** El paso V8
 lo comprueba en vez de suponerlo.

============================================================
 TRES TRAMPAS DE ESTA MAQUINA
============================================================
 1. EL 3000 Y EL 3001 COMPARTEN BUILD. Los dos sirven desde
    /var/www/Spaces y desde el MISMO `.next`. Un solo build vale
    para ambos, pero **hay que reiniciar LOS DOS**: el que no
    reinicies sigue sirviendo el codigo viejo desde memoria, y eso
    no da ningun error -- solo se comporta distinto que su vecino.

 2. LA UNIDAD DE SYSTEMD ES UN ENLACE AL REPO.
    /etc/systemd/system/spaces-demo.service -> apunta dentro de
    /var/www/Spaces/infra/systemd/ (asi se instalo, bloque 2).
    Es decir: **el `git pull` le puede cambiar la unidad por
    debajo**, y systemd seguira usando la vieja hasta que se le
    diga. Por eso el V6 hace `daemon-reload` SIEMPRE.

 3. `next start` NO COMPILA. Sirve lo que haya en `.next`. Sin el
    build, el codigo nuevo no entra por mucho que reinicies.
============================================================


# ------------------------------------------------------------
# V0 - ANCLA DE VUELTA ATRAS. No te saltes este.
# ------------------------------------------------------------
# Apunta lo que salga. Es a donde se vuelve si algo sale mal, y
# sin esto la vuelta atras es a ciegas.

cd /var/www/Spaces && git rev-parse --short HEAD && git status --porcelain

# Esperado: un hash (APUNTALO) y la segunda parte VACIA.
#
# >>> Si `git status` imprime algo, PARA y pasamelo. Significa que
#     hay archivos modificados en el servidor -- que no deberia
#     haberlos-- y el `git pull` fallara o se los llevara por
#     delante.


# ------------------------------------------------------------
# V1 - Sitio para el build
# ------------------------------------------------------------

df -h / | tail -1; free -h | head -2

# Esperado: al menos ~2 GB libres en disco.
# La maquina tiene 4 GB de RAM y el build es lo mas pesado que
# corre. Si va justa, es la causa mas probable de que muera sin
# mensaje claro.


# ------------------------------------------------------------
# V2 - Traer el codigo
# ------------------------------------------------------------

git pull && git log --oneline -1 && git merge-base --is-ancestor 0b7e10a HEAD && echo LLEGO-F52 || echo FALTA-F52

# Esperado:  LLEGO-F52
#
# No se compara contra un hash concreto A PROPOSITO. La version 1 de
# esta tarjeta lo hacia y quedo desfasada en cuanto se commiteo la
# propia tarjeta encima: el hash que anunciaba ya no era el ultimo, y
# quien la corriera no sabria si parar.
#
# `merge-base --is-ancestor` pregunta lo que de verdad importa --si el
# codigo de F5.1 y F5.2 esta dentro de lo que acabas de traer-- y eso
# no caduca por muchos commits que se hagan despues.
#
# >>> FALTA-F52 = el pull no trajo el codigo. PARA.


# ------------------------------------------------------------
# V3 - Que bajo lo que creemos
# ------------------------------------------------------------

ls -l apps/web/app/api/bootstrap/route.ts && ls db/migrations/*.sql | wc -l

# Esperado: la ruta existe, y  73  migraciones.
#
# Se mira ANTES del build: si el pull trajo menos de lo que
# creemos, el build saldria verde igual y el fallo apareceria
# mucho mas tarde.


# ------------------------------------------------------------
# V4 - Que la unidad de systemd no cambio a tus espaldas
# ------------------------------------------------------------
# Sustituye HASH_DEL_V0 por el hash que apuntaste en el V0.

git diff --name-only HASH_DEL_V0..HEAD -- infra/systemd/ db/migrations/ package.json package-lock.json apps/web/package.json

# Esperado: VACIO.
#
# >>> Si aparece `infra/systemd/...`, la unidad de DEMO cambio y el
#     `daemon-reload` del V6 es obligatorio (ya esta en el V6).
# >>> Si aparece algo de `db/migrations`, PARA: habria migracion y
#     esta tarjeta dice que no hay.
# >>> Si aparece un `package*.json`, PARA: haria falta
#     `npm install` y esta tarjeta dice que no.


# ------------------------------------------------------------
# V5 - EL BUILD. Aqui esta el riesgo del despliegue.
# ------------------------------------------------------------

cd /var/www/Spaces/apps/web && npm run build

# Esperado: termina con la tabla de rutas y sin errores, y en esa
# tabla aparece una linea `/api/bootstrap`.
#
# >>> SI EL BUILD FALLA: **NO REINICIES NADA**. Los dos procesos
#     vivos siguen sirviendo lo que tienen cargado, o sea que el
#     sitio aguanta. Un `.next` a medio escribir mas un restart =
#     sitio caido. Pasame el error tal cual.


# ------------------------------------------------------------
# V6 - Reiniciar LOS DOS procesos
# ------------------------------------------------------------
# Primero averigua quien supervisa el 3000, en vez de suponerlo:

systemctl is-active spaces-web 2>/dev/null; pm2 list 2>/dev/null | head -20

# Segun lo que salga, UNA de estas dos:

pm2 restart spaces-web --update-env && sleep 5 && pm2 list

systemctl restart spaces-web && sleep 5 && systemctl is-active spaces-web

# Y AHORA el 3001. El `daemon-reload` va SIEMPRE, por la trampa 2:

systemctl daemon-reload && systemctl restart spaces-demo && sleep 5 && systemctl is-active spaces-demo

# Esperado:  active


# ------------------------------------------------------------
# V7 - LA PRUEBA QUE TOCA LA BASE
# ------------------------------------------------------------
# Es la unica que no se puede fingir. El PADRE estuvo CUATRO DIAS
# sirviendo un login perfecto sin poder autenticar a nadie, y
# ninguna comprobacion de las que haciamos lo veia: un 200 en el
# login es solo una pagina pintandose.

curl -s -o /dev/null -w 'login-post %{http_code}\n' -X POST https://space-os.io/spaces-dooh/api/auth/login/ -H 'Content-Type: application/json' -d '{"email":"no@existe.invalid","password":"x"}'

# Esperado:  401
#
# >>> 500 = el codigo y la base no encajan -> VUELTA ATRAS (abajo).
#     Antes de nada, pasame:
#         journalctl -u spaces-web -n 40 --no-pager
#     (o `pm2 logs spaces-web --lines 40 --nostream`)


# ------------------------------------------------------------
# V8 - Que la ruta nueva ESTA, y que esta CERRADA
# ------------------------------------------------------------
# Estas dos lineas van juntas y ninguna sirve sola.
#
# El 404 por si solo NO demuestra nada: es tambien lo que responde
# una ruta que no existe, o sea lo que veriamos si el despliegue
# no hubiera entrado.

ls -d /var/www/Spaces/apps/web/.next/server/app/api/bootstrap

# Esperado: la carpeta existe -> la ruta esta en el artefacto.

curl -s -o /dev/null -w 'bootstrap %{http_code}\n' -X POST https://space-os.io/spaces-dooh/api/bootstrap/ -H 'Content-Type: application/json' -H 'x-bootstrap-token: loquesea' -d '{}'

# Esperado:  404
#
# Compilada + 404 = los cerrojos hacen su trabajo.
# Compilada + 201 = PARA Y AVISAME AHORA MISMO.


# ------------------------------------------------------------
# V9 - Que el sitio sigue en pie
# ------------------------------------------------------------

curl -s -o /dev/null -w 'login %{http_code}\n' https://space-os.io/spaces-dooh/login/; curl -s -w '\nHTTP %{http_code}\n' -X POST https://space-os.io/spaces-dooh/api/signup/ -H 'Content-Type: application/json' -d '{}'

# Esperado:  login 200   y   HTTP 503  (el registro sigue cerrado)


# ------------------------------------------------------------
# V10 - Las dos bases siguen alineadas
# ------------------------------------------------------------
# No hay migracion nueva, pero que se desalineen ya paso: a
# `spaces_prod` le faltaba la 72 y nadie lo vio en dias.

sudo -u postgres psql -d spaces_prod -Atc "select count(*) from schema_migrations"; sudo -u postgres psql -d spaces_demo -Atc "select count(*) from schema_migrations"

# Esperado:  72   y   72
# (72 aplicadas de 73 archivos: la de datos se omite a proposito.)


# ------------------------------------------------------------
# V11 - LO QUE NINGUNA PRUEBA AUTOMATICA HACE. Dos cosas.
# ------------------------------------------------------------
# (a) GUARDAR ALGO. Entra por el navegador a
#     https://space-os.io/spaces-dooh/login/ y edita tu perfil.
#
#     Este despliegue toca `middleware.ts`, que protege TODAS las
#     operaciones de guardado. Si algo se rompio ahi, el sintoma
#     es un error de token al guardar.
#
# (b) DAR DE ALTA UNA ORGANIZACION DE PRUEBA desde el panel, y
#     luego BORRARLA.
#
#     Esto es lo que cambia F5.1 y es lo unico que lo prueba en
#     produccion. Si el alta devuelve error, la transaccion nueva
#     no encaja con la base real y hay que volver atras.


# ############################################################
#  VUELTA ATRAS -- cuando usarla
# ############################################################
# Dispara si ocurre CUALQUIERA de estas:
#   - el V7 devuelve 500 en vez de 401
#   - el V9 no devuelve login 200
#   - el V11(b) no deja crear una organizacion
#
# Sustituye HASH_DEL_V0 por el hash que apuntaste. Vuelve al
# codigo anterior, reconstruye y reinicia:

cd /var/www/Spaces && git checkout HASH_DEL_V0 && cd apps/web && npm run build

# Y despues los dos reinicios del V6.
#
# NO hace falta tocar la base: este despliegue no lleva migracion,
# asi que no hay nada que revertir en Postgres.
#
# Cuando termines la vuelta atras, avisame con el error del V7.


============================================================
 CUANDO TERMINES
============================================================
 Pegame las salidas del V7, el V8, el V10 y el V11. Con esas
 cuatro queda registrado el despliegue en el expediente.

 Recuerda: **no hay que poner `BOOTSTRAP_TOKEN` en el PADRE.**
 Esa variable se pone en el `.env` de una instancia HIJA nueva,
 el dia que se aprovisione, y nunca aqui.
============================================================
