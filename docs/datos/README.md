# Correcciones de DATOS en producción

Scripts de un solo uso que se ejecutaron **a mano contra `spaces_prod`**, con su
rollback al lado. No son migraciones.

**No van en `db/migrations/`** a propósito: aquéllas describen el ESQUEMA y se
aplican a cualquier entorno. Éstas tocan filas concretas de un tenant concreto,
con UUID escritos a mano; correrlas en otro entorno no haría nada útil, y
correrlas dos veces en el mismo tampoco es lo que se quiere.

Están aquí para que quede rastro de qué se cambió, cuándo y cómo deshacerlo:
un cambio de datos no aparece en `git log` de ninguna otra forma.

## Convenciones

- Un par de archivos por intervención: `<fecha>_<asunto>.sql` y su
  `<fecha>_<asunto>_rollback.sql`.
- El rollback se captura **antes** de aplicar, leyendo los valores previos
  reales — no se escribe de memoria.
- Todo por **id explícito**. Nunca `where nombre like 'TEST%'`: un patrón así
  alcanza mañana un registro nuevo del usuario.
- Antes de aplicar, pasada en seco: el mismo archivo con `commit` cambiado por
  `rollback`, comprobando que el número de filas tocadas es el previsto.

## Historial

| Fecha | Archivo | Qué hizo |
|---|---|---|
| 2026-09-10 | `20260910_pais_mx_g500.sql` | **PREPARADA, sin aplicar al escribirse.** Las **12 pantallas de g500** estaban guardadas con `pais = 'PE'` — direcciones de CDMX y Edomex registradas como Perú, y la **liga pública** de la pantalla imprime la ubicación. No lo causó el traslado del 09/09: venían así del droplet viejo, porque `sitios.pais` era `not null default 'PE'` desde el origen peruano del producto. ⚠️ **Va acompañada de una migración y las dos hacen falta**: `db/migrations/20260910_pais_sin_default.sql` impide que vuelva a pasar —quita el default y el `not null`— pero **no toca ninguna fila a propósito**, porque cambiar datos de un cliente no puede ser efecto colateral de un despliegue. Este script funciona con o sin esa migración aplicada: escribe un valor, no un null. Acotado por el valor previo **y** por la organización, con guard que aborta si no toca exactamente 12 filas. **Corre antes de `migracion-g500-retirar-llaves.txt`**, que retira el acceso SSH. Tarjeta: `docs/evidencias/g500-pais-mx-20260910.txt`. Rollback: `20260910_pais_mx_g500_rollback.sql`. |
| 2026-09-09 | `20260909_desbloquear_cambios_padre.sql` | 🚫 **PREPARADA Y NO APLICADA — y conviene saber por qué.** El bloqueo se resolvió esa misma noche por otra vía: el Dueño **apagó el control entero** desde la aplicación (ver la fila del 28/08). Estos archivos **siguen sirviendo, y harán falta**: el día que se vuelva a encender el control, primero hay que fijar una contraseña utilizable con esto y **después** encenderlo — en ese orden, o se repite el encierro. **No se borren.** Pone `debe_cambiar_password = true` en **una** fila (`emiliano@asnetwork.io`) del PADRE, para reabrir la excepción del **ADR 0018** y poder fijar una contraseña nueva sin teclear la anterior: la del control de cambios se perdió, y el PADRE **no tiene correo saliente** que sirva de recuperación. ⚠️ **No apaga ningún control** — `exigir_reautenticacion` sigue en `t` — ni fija ninguna contraseña, ni toca a los otros dos usuarios. La bandera **se apaga sola** al guardar la contraseña (`usuarios-repo.ts:159-166`), así que el estado es transitorio. Comprobado antes de escribirlo, no supuesto: con la bandera puesta `exigir()` corta las rutas con módulo, pero `/api/auth/me` y `/api/perfil` resuelven con `usuarioActual()` y **no pasan por ese guard** (`auth.ts:198-203`), así que la pantalla donde se cambia sigue alcanzable. Precondición verificada: ese usuario tiene identidad de Google vinculada desde el 25/08 — **sin ella este cambio encerraría al usuario**. Acotado por correo (unicidad global) **y** por el valor previo, con guard que aborta si no toca exactamente 1 fila. Tarjeta: `docs/evidencias/padre-desbloquear-cambios-20260909.txt`. Rollback: `20260909_desbloquear_cambios_padre_rollback.sql`. |
| 2026-09-09 | `20260909_carga_g500*.sql` | ✅ **APLICADA el 2026-09-09**, con los cinco GATE en verde y verificación dentro de la propia transacción (`docs/evidencias/migracion-g500-E4-resultado.md`). Trae los datos de `g500` desde el droplet de julio hasta su instancia dedicada: **541 filas** más la configuración y los 8 contadores de folio. Es la única entrada de este directorio que **no toca `spaces_prod`** sino la base de una instancia, y la única que va acompañada de un plan (`docs/Plan_Migracion_Datos_g500.md`) y un censo (`docs/evidencias/migracion-g500-E2-censo.md`). ⚠️ **Son cinco archivos y ninguno es el que se aplica**: el que se aplica, `carga-g500.sql` (8.5 MB), es la concatenación de tres de ellos y **no se versiona a propósito** — son datos comerciales de un cliente. Se regenera con las tres órdenes de `20260909_carga_g500.md`. Lleva dentro el **rescate de las 12 modalidades** que la deriva del `DEFAULT` etiquetó como `rgb`: sin él, las 12 pantallas de g500 llegan **sin un solo precio y sin dar error**. No trae personas ni bitácora. Rollback: `20260909_carga_g500_rollback.sql`, que deshace las 541 filas pero **no** `config_negocio` ni `folios_consecutivos` — para esos, la captura previa de la tarjeta E4. |
| 2026-08-28 | `20260828_reautenticacion_encendida.sql` | ⚠️ **REVERTIDO EN EL PADRE el 2026-09-10 00:42 UTC**, desde la aplicación y no con SQL: el Dueño apagó el interruptor (`PUT /api/cambios`, que no pide contraseña) porque la contraseña del control se había perdido. Medido: `rgb → exigir_reautenticacion = f`, y la propia aplicación lo registró en la bitácora («Desactivó el control de cambios»). **Hoy el PADRE no exige reautenticación en las ocho rutas que este cambio protegía, tres de ellas de dinero.** Para volver a encenderlo hace falta ANTES fijar una contraseña utilizable — ver la entrada del 09/09. Lo de abajo describe lo que hizo el script, que sigue siendo cierto para las demás organizaciones. Enciende `tenants.exigir_reautenticacion` en las organizaciones **que ya existen**. Con el candado abierto, las ocho rutas que llaman a `exigirCambioSensible()` no pedían nada —tres de ellas mueven dinero: facturar, cobrar y pagar renta—; el permiso del rol seguía aplicando, lo que faltaba era comprobar que quien está al teclado es esa persona. El cambio de ESQUEMA que hace que toda instancia nueva nazca así es `db/migrations/20260828_reautenticacion_por_defecto.sql`, que **no toca ninguna fila** a propósito. ⚠️ **Único caso de este directorio que NO va por id explícito**, y la cabecera explica por qué: se quiere alcanzar también a las organizaciones creadas entre escribirlo y aplicarlo, y el peor caso es cerrar un candado de más. Rollback: `20260828_reautenticacion_encendida_rollback.sql` — **se deja incompleto a propósito**, hay que capturar los slugs antes de aplicar. |
| 2026-08-25 | `20260825_nombre_tenant_rgb.sql` | El encabezado del PADRE decía «RGB CATORCE» y debía decir solo **«RGB»**. Vive en `tenants.nombre` — comprobado que **no** está en `config_negocio`, que en esta base ni siquiera tiene columnas de nombre, ni en `razon_social`/`nombre_comercial`, que estaban **vacías**. Ese vacío es lo que lo vuelve un cambio de rótulo y no un dato fiscal. 1 fila. Acotado por `slug` **y** por el valor previo, para no pisar un cambio ajeno. **Aplicado el 2026-08-25**, con pasada en seco previa (`UPDATE 1` + `ROLLBACK`) y luego `UPDATE 1` + `COMMIT`. Rollback: `20260825_nombre_tenant_rgb_rollback.sql`. |
| 2026-08-04 | `20260804_a9_limpieza_datos_prueba_g500.sql` | Hallazgo **A9** de la auditoría QA: quitó los rastros de prueba visibles en la demo del tenant G500 (prefijos `TEST_`, nombres `WhatsApp Image…`) y corrigió de paso el rango de fechas invertido de la campaña que sostenía el importe negativo de **C2**. 13 filas. Rollback: `20260804_a9_rollback_g500.sql`. |
| 2026-08-10 | `20260810_inc05_residuos_demo_g500.sql` | **INC-05** del plan de incidencias: los rótulos de prueba que se ven en la demo. Nombre comercial `DEMO PIXELED.` → `PIXELED` (vive en `tenants`, no en `config_negocio` como decía el plan: esa columna se retiró en el ADR 0011), creativo `upsivale 1920.jpg` → `creativo-kfc.jpg` (solo el rótulo; `archivo_url` intacto) y el usuario `DEMO` → `Operador Demo` (sin borrarlo: tiene historial en bitácora). 3 filas + su entrada de bitácora. **Aplicado el 2026-08-10** con captura previa y ensayo en rollback. **NO se tocaron las 2 órdenes de trabajo sin responsable** — ver la cabecera del script. Rollback: `20260810_inc05_residuos_demo_g500_rollback.sql`. |
| 2026-08-06 | `20260806_m13b_razon_social_g500.sql` | Hallazgo **M13b**: la razón social de `g500` seguía siendo `DEMO RGB CATORCE S DE RL DE CV` — quedó fuera del A9 a propósito, para resolverse junto con M5. Recorta el prefijo y deja `RGB CATORCE S DE RL DE CV`, que es la razón social **legal** de la misma organización cuyo nombre comercial es G500 (confirmado el 06/08). Importa porque `tenants.razon_social` es la parte arrendataria del contrato que se manda a firma. 1 fila. **Aplicado el 2026-08-06**, con captura previa y ensayo en rollback. Rollback: `20260806_m13b_razon_social_g500_rollback.sql`. |
