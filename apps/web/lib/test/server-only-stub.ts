// Stub de `server-only` para las pruebas (ver vitest.config.ts). El paquete real
// lanza un error si se importa fuera de un React Server Component, lo que impide
// probar los controllers en Node.
export {}
