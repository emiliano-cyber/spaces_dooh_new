---
tipo: diario
estado: verificado
actualizado: 2026-09-03
tags: [agentes, diario, instancias, f5-6, tarjeta-humana, boveda]
archivos:
  - docs/evidencias/TH-F5.6_ensayo-alta-droplet-desechable.txt
  - docs/Plan_Instancias_Soberanas_v3.md
  - docs/runbook-alta-de-owner.md
  - apps/web/app/api/bootstrap/route.ts
  - apps/flota/README.md
---

# 2026-09-03 — la tarjeta de F5.6 no cerraba F5.6

> **El día en una frase:** antes de gastar un droplet se cotejó la tarjeta `TH-F5.6`
> contra el repositorio y contra su propia ficha del plan, y **le faltaban cuatro
> comprobaciones del criterio de aceptación — las cuatro contra la aplicación**.

## Agentes activos

| Agente | Zona | Rama | Ventana |
|---|---|---|---|
| Claude (sesión interactiva) | Z12 · Docs | `main` | mañana |

## Completado

- **`TH-F5.6` corregida: nace el BLOQUE 6b** — working tree · archivos:
  `docs/evidencias/TH-F5.6_ensayo-alta-droplet-desechable.txt:404-437`
  - La tarjeta comprobaba `login` 200 y `metodos` 200, el navegador, `s3cmd`, P13 y
    el conteo de migraciones. **No comprobaba** ninguna de las cuatro que el plan
    exige (`Plan_Instancias_Soberanas_v3.md:1660-1663`):
    `signup` **503** (autoregistro cerrado) · `login-post` **401** ·
    el **segundo `--bootstrap` en 404** · `update.sh --dry-run` diciendo
    **«sin cambios»**.
  - Verificado con: `grep -niE '404|autoregistro|sin cambios|update\.sh|segundo
    intento'` sobre la tarjeta anterior → **cero coincidencias**.
  - Copia al día en `C:\Users\Server\Downloads\`, que es de donde se pega.

- **Corregido un comentario que afirmaba lo contrario de la decisión vigente** —
  working tree · `apps/web/app/api/signup/route.ts:13-36`
  - Decía que DEMO era **«la única con el registro abierto»**. Es falso desde el
    **20/08** (P8 cerró el autoregistro en toda la flota) y la plantilla del alta
    envía `AUTOREGISTRO=0` (`infra/env/app.env.example:80`).
  - **No es una errata inofensiva:** es la frase que me hizo proponer encender
    `AUTOREGISTRO=1` en el ensayo de F5.6, que habría ensayado una instancia
    distinta de la que recibe un cliente. Emiliano lo atajó.
  - El comentario nuevo **no afirma estado que no se pueda medir**: dice qué decidió
    P8 y remite a `GET /api/auth/metodos` para el valor efectivo, que vive en el
    `.env` del servidor y no en el repo.
  - Verificado con: `cd apps/web && npm run typecheck` limpio. Cambio de comentario,
    sin efecto en el comportamiento.

- **Cotejo mecánico de la tarjeta contra el repo — seis puntos, cero desfases**
  - Banderas del script (`provision-instancia.sh:88-96`) · `DO_REGION`/`DO_TAMANO`
    obligatorios con `--crear-droplet` (`:314-315`) · canal por omisión `estable`
    (`:82`) · `s3cmd` instalado por el alta (`setup-droplet.sh:96-98`) ·
    `default 'PEN'` en `db/schema.sql:110,245,390,545` · **75** migraciones de
    esquema (76 archivos − 1 de `@tipo: datos`, criterio en `migrar.mjs:102-106`).
  - Y lo que más podía tumbar el bloque 2: el paso 3 pasa **`--instalacion-nueva`**
    al migrador (`provision-instancia.sh:409`), la bandera que **se verifica contra
    la base** en vez de creerle a la prosa. Es lo correcto sobre una base recién
    nacida, y es el mismo mecanismo que enterró **D1 de F3.4** el 02/09.

- **Corregido el recuento de defectos del encabezado** — decía «DOCE defectos y SEIS
  solo aparecieron al ejecutarlo»; tras el 02/09 son **catorce y ocho**.

## A medias — y dónde exactamente

- **F5.6 sin empezar** — es tarjeta humana, no hay rama
  - **Se quedó en:** la puerta 0 sin correr. El primer comando es
    `docker buildx imagetools inspect …/space-os:estable` y tiene que decir
    `manifest.v2+json`, `sha256:6494bbca…`, **sin `Manifests:` debajo**.
  - Siguiente paso concreto: los tres preparativos del apartado ANTES —`doctl` con
    token **solo de droplets** y 2FA puesto antes de emitirlo, la clave pública del
    PADRE **en la cuenta de DO**, y `dig`— y luego el bloque 1 en seco.
  - ¿Zona liberada en [[tablero]]? sí

## Decisiones tomadas

| Decisión | Por qué | ¿ADR? |
|---|---|---|
| El aprovisionamiento **no** se hace desde la aplicación, y no es una carencia | Cuando hay que crear la máquina la app de esa instancia **todavía no existe**; y un botón en el PADRE que cree droplets metería el token de crear/destruir droplets dentro de una app con IP pública — el riesgo que el **ADR 0025** nombra como la joya del negocio | No: ya está en el ADR 0025 §5 |

## Bloqueos

| Bloqueo | Quién lo desbloquea | Desde |
|---|---|---|
| F5.6 y F5.7 son de servidor: las corre una persona | Emiliano | 2026-09-02 |

## Qué sigue mañana

1. Correr `TH-F5.6` con el bloque 6b incluido, y pasar cada puerta.
2. `TH-SPACES` — los dos buckets y las llaves, que se ensaya en DEMO.

## Notas para quien lea esto sin contexto

**Qué sí se prueba desde la aplicación, porque es fácil creer que nada.** La primera
organización y su Dueño nacen por **`POST /api/bootstrap`** (F5.2), ruta de la app con
tres cerrojos, y el script solo le hace un `curl`
(`provision-instancia.sh:271-274`). Su propio comentario dice por qué existe: sin esa
puerta, aprovisionar un owner **obliga a abrir `psql` en su droplet a mano**
(`apps/web/app/api/bootstrap/route.ts:14-18`). Y las comprobaciones del alta son todas
HTTP contra la app.

**Y qué NO es una UI**: el panel de flota es `node apps/flota/estado.mjs` en el PADRE.
Vive fuera de `apps/web` **a propósito** — si viviera dentro, la lista de clientes con
sus dominios viajaría en la imagen que corre cada owner, y lo único que lo impide es
el `--filter=web` del `Dockerfile` (`apps/flota/README.md`).

**Y el hallazgo mayor, CORREGIDO la misma tarde con autorización de Emiliano:** la misma afirmación
falsa está en **el invariante 9**, en dos documentos vivos —
`docs/noche/PLAN-NOCHE.md:116` y `docs/Plan_Instancias_Soberanas_v3.md:227`:
*«Autoregistro encendido **solo** en DEMO»*. También en
`vault/01-Arquitectura/modelo-instancias-soberanas.md:194` («Encendido solo en
DEMO»). Es uno de los 14 invariantes que se leen **antes** de tocar altas o
multi-tenancy, así que un agente que lo obedezca hace lo contrario de lo decidido.
El propio [[ejecucion-plan-v3]] ya registró el 14/08 que el registro *«queda cerrado
en todas partes»* (`:2042`) — o sea que la contradicción lleva **veinte días** ahí.
Tocar el plan v3 y la lista de invariantes **no lo decide un agente**: se mostró la
evidencia, Emiliano autorizó, y se corrigió en los tres sitios con la redacción vieja
citada al lado, para que un documento viejo se reconozca. La regla nueva dice lo que
el código ya hacía y **lo ancla a las pruebas que lo sostienen**
(`entorno.test.ts:143,197`), no a la memoria. El `Plan_..._v2.md` conserva la frase
vieja: es historia y no se toca.

**El fallo de forma es el de siempre aquí, y van muchas.** No es que la tarjeta esté
mal escrita: es que **se escribió desde el recorrido y no desde el criterio de
aceptación**, y las cuatro que faltaban son justo las que demuestran lo que no se ve
—que el alta se cerró sola, que la instancia habla con su base, que el autoregistro
está cerrado y que repetir no hace nada—. Una tarjeta humana se coteja contra la ficha
del plan **antes** de ejecutarla, no después.

## Relacionadas
[[AGENTES]] · [[tablero]] · [[ejecucion-plan-v3]] · [[MOC-Proyecto]] ·
[[2026-09-02]] · [[01-Arquitectura/modelo-instancias-soberanas]]
