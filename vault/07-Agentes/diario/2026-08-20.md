---
tipo: diario
estado: en-curso
actualizado: 2026-08-20
tags: [agentes, diario, instancias, permisos, credenciales, migraciones, rojos, auditoria]
archivos:
  - scripts/migrar.mjs
  - db/migrations/20260820_grants_rol_app.sql
  - db/migrations/20260820_catalogo_permisos_completo.sql
  - apps/web/scripts/bootstrap-auth.mjs
  - apps/web/lib/password-temporal.mjs
  - apps/web/lib/password-temporal.test.ts
  - apps/web/lib/server/usuarios-controller.ts
  - apps/web/lib/test/alta-instancia.e2e.test.ts
  - apps/web/lib/test/grants-rol-app.e2e.test.ts
  - apps/web/lib/test/permisos-semilla.e2e.test.ts
  - apps/web/lib/test/migraciones.e2e.test.ts
  - apps/web/lib/test/estado-campanas.e2e.test.ts
  - docs/Plan_Instancias_Soberanas_v3.md
  - docs/Registro_Cambios.md
  - infra/scripts/update.sh
  - infra/scripts/pruebas-update.sh
  - vault/07-Agentes/auditoria-f3-9-y-m3.md
---

# Diario — 2026-08-20

> Día de tres partes. Por la mañana otra sesión cerró **D1** y los hallazgos del
> arnés de `update.sh`. Por la tarde se cerraron **los tres ROJO** que había
> dejado abiertos el re-ensayo de la Fase 4 del 19/08.
>
> Esta nota cubre **la segunda mitad**. La primera está en
> [[ejecucion-plan-v3]], en sus callouts de `f295514`, `79447d7` y `b4c2522`.
>
> **Y por la tarde una tercera cosa**, que no estaba en el plan del día: la
> **auditoría de F3.9 y M3**, que llevaba desde el 19/08 sin hacerse. Salió
> **ROJO por un hallazgo** — [[auditoria-f3-9-y-m3]].

## Agentes activos

| Agente | Zona | Rama | Ventana |
|---|---|---|---|
| Sesión de rojos | Z9 · Datos + Z1 · Auth | `feat/servidor-padre-instancias` | 12:25 – 13:10 |
| Auditoría F3.9 + M3 | Z12 · Docs | `feat/servidor-padre-instancias` | 14:35 – 15:40 |

## Completado

**Nueve commits, ninguno empujado.** Y el orden importa: **primero se validaron
los tres rojos contra el código**, con `archivo:línea`, antes de tocar nada.
Ninguno era una exageración del reporte.

- **El runner exige el rol de aplicación** — `61f2668` · `scripts/migrar.mjs`
  - Verificado con: **un Postgres desechable SIN `spaces_app`** — salida 1, el
    mensaje da la línea a teclear y la base quedó con **cero tablas**.
    Contrafactual en el mismo servidor: creado el rol, vuelve a listar sus 69 con
    salida 0. Más 3 casos nuevos en `migraciones.e2e.test.ts`, en rojo primero.
- **Los GRANT sin lista blanca** — `3561bf9` · `db/migrations/20260820_grants_rol_app.sql`
  - Verificado con: 5 casos nuevos en `grants-rol-app.e2e.test.ts`, cuatro en
    rojo primero, **reproduciendo el defecto** (revocar → `permission denied` →
    aplicar → lee y escribe).
- **El catálogo completo, 41 · 9 · 5** — `1edbcce` · `db/migrations/20260820_catalogo_permisos_completo.sql`
  - Verificado con: `permisos-semilla.e2e.test.ts`, 6/6, con el reparto por
    perfil medido (24-5-5-3-4) y **por `permisosDeRol`**, no contando filas.
- **El alta deja de llevar su MATRIZ** — `de6860a` · `apps/web/scripts/bootstrap-auth.mjs`
  - Verificado con: `alta-instancia.e2e.test.ts`, archivo nuevo.
- **La contraseña del Dueño se genera** — `a2353d6` · `apps/web/lib/password-temporal.mjs`
  - Verificado con: 5 unitarias nuevas, 10/10 en el e2e del alta, y
    **`npm run build`** — que era la duda real del `.mjs` importado desde código
    de la aplicación.
- **La enmienda del plan** — `736a3b1` · `docs/Plan_Instancias_Soberanas_v3.md`
- **La regresión de la e2e completa** — `252ed12` · `apps/web/lib/test/estado-campanas.e2e.test.ts`
- **Tablero y bitácora** — `d3f771e` · `e9d936d`

### Y la auditoría que faltaba desde el 19/08 — **ROJO**

Los dos commits que quedaban **sin confirmar** sobre `update.sh` —`a490dd3`
(ciclo 3 de F3.9) y `32042e5` (M3)— por fin pasaron por una sesión aparte. El
informe entero, con los comandos para repetirlo, en [[auditoria-f3-9-y-m3]].

- 🔴 **`update.sh:891` publica la contraseña en el log que sale del droplet.** El
  comentario de `:889` promete que *«el mensaje nombra el PARAMETRO, nunca su
  valor»*, y es verdad **mientras haya separador**. Con `?password%3Dsecreto` el
  `=` va percent-encoded, nombre y valor se funden en un token, y ese token entra
  en `update-publicable.log` — el archivo que F3.9 sube al bucket. **Medido**, no
  deducido: el secreto aparece en lo que el doble de S3 registra como SUBIDO, y
  el contrafactual `?password=secreto` sale limpio. Cae por **M2**. Atenuante:
  una URL así **nunca conectó**. **El color final lo decide Jochelo.**
- ✅ **El invariante de M3 aguanta.** 33 URLs adversarias contra el parseador
  real, extraído verbatim: doble percent-encoding, mayúsculas, `password`
  repetido, `;`, fragmento, IPv6, cadena `clave=valor` de libpq, los ocho
  reenviables juntos. **Ninguna** salió en `argv`.
- ✅ Arnés entero: **95 escenarios · 621 comprobaciones · 0 rojas** (6 min 27 s).
- ✅ **13 mutantes dirigidos, 0 escapan.** La fuga de M3 muere con **110**
  comprobaciones en rojo; subir el log crudo, con **12**. ⚠️ **Tres** de ellos
  cuelgan de **una sola** comprobación: ahí hay que mirar la próxima vez que se
  toque el arnés. La barrida **entera** (52) sigue sin correrse nunca.
- 🟡 **Cinco derivas documentales**, ninguna del comportamiento: el README dice
  **619** donde imprime **621**; `entorno-y-despliegue.md` dice **«88
  escenarios»** dos veces donde son **95** —y **95** en otras dos líneas de la
  misma nota, fechada hoy—; la cita de **P7** está mal en tres notas y de otra
  manera en el README; tres citas más derivadas; y **no existe TH-F3.9**.
- 🛑 **F3.9 no es cerrable hoy.** Su criterio exige bucket, regla de 90 días y
  **leer a ojo la primera subida** anotándolo en `docs/Registro_Cambios.md`. Nada
  de eso existe.

**Nada se corrigió**: auditar y remediar son sesiones distintas a propósito.

**Suites al cierre:** typecheck limpio · **826 unitarias en 75 archivos** ·
**e2e entera: 19 archivos, 205 pruebas + 1 saltada**, exit 0 · `npm run build`
en verde. `aislamiento.e2e.test.ts` **intacto**; `db/schema.sql` sin tocar;
ninguna migración preexistente modificada.

## A medias — y dónde exactamente

Nada a medias en código: el árbol quedó limpio y las tres suites en verde.

Lo que queda **decidido y sin construir** es el **modelo de acceso** (el Dueño
solo con Google + códigos de recuperación). No es un ajuste: `GOOGLE_OAUTH` pasa
de bandera opcional a requisito de entrega, y **los códigos de recuperación no
existen**. Necesita tarea propia. Detalle completo en [[ejecucion-plan-v3]].

**Las notas del 18 y del 19/08 se escribieron después**, el mismo 20/08, a
partir de sus commits, de `.claude/relevo-nocturno-bitacora.md` y de los reportes
de avance. Van marcadas como reconstruidas: [[2026-08-18]] y [[2026-08-19]].

## Decisiones tomadas

Once, todas volcadas a la tabla de [[ejecucion-plan-v3]]. Las que cambian el
mapa:

| Decisión | Por qué | ¿ADR? |
|---|---|---|
| **El droplet actual pasa a ser el PADRE** | Deja de ser entorno de negocio y pasa a plano de control. Cierra P1 y cambia el sentido de la Fase 7 | conviene |
| **P1, P2 y P3 cerradas** | «Todo es demo»: los datos de los tres entornos se recrean. Y las instancias nacen en la cuenta de la casa | no |
| **El rol de la app se llama `spaces_app` en toda la flota** | Lo propio de cada instancia es la **contraseña**, no el nombre: un nombre por instancia es justo lo que hace que 13 migraciones no concedan nada | no |
| **El catálogo de permisos se adopta entero (41)** | Mientras existan dos listas pueden divergir, y ganaba la que corriera última | no |
| **`FINANZAS` sí puede facturar** | Un Finanzas que no factura obliga al Dueño a hacer el trabajo diario, y eso acaba con todo el mundo entrando como Dueño | conviene |

## Bloqueos

| Bloqueo | Quién lo desbloquea | Desde |
|---|---|---|
| **Diecisiete commits ROJO esperando visto bueno** | Jochelo | 13/08 los primeros |
| **Los códigos de recuperación no existen** | tarea propia, sin escribir | 20/08 |
| **¿g500 conserva acceso permanente a cada instancia, o solo durante el alta?** | Jochelo | 20/08 |
| **Normalizar el rol del droplet a `spaces_app`** antes de que tome un release con `20260820_grants_rol_app.sql` | persona, por SSH | 20/08 |
| **El color de F3.9**: el hallazgo de `update.sh:891` es INVALIDANTE por M2, con atenuante medida | Jochelo | 20/08 |

## Qué sigue mañana

1. El visto bueno de los diecisiete ROJO, que es lo único que separa esta rama de un merge.
2. La tarea propia del modelo de acceso (Google + códigos de recuperación).
3. ~~La auditoría de `32042e5` (M3)~~ — **hecha hoy**, ROJO:
   [[auditoria-f3-9-y-m3]]. Deja tres cosas detrás: el color final lo decide
   Jochelo, el arreglo de `:891` es de una línea, y las cinco derivas
   documentales esperan sesión de remediación.

## Notas para quien lea esto sin contexto

**① El paso 4 del reinicio local estaba mal, y cuesta una corrida entera.**
`recrearEsquema()` recrea el **esquema**, no la **base**. Con `spaces_e2e`
borrada, **las 19 suites e2e mueren en el `beforeAll`** con `database
"spaces_e2e" does not exist` — un rojo que no dice nada del código, hermano del
«falta el build» que ya documenta `CLAUDE.md`. Hay que crearla a mano:

```bash
docker exec spaces_db psql -U spaces -d postgres -c "create database spaces_e2e;"
```

**② `pg_roles` es del CLÚSTER, no de la base.** Por eso el caso negativo del
candado del rol **no se puede producir dentro de la suite**: haría falta borrar
el rol que usan todas las demás. Se probó levantando un Postgres desechable. Si
tienes que probar algo de roles, ese es el camino.

**③ Quitar una copia duplicada no es cosmético, y la prueba lo enseñó.** El caso
«el alta no cambia ni una fila del catálogo» **pasaba con la MATRIZ todavía
dentro**, porque sus 36 filas son subconjunto de las 41. Lo que impide que dos
listas diverjan no es que hoy coincidan: **es que solo exista una**, y eso solo
se mide leyendo el archivo.

**④ Y la de siempre, que volvió a morder: una cita `archivo:línea` recalculada a
mano es una cita rota.** Al mover `generarPasswordTemporal()` fuera de
`usuarios-controller.ts`, el archivo se desplazó y dejé una cita apuntando a
`:119` cuando la función había pasado a `:122`. La encontró la auditoría de la
bóveda, no yo.

## Relacionadas
[[AGENTES]] · [[tablero]] · [[MOC-Proyecto]] · [[ejecucion-plan-v3]] ·
[[auditoria-f3-9-y-m3]] ·
[[migraciones]] · [[entorno-y-despliegue]] · [[modelo-instancias-soberanas]]
