---
tipo: diario
estado: verificado
actualizado: 2026-08-17
tags: [agentes, diario, instancias, release, autoregistro, migraciones, evidencias]
archivos:
  - Dockerfile
  - .dockerignore
  - .env.example
  - .env.production.example
  - apps/web/lib/entorno.ts
  - apps/web/lib/entorno.test.ts
  - apps/web/app/api/auth/metodos/route.ts
  - apps/web/app/api/signup/route.ts
  - apps/web/app/(app)/login/page.tsx
  - db/migrations/20260812_schema_migrations.sql
  - apps/web/lib/test/migraciones.e2e.test.ts
  - docs/evidencias/fase-0.md
  - docs/evidencias/fase-1.md
  - docs/evidencias/fase-2.md
  - .claude/agents/documentalista.md
  - .claude/agents/validador-plan.md
  - .claude/commands/orquestar.md
  - docs/Registro_Cambios.md
---

# Diario — 2026-08-14 (viernes)

> [!note] Escrita con retraso
> Redactada el **2026-08-17** desde `git log` y desde la bitácora de
> [[ejecucion-plan-v3]], que ese día recibió más de treinta entradas.

**36 commits: el día más denso de la tanda.** Nacen los expedientes de evidencia,
se ejecuta casi toda la Fase 2, se invierte la decisión del autoregistro y arranca
la Fase 3 con `schema_migrations`.

## Agentes activos

| Agente | Zona | Rama | Ventana |
|---|---|---|---|
| orquestador (`/orquestar`) | Z12 · Docs | `feat/servidor-padre-instancias` | 11:57 – 18:02 |
| **segundo orquestador, sin saberlo** | Z12 · Docs | la misma rama, el mismo worktree | mañana (ver abajo) |
| ejecutor (F2.2, F2.6, F0.3, T-03, F3.1) | Z9 · Datos · Z1 · Auth | misma rama | 12:18 – 17:11 |
| verificador (una sesión por tarea) | — solo lectura | misma rama | intercalado |
| ensayista-local (F2.5, ×2) | — solo lectura | — no commitea | 12:45 y 13:25 |
| **documentalista** (nuevo hoy) | Z12 · Docs | misma rama | 12:16 – 14:58 |
| **validador-plan** (nuevo hoy) | — solo lectura | misma rama | 18:01 |

> [!danger] Dos orquestadores corrieron a la vez sobre esta rama, sin saberlo
> Una segunda sesión abrió el **mismo worktree**, remidió la línea base y lanzó su
> propio verificador sobre **F2.1** mientras el primero hacía lo mismo. Los dos
> veredictos coincidieron —AMARILLO, las dos formas de arranque comprobadas de
> verdad, diff limitado a `next.config.mjs`—, así que **F2.1 queda doblemente
> auditada por caminos independientes**, que es la única lectura buena del
> episodio. El resto es coste: trabajo pagado dos veces.
>
> El verificador de la sesión retirada **lo detectó solo**, por el árbol cambiando
> bajo sus pies: `Dockerfile` y `.dockerignore` apareciendo sin seguimiento a mitad
> de auditoría.
>
> **Decisión de Jochelo:** sigue la sesión que tiene el ejecutor de F2.2 a medias;
> la otra se retira. **Regla que faltaba:** antes de lanzar a nadie, comprobar que
> no haya otra orquestación viva — `git log` de los últimos minutos y zonas
> `TOMADA` en [[tablero]] con fecha de hoy.

## Completado

### El cierre de fase deja de ser una promesa: nace el documentalista

`2091646` (12:00) y `3634a75` (12:04) · `.claude/commands/orquestar.md` y
`.claude/agents/documentalista.md`.

El desorden de fondo: el 13/08 la Fase 1 se declaró «cerrada en local» **sin que
existiera un solo documento que dijera qué quedó probado y qué no**. A partir de
hoy el comando de orquestación **exige un expediente de evidencia antes de dar una
fase por cerrada**, y hay un agente cuyo único trabajo es levantarlo: qué se hizo,
qué quedó probado, **qué NO**, y qué espera a una persona.

`fb09b91` (12:16) es el primero — el de la Fase 1, 17 secciones — y `13e0a53`
(12:17) declara la fase cerrada **ya con el expediente detrás**, que es lo que la
regla nueva pide.

> [!important] El documentalista no se creyó los reportes, y eso es el punto
> Reverificó los cinco commits con `git show --stat`, abrió cada `archivo:línea`
> citado, recuperó el `extractSubdomain` viejo con `git show 3671e8a^` para
> confirmar el `parts.length >= 3`, y **remidió contra el 5433 en solo lectura**:
> 23 tablas con default, 0 sin `NOT NULL`, `sitio_modalidades` vacía, 33 filas
> repartidas en 9 tablas y 14 vacías. Sin discrepancias sustantivas.

Tres punteros que corrigió al citar y que se venían arrastrando mal: el guard de
nombre de base está en `db-e2e.ts:34-39` (no `:32-38`), y la afirmación de que el
esquema desplegado difiere está en **`db-e2e.ts:103-108`** — el plan la cita como
`:100-118` y la bóveda la citaba como `:103-112`.

`42c0f4e` (13:37) muda los expedientes a **`docs/evidencias/`**, que es la ruta que
lee el compilador del PDF. A partir de ahí se reemiten dos de ellos el mismo día
(ver «A medias»).

### F2.2 · la imagen de `space-os`, con su esquema y sus migraciones dentro

`3f16386` (12:18) · nacen `Dockerfile` (107 líneas) y `.dockerignore` (58).
**VERDE** — el segundo de toda la tanda.

240 MB, con `db/` dentro: **68 archivos md5-idénticos al repo**, byte a byte, sin
corrupción de finales de línea pese al host Windows. Y **sin ningún `.env`**.

> [!important] La auditoría no buscó secretos: montó un control positivo
> Extrajo los **11 valores literales** de `apps/web/.env` y `.env.local`, comprobó
> que ese juego de patrones **sí acierta** sobre el artefacto local
> `standalone/apps/web/.env` —o sea, que el método detecta el `.env` cuando lo
> hay— y **solo entonces** afirmó que en la imagen no aparece ninguno. Un cero sin
> control positivo detrás no dice nada.
>
> También materializó el stage `build` para ver que el `.env` **nunca entra al
> contexto**, en vez de conformarse con que no esté al final.

> [!danger] Lo único que impide hornear credenciales en la imagen de toda la flota
> es una línea del `.dockerignore`
> `.dockerignore:13` (`**/.env*`). Si el contexto de build llevara un `.env`, **Next
> lo hornea dentro del standalone sin avisar** — se comprobó el mismo día:
> `apps/web/.next/standalone/apps/web/.env` es byte a byte el mismo que
> `apps/web/.env`, con `GOOGLE_CLIENT_SECRET` incluido. No hay fuga a git
> (`.gitignore:14` lo cubre), pero el criterio de aceptación de F2.2 **depende
> enteramente de esa línea**.
>
> Y `Dockerfile` y `.dockerignore` **no están en ninguna zona de riesgo** de
> [[06-Operacion/zonas-de-riesgo]]. Debería revisarse.

### F2.5 · el smoke de la imagen, y el hallazgo que reordenó el día

`e8b0980` (12:45). El smoke literal del plan salió en verde —`login 200 · metodos
200 · signup 503 · estado 401`— y con estilos de verdad: los activos que la página
pide responden 200 y el CSS grande trae **707 reglas**, no es un 200 vacío. La app
habló con la base como `spaces_app` (**sin `bypassrls`**): un login con credenciales
falsas devolvió 401 desde una consulta real.

Pero el ensayo trajo dos cosas que nadie esperaba.

> [!danger] La imagen NO puede levantar una base nueva ella sola
> `db/migrations/20260729_licencias_permisos.sql:96-97` hace `raise exception` si no
> encuentra un rol de aplicación con grants, y en una base recién creada no lo hay:
> la cadena se corta ahí. **13 migraciones** referencian ese rol. `Dockerfile:94-95`
> copia **solo** `db/schema.sql` y `db/migrations`, y `db/dev-rol-app.sql` no viaja
> — y aunque viajara, es de desarrollo (rol `spaces_app`, contraseña en claro)
> mientras en producción el rol es `spaces_user`.
>
> **El orden obligatorio es: crear el rol de app → `schema.sql` → las migraciones
> con el mapa `ANTES_DE`**, y hoy nada en la imagen crea el rol. Le cae encima al
> runner de F3.2/F3.3 y al aprovisionamiento de la Fase 5.

> [!warning] El 503 salía, pero por el motivo CONTRARIO al que dice el plan
> El plan lo justificaba con «el autoregistro viene apagado **horneado**,
> invariante 9». Falso en esta imagen: el `.dockerignore` excluye `**/.env*`, así
> que la variable **no existía en el build** y Next no sustituyó ningún literal.
> Medido en el compilado: `signup/route.js` y el chunk de `google-oauth` **leen el
> entorno en tiempo de ejecución**.
>
> Lo que sí estaba horneado —y horneado **encendido**— era la **UI**: `login.html`
> es un prerender de build de **15 234 bytes que ya trae dentro el botón «Crear
> cuenta»**, y ningún valor de entorno lo cambia. Cada instancia de owner mostraría
> un botón que al usarse devuelve `503 «El registro de cuentas nuevas está
> deshabilitado»`. **No es fallo de la imagen: es P4-bis sin cerrar.**

Ese hallazgo resolvió de paso el `[SIN VERIFICAR]` del paso 3 de F2.6: la vía de
props/env **queda descartada por evidencia** (la página se prerrenderiza), así que
el valor tiene que llegar por `GET /api/auth/metodos/`.

### F2.6 · el autoregistro se decide al arrancar, no al compilar

`70ca3f0` (13:11) · 18 archivos. Ejecuta **P4-bis**, la contradicción que el plan
del [[2026-08-12]] dejó abierta a propósito. Nace `apps/web/lib/entorno.ts`, la
variable pierde el prefijo (`NEXT_PUBLIC_AUTOREGISTRO` → **`AUTOREGISTRO`**) y el
botón deja de hornearse.

Medido **con la misma imagen sin recompilar**: sin variable → 503, `=0` → 503,
`=1` → 400, y `/api/auth/metodos/` devuelve `autoregistro` en los tres casos.
`login.html` pasó de **15 234 bytes con el botón a 15 104 con 0 apariciones** de
«Crear cuenta». La auditoría reprodujo las dos cifras **dentro de las dos
imágenes**: son ciertas al byte.

> [!important] Invierte la polaridad por omisión, y eso no es un detalle
> Antes, `NEXT_PUBLIC_AUTOREGISTRO !== '0'` significaba **encendido si la variable
> no está**. Ahora, sin variable → `false`, **fail-closed**: «una instancia cuyo
> `.env` se quedó corto no abre el registro por descuido».
>
> Consecuencia práctica: **cualquier despliegue que hoy dependa del valor implícito
> se queda sin autoregistro tras F2.6 salvo que ponga `AUTOREGISTRO=1` explícito**.

Dos detalles de diseño que nadie pidió y están bien pensados: **las dos banderas
del login empiezan en `false` en el cliente**, así que si la consulta a
`/api/auth/metodos/` falla **no se pinta nada** —ofrecer una entrada que contesta
503 es peor que no ofrecerla—; y **es el primer commit de la tanda que escribe en
`docs/Registro_Cambios.md`**, con el criterio correcto: este cambio sí se nota
desde la aplicación. El auditor juzgó esa entrada **sin reparos**: abre por lo que
se ve («una puerta pintada en la pared») y pone el aviso duro —el `.env` que siga
con el nombre viejo amanece con el registro cerrado— de forma que alguien que no
programa pueda actuar sobre él.

El ensayista **reensayó F2.5 con la imagen nueva** (`6e119e5`, 13:25) y cerró la
cadena de evidencia del botón, que era lo que faltaba: «0 apariciones en el HTML»
prueba que **no se hornea**, no que **se pinte cuando toca**. Fue al bundle de
cliente servido por la imagen (`login/page-1ac02c573056c2df.js`), encontró que el
JSX del botón sí viaja, que su única puerta es un estado que arranca en `false`, y
que solo lo enciende un `autoregistro === true` de esa ruta. **Lo único sin probar
es la hidratación en un navegador real** — y si fallara, el botón no aparecería:
fallo en la dirección segura. Y **corrigió su propia medición anterior** sin que
nadie se lo pidiera: dijo 18 activos a 200 y son **22**; los otros 4 se le habían
escapado por una `\` de escape JSON que se comió su regex.

> [!warning] El renombrado rompe CUATRO tareas del plan, y ninguna se tocó
> - **F4.4** (`:1345`) es la peor: manda poner `NEXT_PUBLIC_AUTOREGISTRO=1` en el
>   `.env` de DEMO, así que **DEMO nacería con el registro CERRADO**, justo lo
>   contrario de lo que P4-bis compró.
> - **F0.3** (`:341-352`) busca `/^NEXT_PUBLIC_AUTOREGISTRO=0$/m` en `.env.example`:
>   tras el renombrado esa regex **no puede casar nunca**.
> - **F5.3** (`:1497`, `:1504`) grabaría una variable muerta en la plantilla de
>   todas las instancias.
> - **F0.2** (`:302-307`): su `sed` vale solo mientras el droplet corra el build
>   viejo.
>
> **El plan NO se toca**: la evidencia vive en [[ejecucion-plan-v3]].

### La decisión que invirtió el día: el registro va CERRADO en todas partes

`0dbccb8` (13:31) y `39379bf` (13:34).

🔵 **Decisión de Jochelo.** Revierte P3b del 10/08 («abierto y permanente»).
`.env.example` baja de `AUTOREGISTRO=1` a **`=0`** —la plantilla del repo dejaba el
registro abierto en cualquier clon, que era justo el agujero que F0.3 iba a
cerrar—, y al preguntárselo confirmó además que **ninguna instancia lo abre, DEMO
incluida**. La bandera existe y funciona; hoy **nadie la enciende**.

> [!danger] Contradice F4.4 del plan, y tiene tres consecuencias sin resolver
> (a) **F4.4 (`:1345`) queda contradicha**: manda el `.env` de DEMO con el
> autoregistro encendido. (b) **`POST /api/signup` pasa a ser código sin uso** en
> toda la flota, y `/api/auth/metodos/` devolverá siempre `autoregistro:false`.
> (c) **El alta de una organización nueva ya no tiene camino por la aplicación**:
> queda solo el tenant `rgb` que siembra `db/schema.sql:598` más el usuario que
> crea `bootstrap-auth.mjs`, que resuelve **por slug `rgb` y aborta si falta**. O
> sea que **cada instancia nueva nacería con una organización llamada `rgb`** —
> lo cual enlaza directamente con **P1**, que sigue abierta.

Que nadie encienda la bandera **no invalida F2.6**: sacarla del build sigue siendo
lo correcto —un solo artefacto para toda la flota, y encender el registro deja de
exigir recompilar— y de paso arregló el botón horneado, que era un defecto real con
el registro abierto **o** cerrado.

### F0.3 y T-03 · las dos plantillas de entorno

`6044732` (14:05) · **F0.3**: `.env.example` baja a `AUTOREGISTRO=0` **y una prueba
lo ancla**. Hasta hoy ninguna prueba leía esa plantilla, así que devolverla a `=1`
dejaba la suite verde y el CI mudo. El auditor **comprobó que la prueba muerde**:
puso el archivo en `=1`, la vio roja, y restauró dejando el árbol como estaba. Y
añadió una comprobación que nadie pidió: el archivo tiene finales **CRLF** y la
regex casa igual, porque en JS el `$` con flag `m` ancla antes del `\r` — **no hay
falso rojo esperando a un clon en Windows**.

`b7f3b5f` (14:18) levanta lo que faltaba y `ef70aa9` (14:24) lo arregla como
**T-03**, fuera del plan:

> [!danger] La plantilla de producción proponía una cookie compartida entre instancias
> `.env.production.example:9`: `COOKIE_DOMAIN=.{TENANT_SLUG}.spaces.com`. **Inocuo
> hoy** —`apps/web` no lee la variable— pero **latente por tres vías**:
> (a) el código que sí la consume existe en el repo
> (`_archive/api/src/core/auth/auth.routes.ts:17` hace
> `domain: process.env.COOKIE_DOMAIN`), así que el día que alguien haga configurable
> el `domain` de `cookieSesion()`, los `.env` nacidos de esa plantilla **comparten
> sesión por todo `*.spaces.com`: fuga entre instancias soberanas, R1 y R2 a la
> vez, en silencio**; (b) es una instrucción explícita al operador, que tomará
> decisiones de DNS y certificados sobre un modelo muerto; (c) **ninguna tarea del
> plan la limpia** — F5.3 crea una plantilla *nueva* sin la variable pero no toca
> esta.

El auditor de T-03 **demostró la decisión de diseño en vez de opinarla**: borró
cada plantilla y corrió el archivo de pruebas. Sin `.env.production.example`
(patrón nuevo, lectura dentro del `it`) → **2 fallan y 4 siguen corriendo**. Sin
`.env.example` (patrón viejo, constante de módulo) → **«no tests»: ninguno de los 6
llega a ejecutarse**, el error revienta en la importación. **El patrón nuevo no es
cosmético.**

> [!note] Limpia la plantilla, no los `.env` ya desplegados
> Por eso existe **TH-T03**: alguien tiene que mirar los `.env` que ya viven en los
> servidores.

### F3.1 · cada instancia lleva registro de lo que ya aplicó

`6cb16d4` (17:11) · `db/migrations/20260812_schema_migrations.sql` (234 líneas) +
`migraciones.e2e.test.ts`. Arranca la Fase 3.

El backfill registra **65** migraciones — ni las 66 del plan ni las 67 del repo —
y **las tres exclusiones son la parte valiosa**:

1. **`20260812_sin_default_tenant.sql` fuera**, porque está escrita y **no aplicada
   en producción**. Marcarla como hecha habría hecho que el runner **no la aplicara
   nunca**, dejando el `DEFAULT` de `tenant_id` vivo en el droplet **con el registro
   jurando lo contrario**.
2. **`20260731_calendario_meses_cortos.sql` fuera** por ser la única `@tipo: datos`,
   que `deploy.yml:141-148` no aplica en un despliegue normal.
3. **No se registra a sí misma**, para quedar con su checksum real y no con
   `'backfill'`, que F3.3 se salta por diseño.

El ejecutor resolvió sin escalar el problema de que un `.sql` no puede listar un
directorio, con un argumento que el auditor validó: **la lista literal no
envejece**, porque no es «lo que haya en `db/migrations/`» sino **lo que la flota
tenía aplicado al 2026-08-12**, que es un hecho histórico. Y el auditor **intentó
romper la hipótesis técnica y no cedió**: un `insert` SQL suelto que nombre
`tenants` en base virgen falla **al analizarse** aunque su `where` nunca se cumpla;
dentro de `do $$` con el guard delante, no. **El `do $$` no es adorno.**

## A medias — y dónde exactamente

- **F3.2, con un insumo obligado que hereda de F3.1** — rama
  `feat/servidor-padre-instancias`
  - Último punto bueno: `96c74d5` (17:32)
  - **Se quedó en:** `db/migrations/20260812_schema_migrations.sql:221-223`
  - 🔴 **Su ASSERT se vuelve un falso positivo en cuanto exista el runner.**
    Comprueba `where archivo >= '20260812'` sobre **toda la tabla**, sin distinguir
    una fila del backfill de una que ponga legítimamente el runner. El auditor lo
    reprodujo simulando F3.2: insertó `20260812_sin_default_tenant.sql` y al
    reaplicar la migración **abortó**. Es idempotente **solo mientras no exista
    ninguna fila ≥ 20260812**, condición que F3.2 destruye por diseño — y contradice
    lo que el propio archivo declara en `:72`.
  - Peor: el despliegue de hoy **reaplica todas las migraciones en cada corrida**
    (`deploy.yml:141-148`, con `ON_ERROR_STOP=1`), así que entre F3.2 y el retiro
    de ese workflow **un despliegue normal abortaría**, con un mensaje que además
    miente sobre la causa.
  - Siguiente paso concreto: F3.2 tiene que abrir su fila leyendo esto.

- **Y la prueba de F3.1 contradice a su propia migración**
  - `migraciones.e2e.test.ts:107-112` construye el conjunto esperado **leyendo el
    directorio** (`readdirSync().filter(f => f < '20260812')`), así que ante una
    migración **retrofechada** añadida mañana el oráculo **obligaría a meterla en la
    lista** — que es exactamente lo que `20260812_schema_migrations.sql:30-35`
    declara que no debe pasar jamás. **Probabilidad baja; contradicción real.**

- **Los tres expedientes, reemitidos el mismo día** — y la lección vale más que
  los documentos
  - Fase 1: `fb09b91` (12:16) → reescrito por **`7138f89`** (14:50). Aquel escribió
    593 líneas en la ruta vieja `docs/Instancias_Fase1_Expediente_Cierre.md`.
  - Fase 0: `29c6b9e` (13:43) → sustituido por **`9860d35`** (14:47), porque F0.3
    aún no existía cuando se escribió el primero.
  - Fase 2: `84fe410` (14:09) → reemitido como **`fc04607`** (14:58), «con las
    anclas remedidas».
  - **Se quedó desactualizado a las cuatro horas**, y no porque nadie tocara su
    fase: porque **dos tareas de otra fase escribieron sobre los mismos archivos**.
    Es la demostración práctica de que un `archivo:línea` **no sobrevive una tarde
    en una rama activa**.

- **F0.3 se ejecutó fuera del orden de dependencias, y es del orquestador**
  (`affa785`, 14:49)
  - El plan dice en `:337` «Depende de: F0.1», y **F0.1 nunca corrió** —es de
    servidor—. Se lanzó F0.3 igual, **sin declararlo**. Materialmente es inocuo
    (F0.3 opera sobre archivos del repo y su resultado no cambia con lo que conteste
    el droplet), pero **la regla de ejecución dice respetar el campo «Depende de»**,
    y saltárselo en silencio es justo lo que esa regla previene. **Lo levantó el
    expediente de la Fase 0, no el orquestador.**

- **El PDF del expediente entró y salió el mismo día** — `9f96d13` (15:30) lo mete
  (1,78 MB) y `65ed680` (15:38) lo saca, con `docs/evidencias/*.pdf` en
  `.gitignore`. Es una **salida derivada**: se regenera desde los `fase-*.md`, pesa
  ~2 MB y cambiaría en binario con cada regeneración.

## Decisiones tomadas

| Decisión | Por qué | ¿ADR? |
|---|---|---|
| 🔵 **El autoregistro va CERRADO en local, en producción y en DEMO** | Ninguna instancia abre el registro. Revierte P3b del 10/08. **Contradice F4.4 del plan** | No; en [[ejecucion-plan-v3]] y [[00-Indice/preguntas-abiertas]] |
| **Una fase no se cierra sin expediente de evidencia** | La Fase 1 se cerró ayer sin ninguno | Escrito en `orquestar.md`; nace el `documentalista` |
| **ROJO = lo que su ejecutor declaró ROJO en el reporte** | Los dos expedientes lo contaron distinto —seis y ocho— porque nadie había escrito el criterio. «Toca un tema sensible» **no** basta | Sección propia en [[ejecucion-plan-v3]] |
| **Los recuentos de pruebas dejan de escribirse en la documentación** (`703649e`) | Crecen con cada tarea y envejecen en horas. Se miden, no se citan | `CLAUDE.md` y `entorno-y-despliegue` |
| **El PDF del expediente no se versiona** (`65ed680`) | Salida derivada: 1,7 MB, cambia en binario en cada regeneración, y se reconstruye desde los `fase-*.md` | `.gitignore` |
| **Sigue la sesión con el ejecutor a medias; la otra se retira** | Dos orquestaciones simultáneas sobre el mismo worktree | — |
| **La comprobación del botón «Crear cuenta» se añade a la tarjeta de F4.5** | Es el único eslabón que el ensayo no pudo cerrar (hidratación en navegador real), y montar Playwright/jsdom **no es decisión de un ensayo** | — |
| Nace el **`validador-plan`** como compuerta de cierre (`c0d3f26`) | Sin su VERDE una fase no se cierra | `.claude/agents/validador-plan.md` |

## Bloqueos

| Bloqueo | Quién lo desbloquea | Desde |
|---|---|---|
| **TH-F0.1** — ¿está abierto el registro HOY en el droplet? **Bloquea toda la Fase 4** (`plan:260`) | Una persona | 2026-08-14 |
| **F0.2** — condicionada a la respuesta de TH-F0.1 | Una persona | 2026-08-14 |
| **F2.3 y F2.4** — el nombre del registry (**P4**) | Jochelo (se resuelve el 17/08) | 2026-08-13 |
| **TH-T03** — los `.env` ya desplegados siguen con la cookie comodín | Una persona | 2026-08-14 |
| **TH-01, TH-02, F1.5** — heredadas del 13/08 | Una persona | 2026-08-13 |
| **Seis commits ROJO** esperando visto bueno humano | Jochelo | 2026-08-13 |
| P1, P2, P3, P6 — decisiones de negocio abiertas | Jochelo | antes del 13/08 |

> [!warning] **TH-F0.1 no tenía ficha**, y es la tarjeta que bloquea toda la Fase 4
> Solo existían menciones de pasada. Escrita ahora (`daef9f7`, 15:00) con sus dos
> comandos literales del plan (`:267-271` y `:275`), la tabla de respuestas y el
> aviso que cambia el guion: **si el `curl` devuelve 400, F0.2 ya no se ejecuta
> como está escrita** — con la decisión de hoy, la acción correcta es **borrar la
> línea vieja**, no poner la bandera a 1. Lo levantó el expediente de la Fase 2.

## Qué sigue

1. **F3.2** — el runner de migraciones, con el ASSERT de F3.1 como insumo obligado.
2. **F3.3** — la comprobación de integridad por checksum.
3. Validar las Fases 1 y 2 con el `validador-plan`.
4. Presentar a Jochelo el bloque de tarjetas humanas y los commits ROJO.

## Notas para quien lea esto sin contexto

- **Un callout de cabecera no se actualiza solo porque se actualicen sus filas.**
  El titular de la Fase 0 decía «ninguna tarea ejecutada» **tres líneas encima de
  dos filas COMPLETADA_LOCAL con commit**: lo escribió `b57987c` cuando era cierto y
  cuatro commits posteriores actualizaron las filas sin tocarlo. Es lo primero que
  lee un humano con prisa. **Van tres validaciones y las tres han encontrado el
  mismo tipo de defecto.**
- **Re-fechar una nota con el dato viejo dentro es peor que no fecharla.** Pasó
  cuatro veces hoy, y una fue del orquestador: `CLAUDE.md` §4 seguía diciendo «789
  unitarias en 71 archivos» con la coletilla «medidas el 2026-08-14» **puesta por
  él mismo**. Medido: **803 en 73**. Corregido, y esta vez **sin número**.
- **Cuidado con dónde mides.** `cd apps/web && npm test` desde la **raíz** del repo
  da **796 en 72** — es otra rama (`feat/ui-base-404-atajos`). El worktree da
  **803 en 73**. Son ramas distintas con recuentos distintos.
- **Y el recuento que se coló doce minutos después de prohibirlo.** `703649e` (14:12)
  retiró los recuentos globales de la documentación con el argumento de que crecen y
  hay que medirlos; `ef70aa9` (14:24) escribió «805 pruebas en 73 archivos» en
  `tablero.md:31`. Retirado en `38ace2f`. *(Los «4 casos» y «6 casos» de esa misma
  celda **no** son lo mismo: son el tamaño de un candado, una afirmación de
  contrato, no una medición del entorno.)*
- **Un número que cuadra puede cuadrar por casualidad.** El plan (`:2037`) esperaba
  **6 casos** en `entorno.test.ts` y hoy hay exactamente 6 — pero son de F2.6 + F0.3
  + **T-03**, no los que el plan preveía (F2.6 + F0.3 + F5.3). Los dos de F5.3 siguen
  sin escribirse.
- **Honestidad sobre lo que no se puede reproducir.** El expediente de la Fase 2 **no
  pudo reverificar los 15 104 bytes** de `login.html` post-F2.6 porque esa imagen ya
  no existía en la máquina. Reconstruyó y midió **14 594 bytes con 0 apariciones**
  —misma conclusión, otro artefacto— y lo dijo, en vez de repetir el número ajeno.
- **Y honestidad sobre el TDD.** El auditor de F2.6 dio por «creíble, no probado» el
  segundo rojo del ejecutor: poner la polaridad vieja a propósito para verlo fallar
  **no es verificable desde el repositorio** —no hay commit intermedio ni reflog que
  lo conserve—. Lo que sí hizo fue ejecutar ambas polaridades y confirmar que ese
  rojo **tenía que salir**. Esa distinción es la correcta.
- **Dos fases consecutivas sin una línea en `docs/Registro_Cambios.md`.**
  `git log 1ad1045~1..HEAD -- docs/Registro_Cambios.md` salía vacío para toda la Fase
  1. Defendible tarea a tarea —nada se nota desde la aplicación— pero el patrón se
  repite. **F1.5 sí necesitará entrada**, y en el momento de aplicar la migración.
- **Deuda encontrada y no arreglada, con justificación falsa dentro:** el alias de
  `styled-jsx` de `next.config.mjs:62-65` apunta a `apps/web/node_modules/styled-jsx`,
  que en local solo contiene `.vite` y **dentro del stage `build` ni existe**; y el
  comentario que lo justifica (`:59-61`) habla de «styled-jsx (React 19) … while
  react-dom is still v18» cuando en el árbol hay styled-jsx 5.1.1 y react/react-dom
  18.3.1 — **no hay React 19 por ningún lado**. Inocuo hoy; **merece tarea propia**.
- **Y una cobertura que se perdió sin que nadie la reemplazara:** ningún test ejerce
  `GET /api/auth/metodos/`. El nombre del campo `autoregistro` en ese JSON es hoy **la
  única atadura** entre el servidor y el botón; si alguien lo renombra, el botón
  desaparece **en silencio**. Fail-closed, pero es **cobertura perdida, no
  equivalente**.

## Relacionadas
[[2026-08-13]] · [[2026-08-17]] · [[ejecucion-plan-v3]] · [[tablero]] ·
[[AGENTES]] · [[01-Arquitectura/entorno-y-despliegue]] ·
[[03-Frontend/acceso-y-sesion-ui]] · [[04-Datos/migraciones]] ·
[[06-Operacion/zonas-de-riesgo]] · [[00-Indice/preguntas-abiertas]] ·
[[MOC-Proyecto]]
