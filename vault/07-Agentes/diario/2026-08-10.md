---
tipo: diario
estado: verificado
actualizado: 2026-08-10
tags: [agentes, diario, despliegue]
archivos:
  - db/migrations/20260807_password_resets_rls.sql
  - apps/web/lib/server/password-reset-repo.ts
  - PENDIENTE_20260810_PASSWORD_RESETS_RLS.txt
---

# Diario — 2026-08-10 (lunes)

## Agentes activos

| Agente | Zona | Rama | Ventana |
|---|---|---|---|
| Sesión externa | Z1 · Auth + Z9 · Datos | `main` | 08:45 – 09:10 |

Zonas reclamadas en [[tablero]] antes de empezar y liberadas al cerrar.

## Completado

### Desplegado: `password_resets` bajo RLS

Era el pendiente que quedó apuntado el viernes. Ejecutado siguiendo
`PENDIENTE_20260810_PASSWORD_RESETS_RLS.txt`.

| Paso | Resultado |
|---|---|
| Respaldo | `spaces_prod_pre_pwreset_20260810_085426.sql.gz` — 7.1 MB, 38 tablas **con datos** |
| Ensayo en rollback | La migración entera contra datos reales, deshecha. `ASSERT` pasó |
| Migración | `COMMIT`, y la consulta del invariante en **0 filas** |
| Código | Build limpio (exit 0), `pm2 reload`, un reinicio útil |
| Verificación | `rls=t force=t` · `auth_reset_por_token` existe, `secdef=true`, con GRANT · el artefacto **servido** llama a la función · login 200, `/api/estado` 401, `/api/auth/reset` 503 |

> [!info] El invariante de hard1 vuelve a cumplirse
> **Cero tablas con `tenant_id` sin RLS+FORCE.** `password_resets` era la única
> que faltaba, y llevaba así desde el 23/07.

### El respaldo tenía una trampa que casi muerde

`emiliano` **no tiene sudo sin contraseña**, pero **sí con ella**. Importa: un
`pg_dump` con el rol de la app (`spaces_user`, NOBYPASSRLS) habría salido **sin
filas** por la RLS fail-closed —y habría *parecido* un respaldo bueno—. Se tomó
como `postgres`. Comprobado: 38 bloques `COPY` con datos.

## Dos fallos del propio runbook, ya corregidos en él

1. **Orden equivocado.** El PASO 0 (ensayo) usa el archivo de la migración, que
   llegaba con el `git merge` puesto en el PASO 2. Al seguirlo tal cual, el
   ensayo falló con «No such file or directory». Traer la fuente **no** cambia
   lo servido, así que el pull sube al principio: pull → ensayo → migración →
   build+reload.

2. **El primer build falló y se recargó igualmente.** `turbo` imprimió
   «Compiled successfully» y varias líneas después `Failed: web#build` — son
   fases distintas: webpack compila y luego el typecheck puede tumbarlo. El
   `pm2 reload` se lanzó sin mirar el código de salida, dejando un rato el
   estado exacto que el runbook existe para evitar: **migración aplicada con
   código viejo**.

   Repetido sin tocar nada, el build pasó limpio (exit 0). Fue transitorio y la
   aplicación nunca dejó de responder (comprobado: login 200 en ese intervalo).
   El runbook ahora avisa de mirar el código de salida antes de recargar.

### Aplicado: INC-05 · residuos de demo

`docs/datos/20260810_inc05_residuos_demo_g500.sql`, con captura previa y ensayo
en rollback (`UPDATE 1` ×3, `INSERT 0 1`, comprobación interna, `ROLLBACK`).
Aplicado después: mismo resultado y `COMMIT`.

| Rótulo | Antes | Ahora |
|---|---|---|
| `tenants.nombre_comercial` (g500) | `DEMO PIXELED.` | `PIXELED` |
| Creativo de KFC | `upsivale 1920.jpg` | `creativo-kfc.jpg` |
| Usuario | `DEMO` | `Operador Demo` |

Verificado: **0 residuos** de «DEMO»/«upsivale» y su entrada en bitácora.

> [!warning] Dos correcciones al plan de incidencias
> · El nombre comercial vive en `tenants`, **no en `config_negocio`** — esa
>   columna se retiró en el ADR 0011.
> · El importe negativo de «EdgeCase» y los creativos «WhatsApp Image» **ya no
>   existen**: se resolvieron en el A9 del 04/08. El importe es DERIVADO, así
>   que al enderezar las fechas se corrigió solo.

### NO se asignó responsable a las órdenes de trabajo

El plan pedía asignarles «el primer usuario con rol OPERACIONES». Al mirarlo:
son **las 2 únicas OT que existen en g500**, las dos sin responsable. O sea que
el campo nunca se usó — no es un registro suelto.

Escribir ahí el nombre de una persona sería afirmar que hizo un trabajo de campo
que nadie sabe si hizo, en un sistema cuya bitácora se usa como prueba. Es una
decisión del negocio, no una limpieza. **Pendiente de que una persona diga a
quién corresponden, o de dejarlas así.**

*(Corrección: el 10/08 por la mañana conté «1 OT sin responsable». Eran 2.)*

### Aplicado: INC-09 · los tres detalles que quedaban (.4 .5 .6)

| Punto | Qué se hizo |
|---|---|
| **.5** | Etapa nueva **`facturada`** al final del pipeline. Se deriva de `facturas.campanaId`, no de una bandera nueva |
| **.4** | Helper `lib/plural.ts` (`plural` / `conteo`) aplicado en **12 sitios** |
| **.6** | El IVA por omisión de un cliente nuevo sale de `config.ivaTasas[0]`, no de un `16` escrito a mano |

**El orden de la comprobación en `.5` es lo delicado.** `facturada` se mira
ANTES del candado: si se mirara después nunca se alcanzaría, porque el candado
está completo en toda campaña que llegó a facturarse y ganaría siempre.
Verificado por mutación — invirtiendo el orden cae la prueba, y sustituyendo el
filtro por `facturas.length > 0` cae la de «la factura de otra campaña».

`.6` no era lo que decía el plan. El formulario **ya leía** el catálogo de la
configuración; lo que estaba mal era el valor inicial de un cliente NUEVO
(`?? 16` fijo), que luego se colaba en el desplegable por la línea que añade la
tasa actual para no perderla al editar.

`.4` incorpora la lección de M10: el `+s` es solo el valor por omisión y la
forma plural se puede pasar a mano, para que nadie repita el «mess».

### Aplicado: INC-03 · el estado de la campaña sigue al calendario

`estado_comercial` era un campo ALMACENADO que solo cambiaba por acción de una
persona, así que se congelaba. De ahí salía el doble distintivo «Completada +
Aún vigente» (A1/N5 de la auditoría): transparentaba el desfase sin arreglarlo.

Barrido `recomputarEstadoCampanas()` en `campanas-repo.ts`, enganchado en
`/api/estado` junto a los otros tres y **detrás del permiso `comercial.ver`**,
igual que ellos. Sigue el molde de `recomputarEstatusArrendadores()`: un UPDATE
por regla, con el `where` lo bastante preciso para que la segunda pasada sea un
no-op.

| Regla | Condición | Destino |
|---|---|---|
| 1 | `fecha_fin < current_date` y (`ACTIVA` **o** `CONFIRMADA` publicada) | `COMPLETADA` |
| 2 | `CONFIRMADA` + `fecha_inicio <= current_date` + publicada | `ACTIVA` |

«Publicada» no es una sola condición: DOOH/HÍBRIDA es `enviada_dominio` +
`validacion_estatus = 'APROBADA'`; OOH es una OT `MONTAJE_LONA` `COMPLETADA` —
el mismo criterio que usa `pipelineStage()` para la etapa `instalada`, para que
interfaz y servidor no diverjan. Va en la constante `SQL_PUBLICADA` porque las
DOS reglas la necesitan.

> [!important] La regla 1 se lleva las CONFIRMADA vencidas, y el ORDEN importa
> Una CONFIRMADA publicada cuyo periodo ya terminó es **el atasco que INC-03
> viene a limpiar**. Corriendo la regla 1 primero, llega a `COMPLETADA` en UNA
> pasada con UN apunte. Al revés se activaría hoy y se completaría mañana, con
> dos apuntes que se contradicen.

**Lo que NO hace, y es deliberado:** no completa nada con `fecha_fin` futura
—el cierre anticipado es legítimo y deshacerlo pisaría una decisión humana—; no
toca `CANCELADA`, `DRAFT`, `COTIZACION` ni `LISTA_FACTURAR`; y la transición a
`ACTIVA` **no publica nada**: solo refleja lo ya publicado, así que si se
equivocara lo peor que hace es adelantar un rótulo.

Bitácora a nombre del Sistema, un solo INSERT por regla (`unnest`) en vez de uno
por campaña: en la primera pasada sobre una base atascada eso son veinte idas y
vueltas dentro de la petición que hidrata el shell. El fallo al anotar **no**
tumba el barrido, pero ahora **se registra en el log** en vez de tragarse.

**Interfaz:** fuera el segundo distintivo en `campanas/page.tsx`. Queda el
estado y, para el único caso que sobrevive —cerrada antes de su fecha de fin—,
una nota discreta con `title`.

> [!warning] El tercer punto del plan no tiene dónde aplicarse
> Pedía exigir un motivo «si un usuario marca COMPLETADA una campaña cuya
> `fecha_fin` es futura». **No existe ese endpoint**: no hay `PATCH
> /api/campanas/[id]` ni ninguna pantalla que fije `estado_comercial` a mano.
> El único camino a `COMPLETADA` por acción humana es `facturar`
> (`finanzas-repo.ts:249`), que ya deja factura, bitácora y aviso. Inventar un
> campo para un flujo inexistente habría sido trabajo muerto.

#### Verificación: 23 pruebas nuevas y cuatro mutaciones

`lib/test/estado-campanas.e2e.test.ts`, **por HTTP** y no llamando al repo: el
tenant sale de la cookie, y así se prueba también el cableado y la puerta de
permiso. Cubre los bordes de fecha (fin **hoy** no completa), los cuatro «no se
activa», el medio físico y la convergencia.

| Mutación | Resultado |
|---|---|
| `fecha_fin <` → `<=` | ✅ cae «el último día todavía cuenta» |
| Quitar la rama `CONFIRMADA` de la regla 1 | ✅ caen 2 |
| Quitar `verComercial ?` del cableado | ✅ cae la de permiso |
| Quitar `fecha_fin >= current_date` de la regla 2 | ❌ **sobrevivió** |

La cuarta mató la condición: era **inalcanzable** después de la regla 1. Se
**quitó del código** en vez de dejar SQL que nunca se evalúa aparentando que
guarda algo, y en su lugar quedó fijada la propiedad (una CONFIRMADA publicada
y vencida acaba en COMPLETADA con un solo apunte), que es independiente de cómo
esté escrito.

La bitácora es **append-only** —un trigger prohíbe el `DELETE`—, así que entre
pruebas no se puede vaciar: cada campaña lleva nombre único y la bitácora se lee
cruzándola con las campañas vivas.

### Desplegado: INC-09 + INC-03, juntos

Por decisión explícita del usuario («lo desplegamos juntos»), en contra de la
recomendación de uno por despliegue. Runbook en
`DESPLIEGUE_20260810_INC09_INC03.txt`. `9788b59` → `484e768`.

| Paso | Resultado |
|---|---|
| Migraciones | **Ninguna** — `git diff 9788b59..HEAD -- db` en 0 archivos |
| Build | 2m11s, **EXIT=0 leído antes de recargar** (la lección de esta mañana, ya incorporada al procedimiento) |
| Reload | Un solo reinicio, 27 → 28 · login 200 |
| Artefacto servido | `completada autom…` en `server/chunks/5736.js`; `cerrada antes de tiempo` en el bundle de Campañas; **cero rastros de «Aún vigente»** |
| Log | Sin errores nuevos — los que hay son del 09/08 |

> [!warning] Este despliegue CAMBIA DATOS DE PRODUCCIÓN SOLO
> Es el primero que lo hace. En cuanto alguien con `comercial.ver` abre una
> pantalla, el barrido mueve dos campañas de `ACTIVA` a `COMPLETADA` — y
> **revertir el código NO deshace esa escritura**. Por eso el rollback son dos
> piezas y la de datos se capturó ANTES:
> `docs/datos/20260810_inc03_estados_campana_rollback.sql`.

**Ensayo en seco contra `spaces_prod` antes de desplegar** (los dos WHERE del
barrido como SELECT, sin escribir):

| Regla | Filas | Cuáles |
|---|---|---|
| 1 → `COMPLETADA` | **2** | `KFC` (terminó el 08/08) y `Propuesta para cliente 1` (terminó el 31/07), las dos de g500 |
| 2 → `ACTIVA` | **0** | — |

Radio de impacto: esas dos filas y ni una más. Hay **cinco** campañas en
`COMPLETADA` con fecha de fin futura (`pruebas_produccion` hasta 2027-02, `card`
hasta 2027-05, `prueba anual` hasta 2028-07…): el barrido las deja como están a
propósito, son cierres anticipados. En pantalla pasan del badge ámbar a la nota
gris.

### Aplicado: INC-07 · altas que no se pueden duplicar

**El plan diagnosticaba mal, y los datos lo dijeron.** Se titula «doble submit
en formularios». Al mirar producción:

1. Los **doce** formularios de alta YA deshabilitaban su botón al enviar
   (`guardando`, `enviando`, `ocupado`). No había patrón que extender.
2. El único duplicado real de la bitácora tiene **71 segundos** entre las dos
   entradas (19:55:06 y 19:56:17 del 07/07, `ADMINISTRADORA DE GASOLINERAS
   INTERLOMAS`). Eso no es un doble clic: es una persona repitiendo. Un botón
   bloqueado no habría evitado nada.
3. Hoy no hay **ni un** arrendador repetido por nombre ni por RFC (8 en total),
   ni clientes repetidos. La segunda fila de INTERLOMAS se borró en algún
   momento — sin rastro, porque el borrado sin bitácora era A10, que cerró
   INC-06.

Decisión completa en [[0013-altas-que-no-se-pueden-duplicar]] (ADR nuevo).

| Pieza | Qué hace | Por qué así |
|---|---|---|
| `lib/clic-unico.ts` + `Button` | El botón se bloquea si su `onClick` devuelve promesa | La guarda es un **cierre**, no un `useState`: `setGuardando(true)` no apaga el botón hasta el render siguiente, y ahí cabe el segundo clic |
| `arrendadores_tenant_rfc_uq` | Índice único parcial `(tenant_id, upper(btrim(rfc)))` | Un RFC es de un contribuyente. **Regla dura**, y al ser índice cubre la carrera |
| 409 con `motivo` + `existente` | Nombre repetido avisa y se puede confirmar | Dos propietarios **pueden** llamarse igual. Un índice único no tiene escapatoria |

> [!important] Dónde nos apartamos del plan
> Pedía índice único **también sobre el nombre**. No se hizo: el segundo «Juan
> Pérez» —otra persona, otro predio— sería imposible de dar de alta y sin forma
> de continuar. Se resuelve con un 409 que se puede confirmar
> (`confirmaNombreRepetido`), que nunca es el valor por omisión. Mismo criterio
> que el resto del repo: no bloquear lo legítimo, hacerlo trazable.

`Button` es archivo de **alto contacto** (`components/demo/ui/*`). El cambio no
altera su API: un `onClick` que no devuelve promesa se comporta igual que antes,
y no se tocó ninguno de los doce formularios.

#### Verificación

23 pruebas nuevas (9 de la guarda + 14 e2e por HTTP contra Postgres real), y
cuatro mutaciones:

| Mutación | Resultado |
|---|---|
| `finally` en vez de `then(soltar, soltar)` | ✅ cae la de «no se inventa rechazos» |
| `ocupado()` siempre `false` | ✅ caen las 2 del segundo clic |
| Quitar la migración del índice | ✅ caen las 4 del RFC |
| Comparar el nombre sin `upper(btrim(...))` | ✅ cae la de mayúsculas/espacios |

Se encontró de paso un fallo propio en `Button`: con **StrictMode** React monta,
desmonta y remonta, y el `vivo.current = false` de la limpieza dejaba el botón
sin reactivarse nunca en desarrollo. Corregido poniéndolo a `true` al montar.

761 unitarias y 105 e2e en verde.

### Desplegado: las dos migraciones que quedaban en `main`

`484e768` → `7d27d25`. Runbook en `DESPLIEGUE_20260810_MIGRACIONES.txt`. Van
juntas porque las dos llevaban desde la mañana en `main` sin aplicar: la de
INC-07 y la de notificaciones de la otra sesión.

| Paso | Resultado |
|---|---|
| Respaldo | `spaces_prod_pre_migraciones_20260810.sql.gz` — 7.1 MB, **38 bloques COPY con datos**, tomado como `postgres` |
| Ensayo | Las dos dentro de un `begin`, **con el `commit;` de los ficheros quitado a mano**. Al deshacer quedaron 0 índices y 0 columna |
| Migraciones | Las dos EXIT=0. La verificación de RFC repetidos: **0 filas** |
| Build | 2m00s, **EXIT=0 leído antes de recargar** |
| Reload | Un reinicio, 28 → 29 · login 200 |
| Estructura | `archivada_en timestamptz` · `idx_notif_vivas` · `arrendadores_tenant_rfc_uq` con **`indisunique = t`** |
| Artefacto servido | `archivar-todas` construida; INC-07 en el bundle de Arrendadores; **`leer-todas` ya no existe** en el build |
| Endpoints | `archivar-todas` 401 · `leer-todas` **404** · `arrendadores` 401 |

> [!important] El orden no era negociable, y esta vez sí importaba
> El código nuevo hace `where archivada_en is null` en `listarNotificaciones`,
> que alimenta `/api/estado`. Recargar ANTES de migrar habría dejado el endpoint
> que hidrata el shell entero en 500: **la aplicación entera en blanco**, no una
> pantalla. Al revés no pasa nada: las dos son aditivas y el código viejo las
> tolera.

> [!note] El `commit;` dentro del fichero, una trampa del ensayo
> Un `\i` de una migración que trae su propio `commit;` **cierra la transacción
> externa**, y el `rollback` de después no deshace nada: el ensayo confirmaría
> creyendo que ensaya. Se quitó con un `sed` a una copia en `/tmp` y se
> comprobó DESPUÉS del rollback que no quedaba ni el índice ni la columna.
> Apuntado en el runbook para el próximo.

**Falta la prueba de fuego con sesión**: que «Borrar todas» vacíe el panel de
verdad y que el aviso de nombre repetido deje continuar. Desde fuera no se ve.

### Aplicado: INC-02 · qué pieza va en qué pantalla

**`DOOHMAIN_PUBLISH_ENABLED=1` en producción** — comprobado en el
`.env.production` del droplet. La publicación es real y sale a un CMS externo,
así que esto no es un cambio de pantalla: cambia lo que se le manda a un
servidor de anuncios en vivo.

Lo que había, medido contra `spaces_prod`:

| Campaña | Pantallas digitales sin asignar | Aprobados |
|---|---|---|
| KFC | **12** | 1 |
| mastercard | 2 | 1 |
| card | 1 | 1 |
| prueba final | 1 | 1 |
| Campaña lista — publicar a DOOHmain | 1 | **2** |
| pruebas_produccion | 1 | **2** |

> [!important] La causa real era peor que «faltó capturar»
> `publicarCampanaEnDoohmain()` mandaba el **producto cruzado**: cada creativo
> validado a CADA pantalla digital. No es que la asignación estuviera vacía —
> es que **no participaba de la publicación**. Y `cantDia` era el total de la
> pantalla PARA CADA creativo: dos piezas en una de 8 pedían 16 pases en un loop
> de 8.

| Pieza | Estado |
|---|---|
| Persistencia (`reservas.creativos` jsonb) | Ya existía; **no hizo falta migración** |
| Guard en `enviar-dominio` | **Ya estaba** (`1c0e8c4`, el fix de M14) |
| Guard al **aprobar** | Nuevo — entre los dos pasos hay días de revisión |
| Autoasignación del único aprobado | Nueva, con apunte en bitácora |
| Publicación **por pantalla**, con `veces` como `cantDia` | Nueva |
| Backfill de las 16 reservas | `docs/datos/`, con rollback |

**Decisiones que conviene tener escritas:**

- *Aprobar una segunda pieza NO deshace la asignación de la primera.* Cuando se
  aprobó A era la única y ocupar el loop era la única respuesta posible; al
  aprobar B la cosa se vuelve ambigua y el sistema no decide. **Efecto: B no
  sale en ninguna pantalla hasta que alguien la asigne** — y ya no como antes,
  cuando iba todo a todas partes.
- *Un slot vaciado a mano no se rellena solo.* Vaciarlo es una decisión.
- *Sin `spots_por_dia` no se dicta cuota.* Las 16 reservas de producción lo
  tienen en NULL: no hay pauta contratada y hasta hoy no se mandaba
  `--cant-dia`. La asignación guarda `veces: 1` como marca de «va aquí», y si
  ese 1 viajara como cuota esas campañas pasarían de la pauta completa a **un
  pase al día**, sin que nadie se enterara hasta ver el proof of play.

**Fallo suelto encontrado de camino:** el `update reservas set creativos = …`
que desasigna al rechazar/eliminar/retirar/reemplazar un creativo iba **sin
`where`** — reescribía la columna de TODAS las reservas del tenant en cada
operación. Salía igual (el filtro no encuentra el id en las demás), pero eran
cuatro escrituras masivas sobre filas ajenas. Acotadas las cuatro.

#### Verificación

19 pruebas nuevas por HTTP contra Postgres real. **El arnés enciende DOOHmain
apuntando a un intérprete que no existe**: cada envío falla al arrancar el
proceso, pero la fila del resultado ya se construyó con el sitio, la pieza y
`cantDia` — que es lo que hay que fijar. Sin eso, con el flag apagado, el
reparto por pantalla se quedaba sin probar.

| Mutación | Resultado |
|---|---|
| `cantDia` = total de la pantalla en vez de `veces` | ✅ cae el reparto 4+2 |
| `aprobados.length < 1` en vez de `!== 1` | ✅ (tras añadir el caso del slot vaciado — **sobrevivió a la primera**) |
| Quitar el filtro `VALIDADA` de la publicación | ✅ (tras añadir el caso forzado — **sobrevivió a la primera**) |

Dos mutaciones sobrevivieron al primer intento y las dos señalaban un hueco real
en las pruebas, no en el código. Se añadió el caso que faltaba en cada una.

761 unitarias y 124 e2e en verde. **Sin desplegar**: es el cambio con más riesgo
del día —toca lo que sale a un CMS en vivo— y merece su propio runbook.

### Una sola política de contraseñas (V2-05, punto 2)

Lo dejó apuntado la otra sesión al decidir que el autorregistro es permanente.
Verificado y arreglado.

El servidor **sí** exigía 8 con letra y número (`registrarCuenta` llama a
`validarPassword`). Lo que estaba mal era el navegador:

| Formulario | Pedía | Efecto |
|---|---|---|
| **Registro público** (`/login`) | `length >= 6`, y el placeholder prometía «mínimo 6» | 400 después de enviar, en la primera pantalla de un alta abierta a internet |
| Alta de usuario | `length >= 8`, sin letra ni número | «aaaaaaaa» pasaba el filtro y rebotaba |
| Alta de organización | igual | igual |

> [!important] El fallo de fondo no era el número
> `validarPassword` vivía en `lib/server/auth.ts`, que es **`server-only`**: los
> formularios NO PODÍAN importarla, así que cada uno la reescribía a ojo. El
> comentario la llamaba «único origen de verdad» y no lo era.
>
> Se movió a `lib/password.ts` —sin `server-only`— y `auth.ts` la reexporta,
> así que sus doce llamadores no se tocan. Ahora navegador y servidor corren la
> MISMA función, y el motivo concreto se enseña mientras se teclea.

**La prueba que importa no prueba la función.** Once pruebas nuevas, y la
tercera tanda lee los tres ficheros y falla si alguno vuelve a comparar
longitudes a mano o a prometer un mínimo escrito en el placeholder. Las otras
diez pasaban igual con la regla duplicada — la función siempre estuvo bien; el
problema eran las copias. Verificado devolviendo el `>= 6` al registro público:
la prueba se pone roja.

Distingue un umbral de política de un `password.length > 0`, que es «¿ya
escribió algo?» y no una regla.

Quedan los otros dos puntos de V2-05: **verificación de correo** en el alta por
contraseña y revisar el **anti-abuso** (5/hora por IP).

### Error propio: `dcb03a8` se llevó trabajo de otra sesión

El commit de cierre del despliegue se hizo con `git add -A` y arrastró el
trabajo **en curso de otra sesión** sobre notificaciones: `archivar-todas`,
`notificaciones-repo.ts`, `Topbar.tsx`, su prueba y —lo que importa— la
migración `20260810_notificaciones_archivada_en.sql`.

No se revierte: los archivos están en el disco de esa sesión y un `revert` se
los borraría. Y el estado es sano — `tsc` en 0 y **752 unitarias en verde** con
todo incluido.

> [!danger] Consecuencia que sí hay que tener presente
> **`main` lleva ahora una migración que producción no tiene.** El droplet está
> en `484e768`, que es anterior. El **próximo despliegue** debe aplicar
> `20260810_notificaciones_archivada_en.sql` **antes** de recargar, o el código
> nuevo consultará una columna que no existe. Anotado también en [[tablero]],
> Z9.
>
> La migración es aditiva e idempotente (`add column if not exists` + índice
> parcial), así que aplicarla no tiene riesgo. El riesgo es **olvidarla**.

Lección para el procedimiento: en un repo con varias sesiones a la vez,
`git add -A` no vale. Hay que enumerar los archivos del cambio.

### El barrido corrió en producción — 11:57

El usuario abrió la app en g500 y quedó confirmado contra la base:

| Campaña | Antes | Ahora | Fin |
|---|---|---|---|
| `KFC` | ACTIVA | **COMPLETADA** | 2026-08-08 |
| `Propuesta para cliente 1` | ACTIVA | **COMPLETADA** | 2026-07-31 |

Exactamente las 2 del ensayo en seco. Las **otras 11** campañas siguen con el
estado que tenían antes, comparado fila a fila contra la captura previa.

Dos apuntes en bitácora a nombre de «Sistema», con el **mismo timestamp**
(`11:57:15.885027`) — o sea **un solo INSERT**, que es lo que hace el `unnest`.
Cero errores nuevos en el log; pm2 sigue en 28 reinicios.

> [!note] Un matiz, para no decir de más
> La **idempotencia en producción tiene un solo dato**, no varios: según el
> `access.log` solo ha habido UNA hidratación de g500 desde el despliegue
> (11:57). Está probada en la suite —tres pasadas seguidas dejan un único
> apunte— pero en producción todavía no se ha repetido. Se cierra sola en la
> próxima carga: si el conteo sigue en 2, confirmado.

## A medias

Nada de INC-03. Lo único abierto del día es el aviso de la migración de
notificaciones que quedó en `main` sin aplicar (arriba).

## Qué sigue

Sin cambios respecto al viernes salvo que el punto 1 ya está hecho:

1. ~~Desplegar `password_resets`~~ **HECHO hoy**
2. **Rotar la contraseña del droplet** — se usó otras cinco veces hoy.
3. **El registro público sigue ABIERTO** en producción (P3b).
4. **INC-02** — confirmar `DOOHMAIN_PUBLISH_ENABLED` antes de tocar la
   publicación por sitio. Es el pendiente de mayor impacto: la asignación
   creativo→pantalla se guarda pero **al publicar no se usa**.
5. **INC-07** — índice único de arrendador / doble envío. ~~INC-03~~,
   ~~INC-05~~ e ~~INC-09~~ hechos hoy; INC-03 e INC-09 **esperan despliegue**.
6. **Entrega 2 del ADR 0012**.

### Medido hoy en producción (INC-05 y B5/B9)

| Dato | Estado |
|---|---|
| Campañas con fechas invertidas | **0** — el importe negativo ya no existe |
| Creativos «WhatsApp Image» | **0** |
| OT sin responsable | **2** — son las 2 únicas OT que existen |
| Creativo «upsivale» | **1** |
| Nombre comercial del tenant | **`DEMO PIXELED.`** |
| Usuario DEMO | `demo.owner@pixeled.com.mx` |
| **B5** · sitios sin foto | **11 de 12** |
| **B9** · propuestas rechazadas | **0** → de ahí el win rate del 100% |

## Relacionadas
[[tablero]] · [[preguntas-abiertas]] · [[multi-tenancy-y-rls]] · [[MOC-Proyecto]]
