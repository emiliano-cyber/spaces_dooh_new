---
tipo: diario
estado: verificado
actualizado: 2026-09-09
tags: [agentes, diario, altas, adr-0027, adr-0028, adr-0029, google, flota]
archivos:
  - apps/flota/altas.mjs
  - apps/flota/solicitudes.mjs
  - apps/flota/dns.mjs
  - infra/env/app.env.example
  - infra/scripts/provision-instancia.sh
---

# 2026-09-09 — el alta completa desde el panel, de punta a punta y por primera vez

> **El día en una frase:** un formulario web creó una instancia de cliente que
> arrancó, obtuvo su dominio y su certificado sola, se inscribió en el panel, y
> su Dueño entró con Google y vio sus códigos de recuperación. Los cinco defectos
> de ayer estaban en el camino, y ninguno apareció.

## El recorrido, con sus tiempos

Formulario: instancia `g500`, dominio `g500.space-os.io`, correo del Dueño.

| Hito | Cuándo | Qué confirma |
|---|---|---|
| `pendiente` → `en-curso` | 06:13:13 | el temporizador, habilitado ayer |
| `✓ Docker 29.8.0 instalado` | — | **el candado de `apt`**, en una máquina nueva |
| `78 aplicadas, 1 de datos pendientes` | — | la imagen es `v0.4.1`, con las tres del 07/09 |
| `alta terminada`, ip `142.93.113.106` | 06:18:31 | **5 min 18 s** |
| `registro A creado: g500.space-os.io → …` | 06:18:31 | Cloudflare, por ser zona nuestra |
| `inscrita en el panel de flota` | 06:18:31 | **el defecto 38**, donde ayer decía `EACCES` |
| `esperando-dns` → `emitiendo-cert` → `lista` | — | la máquina de estados del ADR 0029 |
| `bootstrap 201` | 06:5x | la organización existe, y la puerta se cerró sola |
| El Dueño entra con Google y **ve sus códigos** | — | **el arreglo del 08/09, en una instancia** |

Cero comandos de `doctl`. Cero `ssh` para crear la máquina. El ejecutor pasa
`--crear-droplet` él mismo (`solicitudes.mjs:131`).

> [!important] Y de paso quedó zanjado un malentendido que costó dos altas
> «Con `ensayo4` funcionó» incluía comandos a mano, y **no porque tocara**: el
> 07/09 la máquina de estados estaba muerta (temporizador `disabled` + `marcar()`
> guardando donde nadie lee), así que la única salida fue hacer a mano lo que el
> guion imprimía. Ese recuerdo era de la avería, no del diseño.

## Lo que NO estaba documentado: cada instancia nace sin Google

Y no es un detalle: **sin Google configurado el bootstrap contesta 503**, porque
el Dueño entra solo con Google (ADR 0028). Así que el alta «automática» tiene
**tres pasos manuales obligatorios** que ningún documento reunía:

1. Autorizar en la consola de Google la URI de retorno del dominio nuevo:
   `https://<dominio>/spaces-dooh/api/auth/google/callback/` — **con la barra
   final**, o Google redirige a un 308 y el login se rompe
   (`google-oauth.ts:54-56`).
2. Escribir `GOOGLE_OAUTH=1`, `GOOGLE_CLIENT_ID` y `GOOGLE_CLIENT_SECRET` en el
   `/etc/space-os/app.env` **de la instancia**. Nace con
   `GOOGLE_OAUTH=0` y las dos vacías (`app.env.example:100-102`), y el
   aprovisionamiento solo rellena la `REDIRECT_URI`
   (`provision-instancia.sh:621`) — las credenciales no las puede inventar nadie.
3. **Recrear el contenedor.** Ni `docker restart` ni `update.sh` sirven: el
   entorno se congela al crear el contenedor (`update.sh:1669`, `docker run
   --env-file`) y `update.sh` sale con «sin cambios» si la imagen no cambió
   (`:1326`). Lo que funciona es `docker rm -f space-os` y después `update.sh`,
   que al no encontrar contenedor no toma el atajo (`:1327`).

El botón se pinta cuando `googleHabilitado()` es verdad, y eso exige **las tres
cosas a la vez**: `GOOGLE_OAUTH` distinto de `0` y las dos credenciales no vacías
(`google-oauth.ts:44-47`). Se comprueba sin abrir el navegador:

```bash
curl -s https://<dominio>/spaces-dooh/api/auth/metodos/   # {"google":true,…}
```

> [!warning] Zona ≠ registro, y esto costó un droplet ayer
> El token de Cloudflare crea **registros dentro de zonas que ya existen**
> (`POST /zones/{zona}/dns_records`). **Nada en el sistema crea zonas** — un
> dominio registrable nuevo se compra y se delega en un registrador, y eso es del
> owner. Un subdominio de `space-os.io` es automático; `g500-space-os.com` no
> existía en ningún sitio.

## La trampa del bootstrap, que es irreversible por diseño

`bootstrap 201` salió a la primera… **con el correo de relleno de la tarjeta**
(`el-correo-real@gmail.com`). El guion valida la FORMA del correo, y ese texto
era un correo válido, así que pasó. El intento anterior había fallado
precisamente porque `pon-aqui-el-correo-de-google` no lo era.

Y la ruta es **de un solo uso**: cerrada, responde 404 para siempre, con token o
sin él. O sea que el correo del Dueño **no se corrige repitiendo el bootstrap**.

> [!danger] La lección, y es sobre cómo se escriben las tarjetas
> **Un texto de relleno para un campo validado tiene que ser INVÁLIDO a
> propósito.** Uno que parece real anula el guard que lo protegía. El primero
> funcionó por accidente; el segundo falló por descuido de quien lo escribió.
>
> Se arregló a mano y fue barato **solo porque nadie había entrado todavía**:
> `identidades_externas` estaba en 0, así que no había identidad de Google
> vinculada y bastó un `update usuarios set email = … where id = …`, por `id` y
> no por el correo viejo. Con una sesión ya abierta habría que tocar dos tablas.

## Deuda que este día deja abierta

- **El respaldo de una instancia no sale del droplet.** `update.sh` avisa:
  «faltan SPACES_KEY/SPACES_SECRET … si la maquina desaparece, el dump desaparece
  con ella». El mismo par activa el log remoto, sin el cual diagnosticar exige
  entrar al servidor del owner. Para un ensayo da igual; **para un cliente que
  pague, no**.
- **Un cliente OAuth para toda la flota, o uno por instancia.** Hoy se reutiliza
  el del PADRE, y con eso el `CLIENT_SECRET` es el mismo en todos los droplets y
  esta pantalla de Google se vuelve un punto único. Es la misma **forma** del
  riesgo que mató `spaces123` en ROJO-1. Decisión abierta, y no es de Claude.
- **`6dcfc27` y `51a580e` sin fusionar**: el guard de zona DNS (que habría
  ahorrado el droplet de ayer) y el dominio en blanco (que hace de esto un solo
  campo).
- Sigue en pie lo del 08/09: nadie valida el entorno del ejecutor al desplegarlo,
  y `provision-instancia.sh` no tiene arnés propio de extremo a extremo.

## Por la tarde: el plan para llenar esa instancia

La instancia de g500 nació **vacía**, y esa es la siguiente pregunta: sus datos
están en el droplet de julio (`209.97.146.136`). Emiliano decidió traerlos, y el
plan quedó escrito en **`docs/Plan_Migracion_Datos_g500.md`** con su primera
tarjeta en `docs/evidencias/migracion-g500-E1-dump.txt`. **No se ejecutó nada.**

Cuatro decisiones suyas, para que no se vuelvan a deducir: **solo `g500`** viaja
—las otras cuatro organizaciones no—, **viaja todo menos las personas**, el
camino es una **base puente local**, y las dos máquinas están en **cuentas de
DigitalOcean distintas**, así que no hay snapshot ni red privada: el dump pasa
por el equipo local de todos modos.

> [!important] Las personas: dijo «todo» y luego lo corrigió el mismo día
> La primera respuesta fue traer usuarios, roles y hashes; la segunda, **todo
> menos los usuarios**. Manda la segunda, y el plan quedó corregido.
>
> **Sale ganando el ADR 0028:** sin usuarios importados no hay un segundo `DUENO`
> que llegue con contraseña, así que desaparece la única fricción que tenía este
> traslado con esa decisión. Lo que se pierde son **cuatro columnas**: quién
> reportó cada incidencia, `asignado_a` y `supervisor` de cada OT, y quién subió
> cada evidencia — las cinco referencias a `usuarios` son `on delete set null`.
>
> **Y la bitácora sobrevive**, que no era evidente: `acciones` guarda
> `usuario_nombre` **como texto** junto al `usuario_id`
> (`db/schema.sql:574-580`), así que la historia se sigue leyendo con el id en
> nulo.

### La brecha, medida

| | Droplet viejo | Instancia g500 |
|---|---|---|
| Código | `504b4fc` · 11/08 | imagen `v0.4.1` · hoy |
| Migraciones | **66** | **78 + 1 de datos** |
| Organizaciones | 5 | 1, la de esta mañana |

**Trece migraciones de diferencia.** Por eso el puente: se restaura el dump en un
Postgres local, se le corre `migrar.mjs` hasta la versión del cliente, y solo
entonces se recorta g500 — desde un esquema **idéntico** al destino. La
adaptación deja de ser criterio de nadie. Y no es un invento: el runner **ya
contempla** una base con historia y sin `schema_migrations`, y dice qué hacer
(`scripts/migrar.mjs:513-528`).

### Los dos fallos silenciosos que el censo tiene que cazar

- **La deriva del `DEFAULT`.** El droplet viejo todavía tiene el `DEFAULT` de
  `tenant_id` apuntando a RGB, que `20260812_sin_default_tenant.sql` retiró. Hay
  filas de g500 etiquetadas como `rgb`, y un `where tenant_id = g500` **las deja
  atrás sin dar ningún error**. Es R2 por su modo de fallo de siempre.
- **Los folios.** `folios_consecutivos` es `PK (ambito, periodo)` con un `ultimo
  integer` (`db/schema.sql:95-100`). Si entran facturas y propuestas sin
  adelantar ese contador, **la instancia reemite folios ya usados**.

### E1 ejecutada la misma tarde, y una etapa que se cae

La tarjeta se corrió desde la consola web. GATE 1 en verde, y **la aplicación
vive en `/var/www/Spaces`** — no donde suponía la tarjeta, y tampoco donde dice
el `README` de la raíz. El entorno está en `apps/web/.env.production`, con **doce
respaldos `.bak.*`** al lado.

**La pregunta que decidía una etapa entera quedó contestada: no hay bucket.** Si
el droplet viejo hubiera tenido `DO_SPACES_*`, las fotos de las OT vivirían en un
bucket **de la cuenta vieja** (`ot-repo.ts:44`) y las filas llegarían apuntando a
nada. Dos fuentes coinciden en que no las tiene —el entorno del proceso y el
propio `.env.production`, que da **0**—, así que `storageHabilitado()` es falso y
las fotos son **base64 dentro de la base**: viajan en el dump. La etapa extra no
existe.

Y `schema_migrations` **NO EXISTE** con **66** migraciones en disco: la firma
exacta que el runner sabe resolver. Dump de **7.3 MB**.

> [!warning] E1 prometía no escribir nada, y escribió una llave
> La contraseña de `root` se había perdido y la llave efímera de la consola web
> de DigitalOcean (`dotty_ssh`) **caducó a las 11:38**, así que para bajar el
> dump hubo que añadir una llave a `/root/.ssh/authorized_keys`. Es el único
> cambio de toda E1 sobre esa máquina, es reversible borrando la línea, y **se
> retira al terminar el traslado**.
>
> De paso, dos rellenos de ejemplo entraron literales al archivo. Un *heredoc* y
> un `echo` **no validan nada**: el principio de «el relleno tiene que ser
> inválido a propósito» no protege cuando lo que recibe el texto es un
> redirector. Se limpiaron.

> [!note] El ADR 0023 no se ignora: se sustituye
> Su punto 2 dice «no se exporta ni se respalda su base», y lo sostiene en que no
> hay ninguna organización real que migrar. **Eso caducó hoy.** Al cerrar el
> traslado se escribe el ADR 0031; lo demás del 0023 sigue en pie.

> [!tip] La lección de esta mañana, aplicada el mismo día
> Los rellenos de la tarjeta E1 están escritos para **tronar** si nadie los
> sustituye (`<PON-AQUI-EL-USUARIO>`). Es la consecuencia directa del
> `el-correo-real@gmail.com` que pasó el guard del bootstrap.

## Y el mismo día, las otras tres etapas: g500 tiene sus datos

Las cuatro etapas del traslado se ejecutaron el 09/09. **541 filas dentro**, las
**12 pantallas con su tarifa**, ninguna persona y ninguna línea de bitácora.
Expediente: `docs/evidencias/migracion-g500-E4-resultado.md`. Lo autoriza el
**ADR 0031**, que sustituye el punto 2 del ADR 0023 — su premisa («no hay
organización real que migrar») caducó esta misma mañana, cuando g500 tuvo
instancia.

### Lo que encontró el censo, y era lo importante

**Las 12 modalidades de venta de las 12 pantallas de g500 estaban etiquetadas
`rgb`.** Un `where tenant_id = g500` habría entregado las pantallas **sin un solo
precio y sin dar error** — R2 por su modo de fallo de siempre. El detector no fue
una lista a mano: recorre **todas** las claves ajenas donde madre e hija tienen
`tenant_id`. Dos desajustes en toda la base, y `rgb` no tiene ni una pantalla
propia.

### Tres cosas que solo se ven ensayando

1. **El esquema del destino es más estricto que el del droplet viejo.** Mismas
   524 columnas, pero **tres restricciones que solo viven en `schema.sql`** y que
   ninguna migración reparte. Los datos de g500 las cumplen las tres — pero la
   lección es general: *el registro puede decir «78 aplicadas» en dos bases y los
   esquemas no ser iguales*.
2. **`acciones` es append-only por trigger**, así que cargarla dejaba la
   operación sin marcha atrás en el sitio. Se dejó fuera, y con eso el ciclo
   carga → deshacer → recargar funciona.
3. **`folios_consecutivos` no tiene `tenant_id`**, al contrario de lo que afirma
   el comentario de `db/schema.sql:102-103`. El contador de folios es de la
   INSTANCIA, no de la organización.

### El GATE 2 atrapó una máquina equivocada

Durante la operación hubo una sesión abierta en el **droplet viejo** creyendo que
era la instancia. Se comprobó con `hostname` antes de escribir nada. Es el mismo
error del 24/08 —cuando se censó entera la máquina equivocada— con la diferencia
de que esta vez el gate estaba escrito antes del primer comando.

> [!danger] Y apareció un defecto de FLOTA que no tiene nada que ver con los datos
> Al navegar la aplicación por primera vez con contenido dentro, las
> redirecciones mandaban el navegador a `localhost:3000`. **No lo causó la
> carga.** `request.nextUrl` no toma su origen de la cabecera `Host` sino de la
> dirección donde escucha el servidor (`Dockerfile:72-73`), así que el middleware
> mandaba al cliente a la dirección **interna del contenedor**.
>
> Medido con tres pruebas que aíslan la causa: por el dominio, directo al
> contenedor **con `Host` correcto**, y la redirección de `next.config.mjs` —que
> no pasa por el middleware y sale relativa y bien—.
>
> **Lo tienen todas las instancias**, DEMO incluida, hasta que jalen versión
> nueva. Corregido con `Location` relativa en `fix/middleware-redireccion-localhost`
> (7 pruebas nuevas, 1113 unitarias y 344 e2e en verde). Detalle en
> [[autenticacion-y-sesion]].

### Deuda que este traslado deja

- **Dos llaves SSH puestas a mano**, en el droplet viejo y en la instancia:
  tarjeta `migracion-g500-retirar-llaves.txt`, sin correr.
- **El acceso de soporte del ADR 0025 no existe**, y esta fue la primera
  operación real que lo necesitaba. Se resolvió por la consola web de
  DigitalOcean, la excepción que el propio ADR nombra.
- **El respaldo de la instancia sigue sin salir del droplet**: hoy se bajó a
  mano al equipo local, y eso no escala.

## Relacionadas
[[2026-09-08]] · [[acceso-y-sesion-ui]] · [[autenticacion-y-sesion]] ·
[[modelo-instancias-soberanas]] · [[01-Arquitectura/entorno-y-despliegue]] ·
[[tablero]]
