---
tipo: diario
estado: verificado
actualizado: 2026-08-17
tags: [agentes, diario, instancias, orquestacion, multi-tenancy, migraciones]
archivos:
  - docs/Plan_Instancias_Soberanas_v3.md
  - docs/prompt-ejecucion-plan-v3.md
  - vault/07-Agentes/ejecucion-plan-v3.md
  - .claude/commands/orquestar.md
  - .claude/agents/ejecutor.md
  - .claude/agents/verificador.md
  - .claude/agents/ensayista-local.md
  - apps/web/lib/server/campanas-repo.ts
  - apps/web/lib/host.ts
  - apps/web/middleware.ts
  - apps/web/scripts/bootstrap-auth.mjs
  - db/migrations/20260812_sin_default_tenant.sql
  - apps/web/next.config.mjs
---

# Diario — 2026-08-13 (jueves)

> [!note] Escrita con retraso
> Redactada el **2026-08-17** desde `git log` y desde la bitácora de
> [[ejecucion-plan-v3]], que es la que guarda el detalle tarea por tarea.

**17 commits.** Entra el plan v3 con los tres agentes que lo ejecutan, y la Fase 1
se abre y se cierra en local el mismo día.

## Agentes activos

| Agente | Zona | Rama | Ventana |
|---|---|---|---|
| orquestador (`/orquestar`) | Z12 · Docs | `feat/servidor-padre-instancias` | 10:27 – 17:56 |
| ejecutor (F1.3, F1.4, F1.2, T-01, T-02, F2.1) | Z2 · Comercial · Z1 · Auth · Z9 · Datos | misma rama | 14:26 – 17:56 |
| verificador (una sesión por tarea) | — solo lectura | misma rama | intercalado |
| ensayista-local (F1.1) | — solo lectura sobre el 5433 | — no commitea | tarde |

> [!warning] Z9 y Z2 **no aparecen `TOMADA` en el diff de ningún commit**
> Es la regla 1 de [[AGENTES]] y esta tanda la incumple de principio a fin. La
> causa real no se entendió hasta el 17/08: con «una tarea = un commit» **no hay
> forma de mostrar reclamo y liberación**, harían falta dos commits y la regla de
> ejecución prohíbe el segundo. Ver [[2026-08-17]].

## Completado

### Primero, el entorno — y dos trampas que costaron tiempo real

Montar el worktree fue `npm install`, copiar los dos `.env` y revivir `spaces_db`
con `docker start` (había quedado `Exited` al reiniciar Docker). Línea base
medida, no supuesta: typecheck limpio y **789 unitarias en 71 archivos**.

`c710657` (15:05) recoge lo que apareció al hacerlo, porque las dos cosas cuestan
tiempo y ninguna dice nada del código:

> [!danger] Las e2e exigen un build de Next hecho ANTES, o fallan todas en falso
> `apps/web/lib/test/servidor-e2e.ts:31` arranca con `npx next start`, que
> **reutiliza el build existente y no construye nada**. En un worktree recién
> montado no hay `.next/BUILD_ID`, así que los 12 archivos e2e mueren con «El
> servidor de pruebas no respondió» **tras 636 s**. Con el build hecho: 61 s.
> Le costó 10 minutos al ejecutor de F1.3.
>
> ```
> cd apps/web && npm run build && npm run test:e2e
> ```

> [!warning] Los comandos de prueba no estaban donde la documentación decía
> Los tres agentes y `CLAUDE.md` mandaban correr `npm run typecheck`, `npm test` y
> `npm run test:e2e` **desde la raíz del repo**, donde no existen: viven en
> `apps/web/package.json` y desde la raíz devuelven `npm error Missing script`,
> que se confunde con «el entorno está roto». Los cuatro archivos anteponen ahora
> `cd apps/web`. **El plan v3 no se tocó**: sus 46 comandos de verificación ya
> estaban bien.

Y una regla nueva que el DAG del plan no contemplaba: **el par aprobado
(F1.3 ∥ F1.4) no se paraleliza**. `vitest.e2e.config.ts:16-17` explica que las e2e
corren en serie porque comparten la única base `spaces_e2e` y **cada archivo la
recrea con `drop schema public cascade`**. Dos agentes a la vez se borran la base
a media corrida. El DAG las aprobó por no compartir zona ni archivos —cierto— pero
no miró la base compartida. Queda escrita en `orquestar.md`.

### El plan y su equipo entran al repositorio — 14:26

`1ad1045` · 8 archivos, 3 023 líneas, **ni una de código de producción**.

El desorden de fondo: el plan v3 aprobado ese día y su tablero **existían solo en
el disco**. `git status` los daba como no rastreados sobre `845785d`, y **un
`git clean` habría borrado el documento de autoridad de las nueve fases**. Entran
antes de la primera tarea, para que cada tarea siguiente sea un commit legible por
sí solo y no arrastre el plan en su diff.

Van juntos porque son una sola pieza:

- `docs/Plan_Instancias_Soberanas_v3.md` — **46 tareas**, sustituye al v2 de ayer
  (que se queda como historia).
- `docs/prompt-ejecucion-plan-v3.md` — cómo se arranca una sesión de ejecución.
- `vault/07-Agentes/ejecucion-plan-v3.md` — el tablero: DAG y estado por tarea.
- `CLAUDE.md` — lo primero que lee un agente al abrir el repo.
- Los cuatro archivos del equipo: **ejecutor**, **verificador**, **ensayista-local**
  y el comando **`/orquestar`**.

> [!important] Por qué son tres agentes y no uno
> El **verificador** audita en **sesión separada del ejecutor, por diseño**: el
> remediador no se autoconfirma. El **ensayista-local** ensaya lo que en producción
> correría una persona (builds, runner sobre base desechable) y **nunca commitea**.
> Ese reparto es lo que hace que las auditorías de esta tanda encuentren cosas que
> el ejecutor no podía enseñar — se ve tarea por tarea más abajo.

Antes, a las 10:27, `845785d` había metido a la bóveda el avance de la corrección
con dos cosas que el plan no traía: **lo que cuesta** (≈28 USD/mes de
infraestructura nueva —droplet padre, droplet de DEMO, backups, registry y
snapshot— más ≈15 por instancia de owner, **precios de lista, la cuenta no se
consultó**) y **un desacuerdo con el calendario puesto por escrito antes de
arrancar**: las dos semanas del documento salen de sumar 13 días hábiles en
secuencia suponiendo paralelismo perfecto; la nota estima **tres a cuatro semanas**
para las fases 0–6.

### F1.3 · el cupo global se leía fiándose solo de la RLS

`c50344a` (14:56) · `campanas-repo.ts` + `campanas-repo.cupo-clientes.test.ts`.

La auditoría no se creyó el arreglo: **desactivó la RLS de facto** y midió. Con un
GUC de otra organización, la consulta vieja devolvía **1 fila** —fuga— y la nueva
**0**. Veredicto AMARILLO, tres asperezas menores, ninguna bloqueante.

**ROJO por R2** (aislamiento entre organizaciones): pendiente de visto bueno humano
antes del merge. De aquí sale **TH-01**, la primera tarjeta humana: comprobar en
producción que `config_negocio` no tiene filas con `tenant_id` nulo, porque si las
tuviera el filtro nuevo dejaría a esa organización **«sin límite» de cupo sin
avisar**.

### F1.4 · una dirección IP dejaba de ser IP y parecía subdominio

`3671e8a` (15:19) · nace `apps/web/lib/host.ts` (+ sus pruebas) y `middleware.ts`
pierde `extractSubdomain`.

El ejecutor mostró **dos rojos**: primero el módulo ausente, y después la lógica
vieja extraída tal cual **fallando los casos que la tarea predice** — con
`209.97.146.136` el subdominio salía `'209'`.

La auditoría tampoco se fio de las pruebas: compiló `host.ts` con `tsc` y lo
comparó contra la lógica vieja reimplementada, levantó `next start` en el **3312**
y mandó encabezados `Host` a mano. Y comprobó que **el bundle correspondía al
código** (`middleware.js` sin `parts.length`, con la guarda IPv6 nueva) en vez de
fiarse de la marca de tiempo.

> [!warning] Queda abierto, y se escaló: el rewrite de `portal` **sí cambia**
> Con `Host` en mayúsculas (`PORTAL.space-os.io`) o con punto final
> (`portal.space-os.io.`, FQDN perfectamente legal) el comportamiento no es el de
> antes, y la tarea exigía que ese rewrite siguiera igual. `host.ts:44-45` lo
> justifica como «basura» sin mencionar el FQDN. **No es agujero de seguridad**
> —`/portal/*` es público por token— y los navegadores normalizan, así que no es
> alcanzable desde un cliente real. Pendiente de decisión.

### F1.2 · se detiene antes de escribir una línea

`a6bd4c0` (16:36). El ejecutor corrió la comprobación previa que el propio plan
exige en su apartado «Riesgo y vuelta atrás» —un `rg` sobre los 76 inserts de
`apps/web`— y encontró **uno que no nombra `tenant_id`**. Ni reclamó zona ni tocó
el tablero: paró en el paso previo, que es exactamente lo que la regla de «no se
replanea» pide.

> [!danger] El plan afirma algo que el repositorio desmiente
> `Plan_Instancias_Soberanas_v3.md:551-552` dice «Comprobado que **no hay
> ninguna**» ruta que inserte sin fijar tenant. Sí la hay:
> **`apps/web/scripts/bootstrap-auth.mjs:54`** inserta en `usuarios` sin
> `tenant_id`. Como esa columna es `NOT NULL` (verificado contra el catálogo), hoy
> solo funciona porque el `DEFAULT` lo rellena — el que F1.2 retira. Aplicada la
> migración lanzaría **23502 siempre**, incluso para un correo existente: Postgres
> valida el `NOT NULL` **antes** de arbitrar el `ON CONFLICT`, así que el
> `do update` no lo salva.
>
> **Decisión de Jochelo: el plan NO se corrige.** La evidencia vive en
> [[ejecucion-plan-v3]].

### T-01 · el script no estaba a punto de romperse: ya estaba roto

`b976b54` (16:58) · `bootstrap-auth.mjs`, fuera del plan, autorizada por Jochelo y
**antes** de F1.2, para que la migración entrara después con solo sus archivos.

`605e2a4` (16:44) es la corrección más incómoda del día, y es del orquestador:

> [!warning] Una premisa del briefing era falsa, y venía de un reporte sin ejecutar
> Se le pasó al ejecutor como **verificado** que el script «hoy funciona gracias al
> `DEFAULT`». Ese dato venía del reporte de F1.2, que lo **dedujo sin ejecutarlo**.
> El ejecutor lo corrió contra una base desechable y **falla siempre**, por una
> causa anterior: `bootstrap-auth.mjs:56` usa `on conflict (email)`, pero la
> unicidad de correo es un índice **funcional** sobre `lower(email)`
> (`db/schema.sql:72`) y Postgres no lo infiere → **42P10**.
>
> Aisló las dos causas: sin el `on conflict`, sale el 23502 esperado. O sea que el
> script **lleva roto** desde que se cambió la unicidad.

Las dos causas se arreglan en el mismo commit, y se decide que **si falta el tenant
`rgb` el script aborta con error**: el no-op silencioso es el modo de fallo que ya
costó un despliegue entero (ver [[06-Operacion/zonas-de-riesgo]], R2).

La auditoría corrió la e2e **aunque el ejecutor argumentara que no hacía falta**
—«prefiero el dato sobre el argumento»— y midió el razonamiento del `rowCount` en
vez de aceptarlo: con `do update` el conflicto devuelve 1 y con `do nothing`
devuelve 0, así que el 0 solo puede venir del tenant ausente. Sin ventana TOCTOU.

> [!important] Hallazgo preexistente que esa auditoría destapó, y que muerde a la Fase 5
> Montando una base con las 66 migraciones se vio que **el script solo funciona con
> un rol que salte la RLS**. Con `spaces` (superusuario) va; con un rol sin
> `bypassrls` falla, porque `20260720_hard1_usuarios_rls.sql` deja `usuarios` en
> RLS FORCE con `with check (tenant_id = app.tenant_id)` y el script no fija el
> GUC. **Si el runbook de aprovisionamiento siembra con el rol de la aplicación,
> el bootstrap fallará con 42501.**

### T-02 · el bootstrap escribía en la base con datos reales si nadie decía otra cosa

`3ac2bba` (17:33) · **el único VERDE de toda la tanda**.

`bootstrap-auth.mjs:9-10` tomaba por omisión
`postgresql://spaces:spaces@localhost:5433/spaces` — **la base con datos reales**,
y con rol superusuario, que además salta la RLS `FORCE` de `usuarios`. Correr el
script sin `DATABASE_URL` escribía ahí. Ahora la variable es **obligatoria**.

La auditoría añadió el argumento decisivo que el ejecutor no dio para descartar
una lista negra de nombres de base: **una lista negra es fail-open** — protege el
único nombre conocido hoy, y bajo el modelo de instancias soberanas **cada
instancia tiene su propia base con datos reales y nombre propio**. Exigir
explicitud es fail-closed.

Y comprobó un refuerzo que nadie había declarado: **el script no carga `dotenv`**.
`apps/web/.env.local` sí define `DATABASE_URL`, y aun así la corrida con la
variable desactivada aborta. El fail-closed no tiene la puerta trasera de heredar
el destino de un `.env` que el operador no leyó.

> [!note] Contrato nuevo que heredan las Fases 2 y 3
> Cuando se escriba el aprovisionamiento de una instancia, el paso que siembre su
> primer usuario **tiene que pasar `DATABASE_URL` explícito o fallará**. Hoy no hay
> ningún llamador en el repo, así que nada está roto — pero es una condición nueva
> que esas tareas no conocen.

### F1.2 · la migración, con el rojo más elocuente del día

`65bf9b5` (17:24) · `db/migrations/20260812_sin_default_tenant.sql` +
`tenant-sin-default.e2e.test.ts`.

El rojo no fue una prueba que falla por convención: fue **el fallo silencioso
ocurriendo en vivo**. `insert into clientes (nombre)` sin tenant devolvía «promise
resolved» y **la fila nacía etiquetada como `rgb`**.

La migración **descubre las tablas por catálogo, no por lista** —que es lo que la
hace no envejecer—, se probó idempotente ×3, y trae un guard que aborta si alguna
columna con `DEFAULT` no tiene `NOT NULL`.

> [!important] Un guard que nunca se ha visto saltar no está probado
> Eso es lo que el ejecutor no podía enseñar y la auditoría sí: fabricó dos tablas
> con `tenant_id` con `DEFAULT` y sin `NOT NULL`, corrió la migración, **la vio
> abortar nombrando las dos** y, al no haber `commit`, el fixture quedó intacto.
> Ese guard es lo único que impedirá, cuando F1.5 corra contra producción, quitar
> un default que fuera lo único sosteniendo una columna viva.

La misma auditoría **reclasificó los 78 inserts por su cuenta** en vez de creerse
la clasificación: 10 no nombran `tenant_id`, menos los 2 del archivo nuevo del
propio commit = los 9 del plan; **8 son aserciones en `.test.ts`** y el noveno es
`sitios-repo.ts:277`, que la añade dinámicamente en `:275`. **Cero rutas reales
insertan sin fijar tenant.**

**ROJO por partida doble** —migración **y** tenant, los dos disparadores a la vez—:
pendiente de visto bueno humano.

### F1.1 · el catálogo cuadra; los datos son mudos

Ensayada por el ensayista-local en solo lectura sobre el 5433, **en paralelo con
F1.4** — no es un par del DAG, pero no comparten base, archivos ni git, y el
ensayista no commitea. Aprobado por Jochelo, sin interferencia.

Confirmado: **23 tablas exactas** contra `db/schema.sql:604-609`, los 6 nombres de
enganche marcados `[SIN VERIFICAR]` en el plan existen tal cual, y **0 tablas con
default y `attnotnull=false`** (el guard de F1.2 no abortaría).

> [!warning] La mitad de datos del ensayo no vale, y hay que decirlo
> **La base local es fixture**: 33 filas, `sitio_modalidades` vacía, sin `g500` ni
> `eyro`. Sus ceros son **vacuos**, no tranquilizadores. El censo real es
> **TH-02**, y lo corre una persona contra producción.

Y un agujero del plan que el ensayo destapó: el comando de verificación de F1.1
(`:434`) hace `psql -f /tmp/auditoria_tenant.sql`, y **ese archivo no existe ni el
plan lo crea**. TH-02 lo resuelve en dos pasos. **El plan no se tocó.**

### F2.1 · el build produce un servidor autocontenido — 17:56

`8ae8f77` · `apps/web/next.config.mjs`. Arranca la Fase 2. Se auditó al día
siguiente (ver [[2026-08-14]]): la primera auditoría murió a mitad por un login
expirado y hubo que relanzarla de cero.

### Barrido de recuentos desfasados en la bóveda

`64c35df` (17:39). Salió del hallazgo 2 de la auditoría de F1.2, y el grave era
uno solo:

> [!danger] `verificacion-de-produccion.md:144` decía «Esperado: **21** tablas»
> Es **la nota que leerá quien corra F1.5 contra producción**. Si el catálogo real
> devolviera 23 o más, lo habría leído como un desfase alarmante cuando es
> **justo lo que F1.1 busca**. Corregido a «23 o más», con el porqué escrito al
> lado.

Corregidos también `MOC-Proyecto`, `esquema`, `entorno-y-despliegue` (66 → **67**
migraciones) y **P15 de `preguntas-abiertas`, que seguía planteando como pregunta
abierta lo que F1.2 acababa de responder**.

**No se tocaron** `manual-tecnico.md` ni `inventario-2026-08-11.md`, que arrastran
las mismas cifras viejas: el manual se declara construido desde el inventario del
11/08 (`:30-31`), así que parchearle números sueltos lo dejaría **internamente
incoherente con su propia procedencia**. Los dos están pendientes de
**regenerarse**, no de parchearse.

## A medias — y dónde exactamente

- **Fase 1** — rama `feat/servidor-padre-instancias`
  - Último punto bueno: `28c4d4a` (17:45), «cierre de la Fase 1 en local».
  - **Se quedó en:** todo lo ejecutable **en local** hecho y auditado (F1.3, F1.4,
    F1.2, T-01, T-02; F1.1 ensayada). **En producción no se aplicó nada**: el
    `DEFAULT` sigue vivo en `spaces_prod`.
  - Siguiente paso concreto: **TH-02** (censo real) y después **F1.5** (aplicar
    `20260812_sin_default_tenant.sql` al droplet). Las dos las corre una persona.
  - ⚠️ El 17/08 se descubrió que F1.5 llevaba **desde este día** en
    `PENDIENTE_SERVIDOR` **sin tarjeta emitida**, que es lo que la leyenda del
    tablero exige. La tarjeta **TH-F1.5** nació cuatro días tarde.

- **F1.4, el flanco abierto** — `apps/web/lib/host.ts:44-45`
  - Falta decidir qué hacer con `Host` en mayúsculas y con el punto final del FQDN.

- **Una regresión de puntero, corregida el mismo día** — `3c5ff43` (15:36)
  - `acceso-y-sesion-ui.md:74` citaba `middleware.ts:92`; la línea correcta es
    `:94`. **Antes del commit la cita era `:100` y era correcta**: el ejecutor
    aplicó −8 donde tocaba −6, porque el corrimiento no era uniforme (el comentario
    del ruteo creció de 1 a 3 líneas). **Es el precio de reparar punteros con
    aritmética sin abrir el archivo.**

## Decisiones tomadas

| Decisión | Por qué | ¿ADR? |
|---|---|---|
| **P4-bis: la bandera del autoregistro sale del build** (`fef7499`, 17:46) | Un solo artefacto por versión; el autoregistro se decide en el `.env` al arrancar, como ya se hizo con `GOOGLE_OAUTH` | No; queda en [[ejecucion-plan-v3]]. Se ejecuta el 14/08 en F2.6 |
| **T-01 se hace fuera del plan y ANTES de F1.2** | Arreglar `bootstrap-auth.mjs` dentro de F1.2 habría inflado su diff; ignorarlo era peor: ese script crea **el primer usuario de una base recién creada**, que es justo lo que necesita cada instancia nueva | — |
| **T-01a: las dos causas en el mismo commit** (`on conflict` y `tenant_id`) | Son un solo defecto del mismo script | — |
| **T-01b: si falta el tenant `rgb`, abortar** | El no-op silencioso es el modo de fallo que ya costó un despliegue | — |
| **T-02: `DATABASE_URL` obligatoria**, sin lista negra | Una lista negra es fail-open: solo conoce el nombre de hoy, y cada instancia soberana traerá el suyo | — |
| **El plan NO se corrige cuando el repositorio lo desmiente** | La evidencia vive en el tablero de ejecución; corregir el plan a media ejecución es replanear | — |
| **El par (F1.3 ∥ F1.4) no se paraleliza** | Comparten la única base `spaces_e2e`, que cada archivo recrea con `drop schema public cascade` | Escrito en `orquestar.md` |

> [!note] El precedente que P4-bis no necesitaba inventar
> El propio código ya lo documentaba: `apps/web/lib/server/google-oauth.ts:35-36`
> dice «apaga la función EN EL SERVIDOR, no solo escondiendo el botón — **misma
> lección que `NEXT_PUBLIC_AUTOREGISTRO`**. Y NO lleva prefijo». Alguien ya había
> aprendido esto y dejó escrito que esta bandera era el siguiente caso.

## Bloqueos

| Bloqueo | Quién lo desbloquea | Desde |
|---|---|---|
| **F1.5** — aplicar la migración al droplet | Una persona (tarjeta, emitida por fin el 17/08) | 2026-08-13 |
| **TH-02** — el censo real de los 23 `DEFAULT` en producción | Una persona | 2026-08-13 |
| **TH-01** — comprobar `config_negocio` en producción | Una persona | 2026-08-13 |
| **Cinco commits ROJO** esperando visto bueno humano antes del merge | Jochelo | 2026-08-13 |
| **F2.3 y F2.4** — el nombre del registry (**P4**) | Jochelo (se resolvió el 17/08) | 2026-08-13 |
| P1 (destino de `rgb` y del droplet), P2 (fecha de PIXELED), P3 (cuenta DO), P6 | Jochelo | antes del 13/08 |

> [!warning] «Cinco commits ROJO» resultó ser una cifra mal contada
> El 14/08 se descubrió que la cuenta que venía dando el orquestador estaba
> inflada, y el 17/08 que el titular de la Fase 1 seguía diciendo cinco sobre una
> tabla que listaba cuatro. El criterio, escrito el 14/08: **ROJO es lo que su
> ejecutor declaró ROJO en el reporte de la tarea**, ni más ni menos. «Toca un tema
> sensible» no basta. Ver [[2026-08-14]].

## Qué sigue mañana

1. **F2.6** — ejecutar P4-bis: sacar la bandera del build.
2. **F2.2** — la imagen de la aplicación, con `db/` dentro.
3. **F2.5** — smoke de esa imagen.
4. **F3.1** — la tabla `schema_migrations` y su backfill.
5. Empezar a levantar **expedientes de evidencia** por fase: hoy la Fase 1 se
   declaró cerrada en local **sin ninguno**.

## Notas para quien lea esto sin contexto

- **Si las e2e fallan todas y tardan diez minutos en hacerlo, no mires el código:
  falta el build.** `npm run build` antes de `npm run test:e2e`, siempre, en
  worktree recién montado.
- **Los scripts de pruebas no existen en la raíz del repo.** `cd apps/web`
  primero, o `npm error Missing script` te hará creer que el entorno está roto.
- **Dos tareas que verifiquen con e2e no se paralelizan nunca**, por muy
  independientes que las declare el DAG: se borran la base la una a la otra.
- **La base del 5433 es fixture para unas cosas y real para otras.** Sirve para
  confirmar un **catálogo** (23 tablas, columnas, defaults); **no sirve para
  contar filas** — 33 en todo el juego, sin `g500` ni `eyro`. Un cero medido ahí
  no prueba nada de producción.
- **Un reporte que deduce sin ejecutar contamina al siguiente agente.** La premisa
  falsa de T-01 no la inventó el orquestador: la copió de un reporte anterior y la
  pasó como verificada. Cuesta un intento entero. Si un dato no se midió, se
  etiqueta como no medido.
- **Reparar `archivo:línea` con aritmética sin abrir el archivo sale mal.** El
  corrimiento casi nunca es uniforme; un comentario que crece de 1 a 3 líneas
  desplaza dos citas y no la tercera.

## Relacionadas
[[2026-08-12]] · [[2026-08-14]] · [[2026-08-17]] · [[ejecucion-plan-v3]] ·
[[tablero]] · [[AGENTES]] · [[06-Operacion/zonas-de-riesgo]] ·
[[02-Backend/multi-tenancy-y-rls]] · [[04-Datos/migraciones]] ·
[[01-Arquitectura/modelo-instancias-soberanas]] · [[MOC-Proyecto]]
