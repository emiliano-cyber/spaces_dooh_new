---
tipo: tablero
estado: verificado
actualizado: 2026-08-18
tags: [agentes, coordinacion, vivo]
archivos: []
---

# Tablero de zonas

> [!warning] Reclama antes de escribir
> Protocolo completo en [[AGENTES]]. Si tu zona está `TOMADA`, **elige otra
> tarea** — no esperes.

**Estados:** `LIBRE` · `TOMADA` · `PAUSADA` · `REVISION`

## Zonas

| Zona | Estado | Agente | Archivos | Rama | Desde | Notas |
|---|---|---|---|---|---|---|
| Z1 · Auth 🔴 | LIBRE | — | — | — | — | **F2.6 hecha (14/08), ROJA:** el autoregistro sale del build y **cambia de polaridad**. La bandera pasa de `NEXT_PUBLIC_AUTOREGISTRO` a `AUTOREGISTRO` y ahora **solo `=1` enciende**: ausente = APAGADO, al revés que antes (`!== '0'`). Si tocas esto, el fail-closed es el punto, no un detalle. Estaba roto en dos mitades opuestas: el SERVIDOR ya funcionaba en runtime (503/400 con la misma imagen), pero el CLIENTE venía horneado y horneado ENCENDIDO — `/login` se prerrenderiza y su HTML traía el botón «Crear cuenta» dentro, así que cada instancia habría enseñado un botón que contesta 503. Arreglado por `/api/auth/metodos/`, que ahora devuelve `{"google":…,"autoregistro":…}`; **la vía de props desde el layout NO sirve**, se resolvería en el mismo render de build. `aislamiento.e2e.test.ts` pasó SIN TOCARSE: su bloque `:200-213` ya está obsoleto pero se retira en un release posterior (expand → contract). El arnés `servidor-e2e.ts` fija `AUTOREGISTRO: '0'` explícito aunque ausente ya bastaría. **T-02 hecha (13/08):** `bootstrap-auth.mjs` ya **no tiene base por omisión**. Caía en `postgresql://spaces:spaces@localhost:5433/spaces` — la base de desarrollo con DATOS REALES, con el rol `spaces` **superusuario y `rolbypassrls`** (verificado en `pg_roles`), o sea saltándose hasta la RLS `FORCE` de `usuarios`. Era inerte mientras el insert moría con 42P10; **T-01 lo volvió operativo y con eso lo activó**. Ahora `DATABASE_URL` es obligatoria: si falta, aborta con salida 1 diciendo qué falta y cómo pasarla (bash y PowerShell). Apuntar a `spaces` sigue siendo legítimo, pero hay que **decirlo**. Probado en una base desechable (`spaces_t02_test`, ya destruida): dos corridas, 1 usuario en `rgb`, 36 permisos. **T-01 hecha (13/08):** `bootstrap-auth.mjs` estaba roto de antes, no a punto de romperse — fallaba SIEMPRE con 42P10 porque `on conflict (email)` no infiere el índice funcional `usuarios_email_lower_uidx` (`db/schema.sql:72`). Aislado eso, salía el 23502 por `tenant_id`. Se arreglaron las dos en el mismo insert: el conflicto va por `lower(email)` y la organización se resuelve por SLUG (`select … from tenants where slug='rgb'`), nunca por uuid. Si el tenant falta, **aborta con salida 1**: esa forma de insert afecta 0 filas y termina con éxito, el no-op silencioso de R2. Sesión externa cerró el 07/08 y **commiteó todo** (`e7c3517`). Google verificado en producción; ADR 0012 enmendado; `/login` ya redirige con sesión. **DESPLEGADO el 10/08**: `password_resets` aislada en producción, con respaldo y ensayo previos. El invariante de hardening vuelve a dar 0 tablas sin RLS. |
| Z2 · Tenant 🔴 | LIBRE | — | — | — | — | — |
| Z3 · Inventario 🟡 | LIBRE | — | — | — | — | **Sin desplegar (10/08):** `rowToSitio` lleva un tercer parámetro `conMedia`; los listados van sin galería y `SiteFicha` la pide a `/api/sitios/:id/media`. Si añades un consumidor de `sitio.fotos`, recuerda que en los listados llega `[]` — mira `tieneFotos` |
| Z4 · Arrendadores 🔴 | LIBRE | — | — | — | — | **INC-07 desplegado el 10/08** (`7d27d25`). `arrendadores_tenant_rfc_uq` hace único el RFC por organización; el nombre repetido avisa con 409 y se puede confirmar. Antes de tocar el alta: `docs/adr/0013-altas-que-no-se-pueden-duplicar.md`. **11/08:** el alta guarda `direccion` (no lo hacía) y la lista permite editar arrendadores ya dados de alta — el `PATCH` existía y no lo llamaba ninguna pantalla. El contrato exige ese domicilio DOS veces |
| Z5 · Comercial 🟡 | LIBRE | — | — | — | — | **F1.3 hecha (13/08):** `cupoGlobalClientes()` lee `config_negocio` filtrando por `tenant_id` con el GUC, no solo apoyada en la RLS. La firma NO cambió. **INC-03 desplegado y CONFIRMADO el 10/08** junto con INC-09 (`484e768`). El barrido corrio a las 11:57 y movio las 2 campanas del ensayo en seco (`KFC`, `Propuesta para cliente 1`), ni una mas. `recomputarEstadoCampanas()` en `campanas-repo.ts` sincroniza `estado_comercial` con el calendario desde `/api/estado`. Si tocas la definición de «publicada», está en la constante `SQL_PUBLICADA` y la comparten las dos reglas — y `pipelineStage()` usa el mismo criterio para `instalada`: cámbialos juntos |
| Z6 · Operaciones 🟡 | LIBRE | — | — | — | — | — |
| Z7 · Finanzas 🔴 | LIBRE | — | — | — | — | — |
| Z8 · Integraciones 🟡 | LIBRE | — | — | — | — | **INC-02 DESPLEGADO el 10/08** (`c610592`), con backfill de 16 reservas. ⚠️ Quedan 2 pantallas de **eyro** sin asignar («Campaña lista — publicar a DOOHmain» y «pruebas_produccion», 2 aprobados cada una): el día que alguien las apruebe, el guard las rebotará. Se cierra desde un usuario de eyro. `DOOHMAIN_PUBLISH_ENABLED=1` en producción: la publicación es REAL. `publicarCampanaEnDoohmain()` ya no manda el producto cruzado — cada pantalla recibe su pieza asignada, con `veces` como `cantDia`. Sin `spots_por_dia` NO se manda cuota (las 16 reservas de prod lo tienen en NULL). Antes de tocar: [[2026-08-10]] |
| Z9 · Datos 🔴 | LIBRE | — | — | — | — | **F3.3 hecha (17/08), ROJA, NADA CORRIDO CONTRA PRODUCCIÓN:** una migración ya aplicada que cambia en disco **detiene la actualización**: salida **3**, con el archivo y **los dos checksums**, y **sin aplicar nada**. ⚠️ **El plan cita mal el punto de partida** (`Plan_Instancias_Soberanas_v3.md:1012`, «Falla hoy: no hay checksum»): F3.2 ya traía `checksumDe()` y el runner ya **escribía** el hash al registrar; lo que faltaba era **compararlo al reaplicar**. Medido antes de escribir, no supuesto. La comprobación va **antes de todo, incluido `--pendientes`** —«aborta» tiene que significar sin tocar nada, y `--pendientes` es la orden que se teclea justo antes de actualizar—; el rojo lo enseñó: con `--con-datos` el runner llegaba a **aplicar** la migración de datos pendiente. ✅ **Las filas `'backfill'` se saltan la comprobación** y eso es lo que impide negarse a actualizar **toda la flota**: son las 65 que el droplet aplicó a mano sin guardar hash de origen (`20260812_schema_migrations.sql:51-56` lo declara literal), y **T-04 editó dos de esos archivos**. Medido en las dos direcciones: en el droplet se saltan por la marca; en una **instalación nueva** no queda ni una fila `'backfill'` —el `on conflict … do update` del runner las reescribe en la misma pasada— y cuadran porque el hash se calcula sobre el archivo **ya editado**. 🔑 **`--forzar-checksum` EXIGE el nombre del archivo — desviación consciente del paso 3** (`:1018-1019`, que la describe suelta): suelta perdonaría a bulto cualquier migración alterada, presente y futura, y se quedaría puesta para siempre en un `update.sh`. Con nombre, la bandera **se comprueba** —si ese archivo no diverge, o está como `'backfill'`, salida 1— que es la lección de `--instalacion-nueva` aplicada a lo único verificable aquí: no afirma un estado de la base, afirma una decisión sobre un archivo concreto. **No reaplica nada** y actualiza `checksum` pero **no `aplicada_en`** (esa fecha dice cuándo se aplicó, y forzar no aplica), así el escape **no se vuelve permanente**. ❌ **Fuera de alcance a propósito**: una fila registrada **cuyo archivo no está en la imagen** no dispara nada — es otra discrepancia y merece su propia decisión, no colarla aquí. **+8 e2e (172 + 1 saltada en 15); 821 unitarias sin cambio.** Recalculadas tres citas de `migraciones.e2e.test.ts`: `ordenar()` se define en `migrar.mjs:76` y se llama en `:236`, el `on conflict … do update` es `:627-629` y el contrato de salidas `:21-32`. **F3.2 tercer ciclo hecho (17/08), ROJO, NADA CORRIDO CONTRA PRODUCCIÓN:** **`--instalacion-nueva` ya no se cree a ciegas.** Afirmaba un hecho y nadie lo comprobaba en la dirección peligrosa: sobre una base **con historia y sin registro** —el droplet de hoy— el runner se la creía y le **reaplicaba las 67 migraciones con salida 0**, medido y visto en rojo antes de arreglarlo. Y el mensaje del guard **le pone al operador esa línea exacta para copiar**, o sea que no era un caso rebuscado: era el siguiente comando que iba a teclear. Ahora se verifica: **salida 1**, nombrando las tablas que la delatan y quién las crea, y **sin aplicar nada**. La señal se **DERIVA del repositorio, no se cablea**: las **11 tablas** que crean las migraciones y `db/schema.sql` no (`almacen_activos`, `password_resets`, `licencias`, `identidades_externas`…). Las dos partes viajan en la imagen (`Dockerfile:94-95`), así que se deriva igual en el droplet. ⚠️ **Tablas y NO índices, a propósito**: un `constraint … unique` dentro de un `create table` de `schema.sql` crea un índice con ese nombre **sin** `create index` que lo delate → se derivaría como testigo y **una instalación legítima quedaría rechazada**, que es justo lo que la bandera existe para permitir. **Cobertura y su límite**: la primera migración con tabla propia es `20260716_doohmain_playlogs.sql`, así que una base parada **antes** de esa fecha es indistinguible de una nueva por este criterio — ninguna instancia real está ahí (el droplet va por 20260810) y **la ventana peligrosa `[20260723, 20260807)` queda cubierta desde su primer archivo**, `20260723_almacen.sql`. **Fail-closed en las tres formas de no poder verificar** (sin `schema.sql`, sin señal derivable, sin poder preguntar a la base): salida 1 y nada aplicado. 🐤 **La señal puede caducar y hay canario**: `scripts/migrar.test.ts` cablea `almacen_activos` —el único nombre escrito a mano de todo el mecanismo— y se pone rojo el día que se renombre, se retire o entre en `schema.sql`; si lo ves, reelige canario, no borres la prueba. ❌ **Lo que NO se hizo, y fue decisión de Jochelo el 17/08**: la bandera **no** desaparece a favor de la señal — decidir solo por la señal convierte la pregunta explícita en otra heurística, y esa sí es **fail-open**. **Retiradas tres afirmaciones falsas y medibles** («no hay forma de distinguirlas»): lo cierto es lo más débil y preciso, que **la heurística reutilizada —la del backfill— no las distingue**. Y **recalculadas tres citas** de `migraciones.e2e.test.ts` que apuntaban mal: el `on conflict … do update` es `migrar.mjs:453-455`, `ordenar()` se define en `:68` y se llama en `:186` (y en `db-e2e.ts:145`), y el contrato de códigos de salida es `:18-24` (citado `:15-19` **se perdía el 0 y el 2**). ⛔ **Esas cuatro cifras son de la versión de `migrar.mjs` de F3.2 y quedaron superadas**: las vigentes son las de F3.3, más arriba en esta misma celda — `ordenar()` se define en `:76` y se llama en `:236`, el `on conflict … do update` es `:627-629` y el contrato de salidas `:21-32` (reverificadas el 17/08). En la versión de hoy `:68` es un `}`, `:186` un comentario, `:453-455` un mensaje de bandera y `:18-24` corta el bloque de códigos de salida. Solo `db-e2e.ts:145` sigue siendo correcta. **+8 unitarias (821 en 74) y +2 e2e (164 + 1 saltada en 15).** 🟠 Sigue sin poder pasar la 2.ª línea del comando de verificación del plan (`:994-998`). **F3.2 segundo ciclo hecho (17/08), ROJO, NADA CORRIDO CONTRA PRODUCCIÓN:** el runner **ya no supone que «sin registro» = «instancia nueva»**. Si no existe `schema_migrations` **y** la base tiene datos, se para con **salida 1** y nombra las dos salidas en vez de elegir una. ⚠️ **El punto que hay que entender antes de tocar esto: la heurística del backfill —existe `tenants` y tiene filas— NO distingue las dos bases.** Sí las distingue la señal derivada que usa el guard desde `d31a7b8`: las **11** tablas que crean las migraciones y que `db/schema.sql` no crea. Decir «son indistinguibles» a secas era falso y está medido — 359 columnas contra 512** — después de `schema.sql` la instalación recién nacida y el droplet rezagado tienen las dos el tenant `rgb` (`db/schema.sql:598`) y ninguna registro; y **las dos suposiciones hacen daño en silencio**: tratar la rezagada como nueva le reaplica meses de historia (y una base parada en `[20260723, 20260807)` aborta a mitad), y tratar la nueva como rezagada da por aplicadas 65 migraciones que **nunca corrieron**, con `schema.sql` siendo un SUBCONJUNTO al que le faltan 143 columnas (`db-e2e.ts:107-112`) — esquema incompleto y ni un error. Por eso nace la bandera **`--instalacion-nueva`**, **desviación consciente del plan** (F3.2 no la contempla): sin ella no se pueden sostener a la vez sus dos criterios de aceptación, y quien sabe cuál de los dos casos es, es el aprovisionamiento (Fase 5), no el script. Con la bandera sobre una base que **ya** tiene registro, también salida 1. La heurística de «con historia» es la **misma, literal**, que la del backfill (`20260812_schema_migrations.sql:99-110`): reutilizada, no inventada — si divergieran, runner y backfill discreparían sobre qué es una instancia nueva y eso no da error, deja un registro que miente. ✅ **Cubre de paso la tercera rotura de T-04** (ventana `[20260723, 20260807)`): no se repara reaplicando, se evita **no reaplicando**. ⚠️ **T-04 movió el terreno y hay que medirlo, no suponerlo**: el rojo del caso rezagado ya **no** aborta a los 27 como vio la auditoría — reaplica los **68 y sale 0**, o sea que el fallo se había quedado **sin síntoma**. Cerrados también: el registro que falla sale **2** y no 1 con volcado de pila (nombra lo aplicado sin constar), y **`--pendientes` cae dentro del guard** — es la orden que se teclea justo antes de actualizar, así que dejó de contestar «Aplicadas: 0» sobre una base que las tenía casi todas. **+5 casos e2e** (162 + 1 saltada en 15 archivos); 813 unitarias sin cambio. 🟠 **La 2.ª línea del comando de verificación del plan (`:994-998`) no puede pasar**: hereda `cd apps/web` y el runner vive en la raíz. **T-04 hecha (17/08), ROJA, FUERA DEL PLAN y con permiso expreso de Jochelo para EDITAR DOS MIGRACIONES YA APLICADAS (R3):** la cadena ya se reaplica entera sobre una base que ya la tiene. Eran **DOS roturas, no una** — la primera aborta la pasada y **tapa** a la siguiente, así que el censo hay que hacerlo continuando tras cada fallo: `20260720_hard1_usuarios_rls.sql` (`cannot change return type of existing function`) y `20260729_datos_contrato_documento.sql` (`add constraint` no admite `IF NOT EXISTS`). ⚠️ **La causa escrita en la fila de F3.2 era la equivocada**: quien cambia el tipo de retorno de `auth_usuario_por_sesion` es `20260804_reautenticacion_individual.sql:70-71`, **no** `20260806_identidades_externas.sql` ni `20260807_password_resets_rls.sql` — esas dos crean funciones nuevas y propias y no tocan las de julio. Patrón aplicado: **guarda delante, cero cambio de semántica**; se verificó objeto a objeto (columnas, RLS+FORCE, constraints, índices, políticas, funciones con su retorno y su cuerpo, ACLs, enums, triggers, comentarios) que **una base virgen queda IDÉNTICA** a la de `c29d700`. En `auth_usuario_por_sesion` se usó `to_regprocedure(...) is null` y **no** `drop function if exists`, a propósito: dropear degradaría a la versión de 7 columnas mientras la cadena vuelve a la migración de agosto, y `auth.ts:116-117` pide `debe_cambiar_password` en CADA petición autenticada. `db/schema.sql` **no se tocó**; `aislamiento.e2e.test.ts` pasó **sin tocarse**. Nace `apps/web/lib/test/reaplicacion.e2e.test.ts` (4 casos, ~3 s): la segunda pasada **no la cubría nadie** —`recrearEsquema()` aplica siempre sobre base vaciada— y por eso dos migraciones de JULIO llegaron rotas a agosto. ✅ **Editar los archivos cambia su checksum pero NO dispara F3.3**: en el droplet están registrados como `'backfill'`, y `20260812_schema_migrations.sql:51-56` declara que la comprobación de integridad se los salta a conciencia. Si citabas líneas de `20260720_hard1_usuarios_rls.sql`, **recalcula**: RLS+FORCE `:109-114`→`:136-141`, ASSERT `:119-130`→`:146-157`, las tres funciones `:40-87`→`:40-114`. **F3.2 hecha (17/08), ROJA, NADA CORRIDO CONTRA PRODUCCIÓN:** nace `scripts/migrar.mjs`, el runner que aplica lo PENDIENTE y lo registra. **`DATABASE_URL` es obligatoria — desviación consciente del paso 2 del plan** (`Plan_Instancias_Soberanas_v3.md:979-980`, que pedía la misma resolución que `apply-migration.mjs:16-24`): ese default es la base de desarrollo con datos reales y rol superusuario con `BYPASSRLS`, y este script ejecuta DDL, no una fila — mismo criterio que T-02 aplicó a `bootstrap-auth.mjs:10-22`. Sin la variable, salida 1. El mapa **`ANTES_DE` ya no está duplicado**: vive en el runner y `db-e2e.ts` importa `ordenar()` (si citabas `db-e2e.ts:144-151`, recalcula). El tipo `@tipo: datos` se lee de la **PRIMERA línea**, no del contenido: `20260812_schema_migrations.sql` menciona esa cadena en prosa (`:44` y `:168`) y un filtro por contenido se saltaría en silencio justo la migración que crea la tabla de registro. ⚠️ **El runner NO levanta una base virgen él solo**: `20260729_licencias_permisos.sql:96-97` aborta sin rol de aplicación y 13 migraciones dependen de él; la secuencia real es rol de app → `schema.sql` → migraciones, y de las dos primeras no se encarga nadie (`Dockerfile:94-95` no lleva `dev-rol-app.sql`). La prueba reproduce el prólogo de `recrearEsquema()`; el hueco sigue abierto para la Fase 5. ⚠️ **El ASSERT de `20260812_schema_migrations.sql:221-223`** (`archivo >= '20260812'` sobre toda la tabla) **se vuelve falso positivo en cuanto el runner registre esas dos filas**: reaplicar esa migración abortaría, y `deploy.yml:141-148` reaplica todas en cada despliegue. Hoy no muerde —nadie ha corrido el runner en el droplet— y se retira en F3.6. **F3.1 hecha (14/08), ROJA, SIN APLICAR EN PRODUCCIÓN:** nace `schema_migrations (archivo pk, checksum, aplicada_en, tipo)`, **exenta de RLS a proposito** como `folios_consecutivos` — con RLS y sin `app.tenant_id` devolveria cero filas sin error y el runner concluiria que no hay nada aplicado. El backfill lleva la lista de 65 archivos **escrita dentro del `.sql`**: un `.sql` no puede listar un directorio (`pg_ls_dir` es de superusuario y lee el disco del servidor de BD, no el repo). **Y no envejece**, que es la objecion evidente: no es «lo que haya en `db/migrations/`», es *lo que la flota tenia aplicado al 2026-08-12* — un hecho historico. Deja fuera tres cosas, cada una por su motivo: **`20260812_sin_default_tenant.sql`** (escrita y NO aplicada en prod — marcarla dejaria el DEFAULT de `tenant_id` vivo en el droplet con el registro jurando lo contrario), **`20260731_calendario_meses_cortos.sql`** (es `@tipo: datos` y `deploy.yml:141-148` no las aplica nunca en un despliegue normal, asi que no consta que corriera) y **ella misma** (la registra quien la aplique, con checksum real). Heuristica: existe `tenants` **y** tiene filas; que la tabla deba EXISTIR es el punto — en base virgen queda vacia y el runner aplica todo. El orden lexicografico deja `schema_migrations` **antes** que `sin_default_tenant` (`c` < `i`), que es el que se quiere: **no hizo falta entrada nueva en `ANTES_DE`** (`db-e2e.ts:144-151`). Una base del repo pasa de 38 a **39 tablas**. **F1.2 hecha (13/08), SIN APLICAR EN PRODUCCIÓN:** `20260812_sin_default_tenant.sql` retira el `DEFAULT` de `tenant_id` de las **23** tablas (no 21) recorriendo el catálogo, no una lista. Un insert descuidado ahora truena con 23502 en vez de nacer etiquetado como `rgb`. El guard del paso 1 **no abortó**: 0 tablas con default y sin NOT NULL. `db/schema.sql` NO se tocó — sigue creando el default y la migración lo retira, que es como nace cualquier instalación nueva. Aplicarla en prod es **F1.5 y la corre una persona**. **Al día.** Últimas migraciones aplicadas en producción el 10/08: `20260807_password_resets_rls.sql`, `20260810_notificaciones_archivada_en.sql` y `20260810_arrendadores_rfc_unico.sql`. Producción en `7d27d25`; **pendiente de aplicar queda la de F1.2** (antes no había ninguna). Invariante: 0 tablas con `tenant_id` sin RLS+FORCE |
| Z10 · UI base 🟡 | LIBRE | — | — | — | — | `Button` se bloquea solo mientras su `onClick` esté en vuelo (10/08, A5). No cambia su API y ningún formulario se tocó. La guarda vive en `lib/clic-unico.ts` y tiene sus propias pruebas. **11/08: el menú va por fases del proceso** (`nav.ts`, campo `grupo` + `GRUPOS`); si añades un módulo, ponle grupo o `nav.test.ts` se pone roja |
| Z11 · Utilidades 🟢 | LIBRE | — | — | — | — | **T-03 hecha (14/08), ROJA por tema:** el candado de F0.3 se extiende a `.env.production.example` — la plantilla que se copia para montar una instancia real, y que quedaba fuera. Su línea 9 declaraba `COOKIE_DOMAIN=.{TENANT_SLUG}.spaces.com`, cookie **comodín de segundo nivel** del modelo de subdominios muerto el 12/08, contra el invariante 4 (`Plan_Instancias_Soberanas_v3.md:219`). **Inocua hoy** (`apps/web` no lee la variable; `cookieSesion()` `auth.ts:191-201` y `cookieCsrf()` `:216-226` no fijan `domain`, medido hoy) y **latente**: `_archive/api/src/core/auth/auth.routes.ts:17` sí la consume, así que el día que el `domain` se vuelva configurable, las instancias nacidas de esa plantilla comparten sesión por `*.spaces.com`. Fuera la línea, con el porqué escrito. `entorno.test.ts` pasa de 4 a **6 casos**: los dos nuevos leen la plantilla **dentro del `it`** (los de F0.3 la leen al cargar el módulo, y así un archivo ausente tumbaría también los casos de F2.6). El de la cookie nació rojo; el de `AUTOREGISTRO=0` nació verde y **se comprobó que muerde** poniéndolo en `=1`. **F0.3 hecha (14/08):** `entorno.test.ts` crece de 2 a **4 casos** y los dos nuevos leen `.env.example` **desde el disco**: exigen `AUTOREGISTRO=0` y que no haya `COOKIE_DOMAIN=` con valor. La mitad que faltaba de F0.3 era justo esa — el valor `=0` ya estaba (`0dbccb8`) pero **ninguna prueba lo miraba**, así que devolverlo a `=1` dejaba la suite verde y el CI mudo. Comprobado que la prueba muerde: con la plantilla en `=1` los dos casos se ponen rojos. La ruta se resuelve por `__dirname` (`lib` → `apps/web` → `apps` → raíz), no por el directorio de trabajo. Y **`COOKIE_DOMAIN=localhost` salió de la plantilla**: no lo leía ni una línea de `apps/` y contradecía el invariante 4 (cookies host-only, `auth.ts:191-201` y `:216-226`, citas verificadas hoy). **F2.6 hecha (14/08):** nace `lib/entorno.ts` con `autoregistroActivo()`, las banderas del despliegue que se deciden AL ARRANCAR. Que su prueba pueda existir es el punto: con el prefijo `NEXT_PUBLIC_` era IMPOSIBLE escribirla, porque Next inlinea el valor en el build y cambiar `process.env` entre dos llamadas no cambiaba nada. **F1.4 hecha (13/08):** nace `lib/host.ts` con `etiquetaDeHost()`, la **única** función que mira el `Host`. Una IP desnuda (`209.97.146.136`) ya no se confunde con el subdominio `209`. No resuelve marcas ni organizaciones: solo decide si el rewrite a `/portal` se dispara. Zona de entrada para agentes nuevos |
| Z12 · Docs 🟢 | LIBRE | — | — | — | — | ✅ **F3.8 · el pull reintenta con backoff, la migracion no reintenta nunca (18/08).** Zona tomada y liberada en el mismo commit; solo `infra/scripts/`, nada de `apps/web`. El `docker pull` de `update.sh` va ahora con **3 reintentos y esperas de 1 s, 5 s y 30 s**, y si a la cuarta no llega aborta con codigo 1 **antes de tocar nada** —ni respaldo, ni contenedor, ni base—. Lo importante es el LIMITE: **una migracion fallida no se reintenta jamas**, y eso quedo PROBADO contando llamadas al runner (E36 con codigo 2 y E37 con codigo 3: exactamente **una**), no escrito en un comentario. Bandera nueva **`--simular-fallo-pull`** —la del comando de verificacion del plan—: falla el pull a proposito sin llamar a docker, asi que se ensaya la politica sin cortarle la red al droplet. Corrida en local: `salida: 1`, `grep -c reintento update.log` = **3**, 36.9 s de reloj y el directorio de estado VACIO. El arnes gano el doble de `sleep` (comprueba el backoff por lo que se PIDE, no por el reloj) y paso de **32·121** a **37 escenarios · 165 comprobaciones · 0 rojas**, con **10 mutantes · 0 escapan**; los tres nuevos: pull sin reintentos, backoff aplanado a 1 s y —el que corrompe bases— **reintentar la migracion fallida**. Los 21 rojos se vieron ANTES de tocar `update.sh`. **La cuarta fila de la tabla del plan (reporte al padre) es F6.4: queda DOCUMENTADA en el README y sin implementar.** No se toco la restauracion de §7a, donde vive D1. · ✅ **F3.4 · los cuatro defectos del ensayo local, corregidos (18/08).** El ensayo salio DEMOSTRADO en los nueve puntos y dejo cinco defectos; estos cuatro se arreglaron con su escenario EN ROJO antes del arreglo. **D2 · el que podia tirar un release sano:** el `\|\| echo 000` del health check concatenaba un SEGUNDO codigo, porque `-w '%{http_code}'` ya imprime uno pase lo que pase. Con un fallo de conexion solo se veia feo en el log (`intento 1/5 -> 000000`); pero con un `curl` que alcanza a leer el **200** y LUEGO sale != 0 —`--max-time` agotado a mitad del cuerpo, contenedor recien arrancado y lento— la variable valia `200000`, no casaba con `200`, **y esa es la linea que gobierna si se restaura la base**: un release SANO acababa tirado por un `pg_restore` que ademas pierde lo escrito desde el respaldo. Ahora la salida y el codigo de salida de `curl` se recogen POR SEPARADO y solo se cae a `000` si no imprimio nada. **D3 ·** los dos codigos 5 de la restauracion (`update.sh` §7a) decian «Una persona tiene que mirar esto» y callaban que en ese punto **no hay servicio** y que el contenedor viejo quedo aparcado y PARADO como `${CONTENEDOR}-anterior`. Ya dicen «La instancia queda SIN servicio» —como sus hermanas— y traen el **comando de rescate exacto**, `docker rename space-os-anterior space-os && docker start space-os`, **calculado** y no fijo: si el rename de 5b no llego a hacerse, el viejo conserva su nombre y el mensaje dice `docker start space-os` a secas. Medido en el ensayo: rescate en 8 s. **D4 (primera mitad) ·** el respaldo de 0 bytes ya no se queda junto a los buenos: se borra al abortar por `BACKUP VACIO`. **La poda de `DIR_RESPALDOS` NO se hizo aqui a proposito** —el script no borra nunca los anteriores y en una instancia real son gigas por noche—: es materia de **F3.7**. **D5 ·** el codigo 2 con la base intacta afirmaba «Tipicamente no pudo conectar o no pudo abrir la transaccion» y en el caso medido la causa fue una migracion que fallo contra un objeto ya existente; ahora **remite** al mensaje del runner, que va impreso justo encima y si es exacto. Arnes: **32 escenarios · 121 comprobaciones · 0 rojas** y **7 mutantes · 0 escapan** (~10 min; el septimo devuelve el `\|\| echo 000` y cae con 6 comprobaciones en rojo). Los 12 rojos se vieron ANTES de tocar `update.sh`. typecheck limpio y 821 unitarias en verde, sin cambio: este commit no toca `apps/web`. 🔴 **D1 NO se toco y sigue ABIERTO:** la vuelta atras no devuelve el esquema — `pg_restore --clean --if-exists` solo suelta los objetos QUE ESTAN EN EL DUMP, asi que los que creo el release fallido sobreviven, y con una migracion no idempotente la instancia se atasca en **codigo 2 en cada corrida del cron**. Toca migraciones, es ROJO, exige decision de diseno y espera a Jochelo. Mientras tanto, un codigo 4 significa «la instancia volvio», no «la base volvio tal cual estaba». · 🔴 **F3.4 SEGUNDO CICLO (17/08) — la primera version NO restauraba la base.** La auditoria de `acbbe0b` salio ROJA por dos cosas, las dos reproducidas contra la imagen real y bases desechables antes de tocar nada: (1) `update.sh` decidia si restaurar leyendo la PROSA del runner con un `sed` que exigia un punto pegado a «aplicadas», y `migrar.mjs:694-696` imprime `67 aplicadas, 1 de datos pendientes.` en cuanto hay una migracion `@tipo: datos` pendiente — y la hay, `20260731_calendario_meses_cortos.sql`, cuya primera linea ademas lleva CRLF. La cuenta caia a 0, el log decia «no corrio ninguna migracion: la base no se toca» y `pg_restore` recibia CERO llamadas: **el contenedor volvia y la base no**, en silencio; (2) el codigo 2 MENTIA en el log — con `se aplicaron 66 migraciones y no se pudieron registrar` (`migrar.mjs:687-692`, alcanzable HOY porque la imagen no lleva `20260812_schema_migrations.sql`) escribia «suele ser que no pudo conectar», justo sobre la unica pregunta que el 2 existe para responder. 🔑 **El arreglo no es un tercer patron: el script deja de leer prosa.** Toma una HUELLA de la base (columnas con sus DEFAULT, indices, restricciones, politicas RLS, funciones + contenido de `schema_migrations`) ANTES y DESPUES de migrar y compara. Medido contra Postgres real: distingue base virgen / 66 migraciones sin registro / 67 con registro, **funciona aunque `schema_migrations` no exista** (que es justo el caso del 2), y **no se mueve con un `insert`** de la version anterior sirviendo entre las dos lecturas. Si la huella no se puede leer ANTES, el update se para sin migrar (codigo 1); si no se puede releer DESPUES, **restaura por prudencia**. El numero de migraciones del log es la resta de filas de `schema_migrations` y es DECORADO. ⚠️ **El arnes ahora esta EN EL REPOSITORIO** (`infra/scripts/pruebas-update.sh`): la version anterior afirmaba «18 escenarios y 58 comprobaciones» que no existian en ningun sitio. Hoy `bash infra/scripts/pruebas-update.sh` imprime **28 escenarios · 101 comprobaciones · 0 rojas** y `--mutantes` imprime **6 mutantes · 0 escapan** (~4 min). Cada mutante se VALIDA antes de correrlo —una sola linea de diff, mismo numero de lineas, `bash -n` limpio— porque el ciclo anterior tuvo un falso verde por un `sed` que dejo el archivo vacio y «paso». Los dos que el arnes viejo NO cazaba: **quitar `export DATABASE_URL`** (en un servidor `docker run --env DATABASE_URL` no pasa nada y fallan TODAS las migraciones) y **`pg_restore` sin `--clean --if-exists --single-transaction`** (la vuelta atras moriria objeto por objeto). Tres menores mas: el contenedor nuevo se retira **por ID y no por nombre** —si el `rename` de 5b fallo, el nombre lo lleva el contenedor VIEJO y se borraba justo lo que renombrar pretendia conservar—; la contrasena sale de `argv` de `pg_dump`/`pg_restore` y viaja por `PGPASSWORD` (era visible en `ps`); y **la ventana de corte queda escrita**: conmutar es `stop`+`run`, ~10-20 s con un release bueno y **hasta ~3 min** con uno malo, asi que «el owner no se entera» hay que leerlo como «se queda en la version anterior y no pierde datos», no como «no hay corte». **Sigue SIN correrse en ningun servidor: el ensayo es F3.5.** · **F3.4 primer ciclo (17/08), ESCRITA Y NO CORRIDA EN NINGUN SERVIDOR:** nace `infra/scripts/update.sh` (+ `infra/scripts/README.md`). La instancia jala su canal, respalda, migra y **solo entonces** conmuta; si la salud falla, se devuelve sola. 🔴 **El paso 5 del plan no se puede cumplir literalmente y esto hay que saberlo antes de tocar nada**: manda correr `node scripts/migrar.mjs` **dentro de la imagen**, pero `Dockerfile:94-95` copia `db/schema.sql` y `db/migrations` y **NO copia `scripts/`** — medido contra la imagen real, `/app` trae `apps db node_modules package.json` y sin montaje el contenedor muere con `MODULE_NOT_FOUND`. Solución sin tocar F2.2 (auditada): **se MONTA** el runner de la instancia en `/app/scripts/migrar.mjs`, y funciona porque el runner resuelve rutas desde su propio archivo (`scripts/migrar.mjs:43-48`) → lee **las migraciones de la IMAGEN**. Medido con la imagen real y una base desechable (`spaces_f34_test`, ya destruida): contesta **`67 pendientes`**, las 67 de la imagen, con **68** en el repositorio — o sea que la prueba distingue de verdad imagen de repo. ⚠️ **El costo**: el runner queda versionado con el APROVISIONAMIENTO y no con la imagen; una instancia aprovisionada antes de F3.3 migraria **sin comprobacion de checksum** aunque jale imagenes nuevas. El arreglo duradero es **una linea `COPY scripts/migrar.mjs` en el `Dockerfile`** → **tarjeta para el orquestador, NO se toco**. El script ya lo prevee: sonda la imagen y si trae el runner no monta nada. 🔑 **Los cuatro codigos del runner se propagan uno a uno** (`migrar.mjs:21-32`), que es justo donde un `set -e` los aplanaria: **1** no empieza (nada tocado) · **2** la base PUDO CAMBIAR (no conmuta, **no restaura**: la version anterior esta sirviendo y un `pg_restore --clean` sobre base viva tumbaria una instancia que funciona) · **3** historia distinta (nada aplicado) · y propios: **4** vuelta atras BUENA, **5** vuelta atras fallida, **75** candado tomado. El mensaje del guard «base con datos y sin `schema_migrations`» —el estado del droplet de HOY— se **vuelca al log**, no se traga: trae el comando exacto que falta. 📌 **`schema_migrations` viaja DENTRO del dump**, asi que restaurar devuelve esquema y registro al mismo instante; si la restauracion no corre, el registro nombra migraciones que la imagen anterior no tiene y **eso no aborta nada a proposito** (el hueco que F3.3 dejo fuera existe para que esta vuelta atras sea posible). Se restaura **solo si la huella de la base dice que cambio** (segundo ciclo; antes se leia la prosa del runner y ESO era el rojo). **Un respaldo vacio DETIENE el update** (`BACKUP VACIO`), criterio de `deploy.yml:117-125`. ⚠️ Su verificacion ERA una promesa: afirmaba «18 escenarios y 58 comprobaciones» que no estaban en el repositorio (arreglado en el segundo ciclo, arriba). ⚠️ **Asume un contrato de aprovisionamiento que la Fase 5 tiene que cumplir**: `/etc/space-os/instancia.env` (0600), `/etc/space-os/app.env`, `/opt/space-os/migrar.mjs`, `flock`, `pg_dump`. Y **DATABASE_URL es la PRIVILEGIADA**, no la de `spaces_app`: si `instancia.env` y `app.env` apuntan a **bases distintas**, el script se para. Falta: el ensayo en un servidor es **F3.5** y el respaldo sale del droplet en **F3.7**. **F2.4 hecha (17/08), ESCRITA Y NO CORRIDA:** nace `.github/workflows/promover.yml`. Mueve `estable` **reetiquetando** la imagen que hoy lleva `beta`: un solo job, **sin `checkout`** —no tener el código delante es lo que impide reconstruir por descuido— y sin un solo `docker build`. 🔑 **Reetiquetar es `docker buildx imagetools create` sobre el DIGEST, no `pull` + `tag` + `push`**: lo segundo pasa por el demonio local, que guarda **una** plataforma y **vuelve a serializar** el manifiesto al empujar → digest distinto, o sea `estable` dejaría de ser el artefacto validado. Y el origen es el digest y no la etiqueta `beta` porque entre la comprobación y el reetiquetado puede entrar un release nuevo y moverla. **Tres puertas:** la versión existe **y es la de `beta` hoy** · DEMO contesta 200 en `/login/` y `/api/auth/metodos/` (la misma ruta de salud que usa F3.4, y por el mismo motivo) · **después de mover, se COMPRUEBA que el digest no cambió** — el criterio de aceptación es medible, así que se mide en el run. ⚠️ **El patrón de la versión es ESTRICTO** (`^v[0-9]+\.[0-9]+\.[0-9]+$`), **al revés que `release.yml:207`, y no se unifican**: `-rc1` es material de `beta`; en `estable` sería una precandidata corriendo en la flota entera. ⚠️ **Consecuencia de la puerta 1:** si ya salió una `beta` más nueva, la vieja **ya no se promueve por aquí** — devolver la flota es un *rollback*, y el resumen del run deja escrito su comando con el digest al que apuntaba `estable` antes (capturado **antes** de tocar nada; en la primera promoción no hay a dónde volver y lo dice, en vez de imprimir un comando impegable). ⚠️ **El smoke dice que DEMO responde, NO que DEMO corra esa versión**: eso solo lo dirá `/api/version` (F6.1, que además exige `X-Flota-Token`); el workflow ya manda la cabecera y compara **si** hay secreto, y mientras no exista la ruta escribe «NO COMPROBADO» en el resumen — un verde que se leyera como «DEMO corre esta versión» sería peor que nada. `environment: flota` es el gancho para exigir aprobación humana y **hoy no frena nada** (un entorno sin reglas se crea al vuelo). 🔴 **Debajo de este botón no hay red**: promover manda a **toda la flota** a jalar la imagen y las que ya jalaron no vuelven solas (rollback local = F3.4, que desde el 17/08 ESTA ESCRITO --`infra/scripts/update.sh`, commit acbbe0b-- pero NO corrido en ninguna maquina, y su primera auditoria salio ROJA: la vuelta atras no restauraba la base). Verificado por lectura: YAML parseado, `bash -n` en los **9** bloques `run` y **35 casos** de guard con dobles de `docker`/`curl` (sin red) — el ejercicio encontró y arregló el comando de vuelta atrás roto de la primera promoción. **F2.3 hecha (17/08), ESCRITA Y NO CORRIDA:** nace `.github/workflows/release.yml`. Un tag `v*.*.*` corre la suite **completa** y publica la imagen con **dos** etiquetas, la versión y `beta`. **El gate es el orden y no una comprobación**: el push es el último paso del último job y ese job cuelga de `needs: pruebas`, así que `beta` no se puede mover sin typecheck + unitarias + e2e en verde. ⚠️ **Su comando de verificación es de servidor** (`plan:790-795`: `git push` de un tag y `gh run`), así que **no se corrió nada**: sale como tarjeta humana y va **después de TH-P4**, que a su vez espera a **P3**. Tres cosas que el plan no sabía y sin las cuales el workflow sería rojo siempre: (1) **el job de pruebas construye antes de las e2e** — `servidor-e2e.ts:31` arranca con `npx next start`, que reutiliza el build y no construye, y un runner parte de un clon limpio sin `.next/BUILD_ID` (las 15 e2e morirían por timeout tras 636 s, con un rojo que no dice nada del código); (2) **la base del runner la monta el arnés, no el YAML** — el servicio solo crea `spaces_e2e` (lo exige el guard de `db-e2e.ts:43-60`) y el rol `spaces_app` lo crea `recrearEsquema()` con `db/dev-rol-app.sql` antes de nada, que es lo que salva el `raise exception` de `20260729_licencias_permisos.sql:96-97` — **13** migraciones exigen ese rol; montarlo por otro camino sería una segunda copia del procedimiento; (3) **dos conexiones**, `DATABASE_URL_TEST` con el superusuario y `DATABASE_URL_TEST_APP` con `spaces_app`, o el aislamiento pasaría por casualidad --lo cual es FALSO y lo corrigio la auditoria del 17/08: sin esa variable el arnes respalda con `spaces_app@localhost:5433` (`db-e2e.ts:79-81`), o sea el Docker local, que en un runner da ECONNREFUSED y rojo ruidoso, no un aislamiento silencioso. Las dos conexiones siguen siendo necesarias; el motivo era otro. 🔑 **Ni el registry ni el token están cableados**: destino en `vars.REGISTRY` y login elegido por `vars.REGISTRY_TIPO` (`docr`/`ghcr`), fail-closed si falta cualquiera de los dos — el job se para **antes** de construir y escribe en el error los comandos de TH-P4. **`estable` NO se toca aquí**: eso es F2.4. ⚠️ **Lo único que no puede verificarse desde local**: que las e2e pasen **sin `.env`** — en local `next start` carga `apps/web/.env` y en el runner no habrá ninguno. Medido lo que se podía: ningún archivo e2e lee `DO_SPACES`/`RESEND`/`SPACE_EYE`/`CFDI`/`ADMOBILIZE`, y las `NEXT_PUBLIC_*` tienen valor por omisión en las seis rutas que las leen. **F2.2 hecha (14/08):** nacen `Dockerfile` y `.dockerignore` en la raiz. Imagen de **240 MB** sobre `node:20-alpine`, tres etapas (`deps` con `npm ci` → `build` con turbo → `runtime`), y **el esquema y las 67 migraciones viajan dentro**, en `/app/db` (invariante 1: en el servidor de una instancia no hay repo clonado). ⚠️ **`**/.env*` en el `.dockerignore` es lo unico que impide hornear credenciales**: el standalone se lleva el `.env` dentro (F2.1 lo midio: mismo md5, con `GOOGLE_CLIENT_SECRET`). Comprobado DENTRO de la imagen, no solo por el ignore: `find / -name '.env*'` devuelve vacio. Los patrones `*.xlsx`/`*.pdf` van **sin `**`** a proposito — con `**` se perderia `apps/web/public/plantilla-sitios-set.xlsx`. El `docker build` real y el smoke formal son de F2.5. **F2.1 (13/08):** el build sale además en `output: 'standalone'`. **Las dos formas de arrancar conviven y las dos se probaron**: `npm start` (`next start -p 3000`, lo que usa `ecosystem.config.js` en el droplet) da 200 en `/spaces-dooh/login/`, y `node .next/standalone/apps/web/server.js` también. ⚠️ El standalone **no lleva `.next/static` ni `public/`** — el CSS del login da 404 si se arranca tal cual. No es un defecto: copiarlos es paso del `Dockerfile` (F2.2) y se verifica en F2.5. `next.config.mjs` creció 10 líneas: **las citas `next.config.mjs:N` de cinco notas se recalcularon** (basePath 8-9→19-20, HSTS 40→51, alias webpack 47-56→58-67, images 11-22→22-33). **T-01 (13/08):** los inserts de ejemplo de `db/README.md` ya ponen `tenant_id` resuelto por slug — `usuarios` y `clientes` están las dos en el bucle de RLS de `db/schema.sql:600-624`. Bóveda creada el 07/08, **validada contra el código el 10/08** y actualizada con la tarde del 10/08 ([[2026-08-10]]) |

> [!warning] V2-01 ya está en `main` — FUSIONADO el 10/08, pero SIN DESPLEGAR
> `/api/estado` deja de llevar el PDF de los contratos y las fotos de las
> pantallas. Verde en local (typecheck · 772 unitarias · 129 e2e · build), y
> `feat/estado-ligero` fusionada en `main`.
>
> **Lo que falta es desplegar y MEDIR.** Su métrica —`<500 kB`, `<1 s` en frío—
> **no está tomada**: la base local tiene 3 contratos sin adjuntos, así que solo
> se reproduce en producción.
>
> Al desplegar, comprobar **la firma `/firmar/[token]` y el export de
> contratos**: son los dos flujos que leían el documento.
>
> Del merge: el runbook de INC-02 conserva la versión de `main` (la EJECUTADA);
> la de la rama era la instantánea previa al despliegue.

> [!important] `eyro` es el tenant de PRUEBAS (confirmado el 10/08)
> Reclasifica el «pendiente» de Z8: las 2 pantallas sin creativo asignado son de
> `eyro`, o sea **datos de ensayo**, no un cliente esperando. Sigue siendo cierto
> que rebotarán con 409 si alguien las aprueba — pero eso es la guarda haciendo
> su trabajo, no una incidencia. Ver [[multi-tenancy-y-rls]].
>
> Lo que **no** cambia: `DOOHMAIN_PUBLISH_ENABLED=1`, así que lo que salga por
> `eyro` llega a pantallas de verdad.
>
> **Reinicio pedido y escrito, SIN EJECUTAR:**
> `docs/datos/20260810_reset_tenant_eyro.sql`. Borra todo `eyro` y lo recrea
> vacío. Antes de correrlo hay que **editar el correo del Dueño** (`\set
> duenio_email`) — el script aborta si no. Y **retirar de DOOHmain** lo que siga
> publicado: el borrado no lo baja de las pantallas y además pierde el rastro de
> qué era.

## Archivos de alto contacto

Claim aparte, aunque estés en otra zona. Lista completa en [[AGENTES]].

| Archivo | Estado | Agente | Desde |
|---|---|---|---|
| `apps/web/middleware.ts` | LIBRE | — | — |
| `apps/web/next.config.mjs` | LIBRE | — | — |
| `apps/web/lib/server/db.ts` | LIBRE | — | — |
| `apps/web/lib/server/auth.ts` | LIBRE | — | — |
| `apps/web/lib/server/errores.ts` | LIBRE | — | — |
| `apps/web/lib/server/uploads.ts` | LIBRE | — | — |
| `apps/web/lib/modulos.ts` | LIBRE | — | — |
| `apps/web/components/demo/shell/nav.ts` | LIBRE | — | — |
| `apps/web/components/demo/ui/*` | LIBRE | — | — |
| `apps/web/app/providers.tsx` | LIBRE | — | — |
| `packages/types/src/*` | LIBRE | — | — |
| `db/schema.sql` | LIBRE | — | — |
| `package.json` / `package-lock.json` | LIBRE | — | — |
| `docs/Registro_Cambios.md` | LIBRE | — | — |

## Cómo se rellena una fila

```
| Z5 · Comercial 🟡 | TOMADA | agente-3 | lib/server/propuestas-repo.ts | feat/comercial-descuento | 07/08 14:20 | descuento por volumen |
```

Al terminar, vuelve a `LIBRE` **en el mismo commit** que cierra el trabajo.
Si te interrumpen, `PAUSADA` + dónde exactamente te quedaste (y al diario).

## Relacionadas
[[AGENTES]] · [[zonas-de-riesgo]] · [[_plantilla-diaria]] · [[MOC-Proyecto]]
